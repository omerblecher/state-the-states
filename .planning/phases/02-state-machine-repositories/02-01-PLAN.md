---
phase: 02-state-machine-repositories
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/game/game_phase.dart
  - lib/features/game/game_mode.dart
  - lib/features/game/game_session.dart
  - lib/core/ticker.dart
  - test/features/game/game_session_test.dart
autonomous: true
requirements: [SCORE-01, SCORE-02, SESS-03]
must_haves:
  truths:
    - "GamePhase enum exposes idle, countdown, playing, paused, completed"
    - "GameMode enum exposes learn, statesMaster, geographicalMaster, grandMaster"
    - "Two GameSession values with identical fields compare equal and share a hashCode"
    - "copyWith(activePostal: null) clears the field via the sentinel pattern"
    - "FakeTicker.tick() invokes the registered onTick callback exactly once"
  artifacts:
    - path: "lib/features/game/game_phase.dart"
      provides: "GamePhase enum (idle/countdown/playing/paused/completed)"
      contains: "enum GamePhase"
    - path: "lib/features/game/game_mode.dart"
      provides: "GameMode enum with statesMaster (renamed from flagsMaster)"
      contains: "statesMaster"
    - path: "lib/features/game/game_session.dart"
      provides: "GameSession value object with activePostal/matchedPostals, copyWith sentinel, ==/hashCode"
      contains: "matchedPostals"
      min_lines: 70
    - path: "lib/core/ticker.dart"
      provides: "Ticker abstraction with RealTicker + FakeTicker seam"
      contains: "class FakeTicker"
    - path: "test/features/game/game_session_test.dart"
      provides: "GameSession equality + copyWith sentinel tests"
      contains: "matchedPostals"
  key_links:
    - from: "lib/features/game/game_session.dart"
      to: "lib/features/game/game_phase.dart"
      via: "import + GamePhase field"
      pattern: "import 'game_phase.dart'"
    - from: "lib/features/game/game_session.dart"
      to: "lib/features/game/game_mode.dart"
      via: "import + GameMode field"
      pattern: "import 'game_mode.dart'"
---

<objective>
Establish the pure-Dart contracts that every other Phase 2 file imports: the
`GamePhase` and `GameMode` enums, the `GameSession` value object (with the
`isoCode`→`postal` rename deltas), and the `Ticker`/`RealTicker`/`FakeTicker`
display-pulse seam. These are interface-first: the repositories (Plan 02) and the
notifier + observer (Plan 03) all depend on the types defined here.

Purpose: Removes the "scavenger hunt" — downstream plans receive these contracts
directly instead of exploring the codebase to learn field names and signatures.
Output: Four source files + the `GameSession` unit test proving equality,
hashCode, and the nullable-`activePostal` sentinel pass-through.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-machine-repositories/02-PATTERNS.md
@.planning/phases/02-state-machine-repositories/02-RESEARCH.md

<interfaces>
<!-- Contracts this plan creates. Downstream plans (02, 03) import these directly. -->

lib/features/game/game_phase.dart:
  enum GamePhase { idle, countdown, playing, paused, completed }

lib/features/game/game_mode.dart:
  enum GameMode { learn, statesMaster, geographicalMaster, grandMaster }

lib/features/game/game_session.dart:
  class GameSession — const ctor with required {phase, mode, score, elapsed,
  errorCount, hintsRemaining}, optional {activePostal (String?),
  matchedPostals (List<String> = const [])}; copyWith with _sentinel for
  nullable activePostal; ==/hashCode using _listEquals + Object.hash.
  NOTE: hintPenalty is NOT a field on GameSession — it is notifier-internal
  state (Plan 03), persisted separately in the snapshot (Plan 02).

lib/core/ticker.dart:
  abstract class Ticker { void start(void Function() onTick); void stop(); }
  class RealTicker implements Ticker (Timer.periodic 1s)
  class FakeTicker implements Ticker (exposes tick() for deterministic tests)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the GamePhase, GameMode enums and the Ticker seam</name>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\game\game_phase.dart (verbatim port source)
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\game\game_mode.dart (rename source)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ticker.dart (verbatim port source)
    - .planning/phases/02-state-machine-repositories/02-PATTERNS.md (complete file listings for all three)
  </read_first>
  <action>
    Create `lib/features/game/game_phase.dart` with `enum GamePhase { idle, countdown, playing, paused, completed }` — verbatim port, no changes.
    Create `lib/features/game/game_mode.dart` with `enum GameMode { learn, statesMaster, geographicalMaster, grandMaster }` — apply the single Claude's-discretion rename `flagsMaster` → `statesMaster` (the high-score keys in Plan 02 derive from these enum names).
    Create `lib/core/ticker.dart` verbatim from the Flags analog: `abstract class Ticker` with `start(void Function() onTick)` and `stop()`; `RealTicker` backed by `Timer.periodic(const Duration(seconds: 1), ...)`; `FakeTicker` storing `_onTick` and exposing a public `tick()` that calls `_onTick?.call()`. The tick is display-only per D-02 — it carries no elapsed-time semantics.
  </action>
  <verify>
    <automated>flutter analyze lib/features/game/game_phase.dart lib/features/game/game_mode.dart lib/core/ticker.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/game/game_phase.dart` contains `enum GamePhase` with exactly the five values idle, countdown, playing, paused, completed
    - `lib/features/game/game_mode.dart` contains the token `statesMaster` and does NOT contain `flagsMaster`
    - `lib/core/ticker.dart` contains `class FakeTicker` with a public `tick()` method and `class RealTicker` using `Timer.periodic`
    - `flutter analyze` on the three files exits 0 (no errors)
  </acceptance_criteria>
  <done>All three files exist, `flutter analyze` reports no issues on them, enum values match the interfaces block exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Port the GameSession value object with postal renames + write its tests</name>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\game\game_session.dart (port source — fields, copyWith sentinel, _listEquals, ==/hashCode)
    - .planning/phases/02-state-machine-repositories/02-PATTERNS.md §game_session.dart (line-by-line target with renames applied)
    - test/core/models/state_data_test.dart (in-repo test structure analog — group/test/expect conventions)
    - lib/features/game/game_phase.dart, lib/features/game/game_mode.dart (just created — the types GameSession imports)
  </read_first>
  <behavior>
    - Two GameSession instances with identical field values are `==` and have equal `hashCode`
    - `copyWith` replaces only the fields passed; unpassed fields retain prior values
    - `copyWith(activePostal: null)` clears the field (sentinel pass-through), while `copyWith()` with activePostal omitted preserves the prior value
    - `copyWith(matchedPostals: [...])` replaces the list; `==` uses element-wise `_listEquals` so two sessions with equal-content lists compare equal
  </behavior>
  <action>
    Create `lib/features/game/game_session.dart` as a direct port of the Flags analog with two field renames: `activeIsoCode` → `activePostal` (type `String?`) and `matchedIsoCodes` → `matchedPostals` (type `List<String>`, default `const []`). Keep all other fields verbatim: `phase` (GamePhase), `mode` (GameMode), `score` (int), `elapsed` (Duration), `errorCount` (int), `hintsRemaining` (int). Port the `static const Object _sentinel = Object();` copyWith sentinel pattern so `activePostal` can be set to null explicitly. Port `_listEquals`, `operator ==`, and `hashCode` (using `Object.hash` + `Object.hashAll(matchedPostals)`) verbatim with the rename applied. Do NOT add a `hintPenalty` field — it stays notifier-internal per RESEARCH.md §3.
    Then create `test/features/game/game_session_test.dart` with a `group('GameSession', ...)` covering: equality + hashCode for identical sessions; copyWith replaces-changed-only; copyWith(activePostal: null) clears via sentinel; matchedPostals list equality.
  </action>
  <verify>
    <automated>flutter test test/features/game/game_session_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/game/game_session.dart` contains the tokens `activePostal` and `matchedPostals` and does NOT contain `activeIsoCode` or `matchedIsoCodes`
    - `lib/features/game/game_session.dart` does NOT contain the token `hintPenalty`
    - `game_session.dart` imports `game_phase.dart` and `game_mode.dart`
    - `flutter test test/features/game/game_session_test.dart` exits 0
    - The test file asserts a `copyWith(activePostal: null)` clears the field AND an equality assertion on two identical sessions
  </acceptance_criteria>
  <done>GameSession compiles, all equality/copyWith tests pass, no isoCode tokens remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none in this plan) | Pure in-memory value types + enums; no I/O, no persistence, no external input crosses any boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure (COPPA) | GameSession field set | mitigate | GameSession carries ONLY game-state values (phase, mode, score, elapsed, errorCount, activePostal, hintsRemaining, matchedPostals) — zero persistent identifiers. Verified by field enumeration; serialization lives in Plan 02. |
| T-02-SC | Tampering | package installs | accept | No package-manager installs in this phase — all deps declared in Phase 1 (RESEARCH.md Package Legitimacy Audit confirms none added). |
</threat_model>

<verification>
- `flutter analyze lib/features/game/ lib/core/ticker.dart` reports no errors
- `flutter test test/features/game/game_session_test.dart` exits 0
- Grep confirms zero `isoCode` tokens remain: `grep -rn "isoCode" lib/features/game/` returns nothing
</verification>

<success_criteria>
- GamePhase, GameMode, GameSession, and Ticker all compile and are importable by downstream plans
- GameSession `==`/`hashCode`/`copyWith` behave correctly under the test suite (proves the round-trip foundation for SESS-03 Criterion #4 in Plan 02)
- statesMaster rename applied (no flagsMaster anywhere)
</success_criteria>

<output>
Create `.planning/phases/02-state-machine-repositories/02-01-SUMMARY.md` when done
</output>
