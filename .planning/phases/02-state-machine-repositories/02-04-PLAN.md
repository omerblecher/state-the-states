---
phase: 02-state-machine-repositories
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/audio/real_audio_service.dart
  - test/core/audio/audio_service_test.dart
autonomous: true
requirements: [WEL-04]
must_haves:
  truths:
    - "RealAudioService.init() with a missing asset leaves _initialized false and does NOT throw"
    - "RealAudioService.dispose() completes without throwing even when init failed (players assigned before the try block)"
    - "StubAudioService satisfies every AudioService method as a no-op without throwing"
    - "Both services expose the identical AudioService interface (init, playCorrect, playError, playAnthem, stopAnthem, setMuted, dispose)"
  artifacts:
    - path: "lib/core/audio/real_audio_service.dart"
      provides: "Hardened RealAudioService with documented unconditional dispose"
      contains: "dispose"
    - path: "test/core/audio/audio_service_test.dart"
      provides: "WEL-04 leak-free init/play/dispose + stub interface-parity tests"
      contains: "RealAudioService"
  key_links:
    - from: "test/core/audio/audio_service_test.dart"
      to: "lib/core/audio/real_audio_service.dart"
      via: "imports + exercises init/dispose"
      pattern: "RealAudioService"
    - from: "test/core/audio/audio_service_test.dart"
      to: "lib/core/audio/stub_audio_service.dart"
      via: "imports + asserts no-op interface parity"
      pattern: "StubAudioService"
---

<objective>
Close WEL-04 by HARDENING the existing Phase 1 audio service (no redesign) and
proving it with leak-free lifecycle tests. `RealAudioService` already guards
playback behind `_initialized` and assigns its three `AudioPlayer`s before the
asset-loading try block; this plan documents why `dispose()` is intentionally
unconditional (players always exist and are disposable even when `setAsset`
failed — Pitfall 8) and adds the test suite that proves init/play/dispose leak
no players and that `StubAudioService` passes the identical interface
assertions as a no-op.

Purpose: WEL-04 is a HARDEN-AND-TEST requirement, not a build. The audio
service is otherwise frozen until Phase 5 (real anthem render).
Output: One clarifying-comment edit to `real_audio_service.dart` + one new test
file covering both services. Independent of all game logic — runs in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-machine-repositories/02-PATTERNS.md
@.planning/phases/02-state-machine-repositories/02-RESEARCH.md

<interfaces>
<!-- The AudioService interface both implementations satisfy (Phase 1, frozen). -->

lib/core/audio/audio_service.dart:
  abstract interface class AudioService {
    Future<void> init();
    Future<void> playCorrect();
    Future<void> playError();
    Future<void> playAnthem();
    Future<void> stopAnthem();
    Future<void> setMuted(bool muted);
    Future<void> dispose();
  }

lib/core/audio/real_audio_service.dart (Phase 1, this repo):
  - three late AudioPlayer fields assigned at the top of init() BEFORE the
    try block; _initialized bool gates all playback methods.
  - dispose() currently disposes all three players unconditionally.

lib/core/audio/stub_audio_service.dart (Phase 1, this repo):
  - const StubAudioService(); every method is an async no-op.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the unconditional-dispose hardening in RealAudioService</name>
  <read_first>
    - lib/core/audio/real_audio_service.dart (current Phase 1 implementation — the file being edited)
    - lib/core/audio/audio_service.dart (the interface, for reference)
    - .planning/phases/02-state-machine-repositories/02-PATTERNS.md §real_audio_service.dart (the dispose() clarifying-comment delta)
    - .planning/phases/02-state-machine-repositories/02-RESEARCH.md §Pitfall 8 (why dispose is intentionally unconditional)
  </read_first>
  <action>
    In `lib/core/audio/real_audio_service.dart`, add a clarifying comment to the existing `dispose()` method explaining that disposal is intentionally unconditional and safe: `_correctPlayer`, `_errorPlayer`, and `_anthemPlayer` are assigned at the top of `init()` before the asset-loading try block, so they always exist even when `_initialized` is false (init failed after assignment), and `AudioPlayer.dispose()` is idempotent on partially-initialized instances. Do NOT add an `if (_initialized)` guard around dispose — that would be the regression Pitfall 8 warns against (it would leak the players when init failed). This is a documentation-only hardening; the disposal behavior is unchanged.
  </action>
  <verify>
    <automated>flutter analyze lib/core/audio/real_audio_service.dart</automated>
  </verify>
  <acceptance_criteria>
    - `dispose()` still calls `dispose()` on all three players unconditionally (no `if (_initialized)` guard added)
    - A comment in the `dispose()` method explains the players-assigned-before-try rationale
    - `flutter analyze lib/core/audio/real_audio_service.dart` exits 0
  </acceptance_criteria>
  <done>real_audio_service.dart documents the unconditional dispose rationale; analyze is clean; disposal behavior unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Write WEL-04 lifecycle + interface-parity tests for both audio services</name>
  <read_first>
    - lib/core/audio/real_audio_service.dart (just hardened — init/playCorrect/playError/dispose under test)
    - lib/core/audio/stub_audio_service.dart (the no-op to assert interface parity)
    - lib/core/audio/audio_service.dart (the shared interface)
    - .planning/phases/02-state-machine-repositories/02-PATTERNS.md §audio_service_test.dart (interface-parity + dispose-no-throw test pattern)
    - .planning/phases/02-state-machine-repositories/02-RESEARCH.md §Criterion #5 + §Pitfall 8
  </read_first>
  <behavior>
    - StubAudioService: init, playCorrect, playError, playAnthem, stopAnthem, setMuted(true), dispose all complete without throwing
    - RealAudioService: init() in the test environment (missing/unloadable asset) leaves _initialized false and does not throw
    - RealAudioService: dispose() completes (does not throw) even when init failed — no leaked players
  </behavior>
  <action>
    Create `test/core/audio/audio_service_test.dart`. Call `TestWidgetsFlutterBinding.ensureInitialized()` at the top of `main()`. Group `StubAudioService`: construct it and `await` each interface method (init, playCorrect, playError, playAnthem, stopAnthem, setMuted(true), dispose) asserting each completes — this is the no-op interface-parity assertion (Criterion #5). Group `RealAudioService`: construct it, `await service.init()` (assets are expected to fail to load under the test binding, driving `_initialized = false` gracefully via the existing try/catch), then assert `expectLater(service.dispose(), completes)` to prove the leak-free unconditional dispose (Pitfall 8). Both services are exercised through the `AudioService` interface type to make the parity explicit.
  </action>
  <verify>
    <automated>flutter test test/core/audio/audio_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/core/audio/audio_service_test.dart` references both `RealAudioService` and `StubAudioService`
    - The StubAudioService test awaits all seven interface methods without expecting any throw
    - The RealAudioService test asserts `dispose()` completes (e.g. via `expectLater(..., completes)`) after a failed/partial init
    - `flutter test test/core/audio/audio_service_test.dart` exits 0
  </acceptance_criteria>
  <done>Audio test file green; stub parity + real-service leak-free dispose both proven (WEL-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| bundled audio asset ↔ player | Asset load may fail (missing/corrupt asset, platform error); failure must degrade gracefully, never crash |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Denial of Service (resource leak) | RealAudioService.dispose | mitigate | Players are assigned before the asset try block and disposed unconditionally, so a failed init cannot strand undisposed AudioPlayers. Proven by the dispose-completes-after-failed-init test (Pitfall 8). |
| T-02-10 | Information Disclosure (COPPA) | audio service | accept | Audio service holds no persisted state and no identifiers — purely playback of bundled assets. No COPPA surface. |
| T-02-SC | Tampering | package installs | accept | No package-manager installs in this phase — just_audio declared Phase 1. |
</threat_model>

<verification>
- `flutter test test/core/audio/audio_service_test.dart` exits 0
- `flutter analyze lib/core/audio/` reports no errors
- `grep -n "if (_initialized)" lib/core/audio/real_audio_service.dart` does NOT match inside dispose() (Pitfall 8 regression guard)
</verification>

<success_criteria>
- RealAudioService init/play/dispose leak no players, dispose safe after failed init (WEL-04)
- StubAudioService is a no-op passing the identical interface assertions (WEL-04 / Criterion #5)
- No redesign of the audio service — only a clarifying comment + new tests
</success_criteria>

<output>
Create `.planning/phases/02-state-machine-repositories/02-04-SUMMARY.md` when done
</output>
