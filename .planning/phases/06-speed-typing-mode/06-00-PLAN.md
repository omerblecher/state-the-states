---
phase: 06-speed-typing-mode
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/features/typing/speed_typing_screen_test.dart
  - test/features/game/game_session_notifier_test.dart
autonomous: true
requirements:
  - TYPING-03
  - TYPING-06

must_haves:
  truths:
    - "test/features/typing/ directory exists and is importable by the Flutter test runner"
    - "speed_typing_screen_test.dart exists with a compilable placeholder test (not skipped)"
    - "game_session_notifier_test.dart contains a stateFixture() helper returning List<StateData> with at least 3 placeable states including Georgia (postal=GA) and one multi-word state (New York, postal=NY)"
  artifacts:
    - path: "test/features/typing/speed_typing_screen_test.dart"
      provides: "Compilable stub widget test file for SpeedTypingScreen"
      contains: "void main()"
    - path: "test/features/game/game_session_notifier_test.dart"
      provides: "State fixture helper for submitTyping() unit tests"
      contains: "stateFixture"
  key_links:
    - from: "test/features/typing/speed_typing_screen_test.dart"
      to: "lib/features/typing/speed_typing_screen.dart"
      via: "import — file does not exist yet in Wave 0; stub must NOT import the screen (it would fail to compile)"
      pattern: "TODO: import SpeedTypingScreen once Wave 2 creates it"
    - from: "test/features/game/game_session_notifier_test.dart"
      to: "lib/core/models/state_data.dart"
      via: "StateData constructor used to build fixture"
      pattern: "StateData\\("
---

<objective>
Create the test scaffolding required before any Phase 6 feature code is written. The Nyquist validation strategy marks these two files as Wave 0 gaps — Wave 2 widget tests will fail to compile without this directory and stub.

Purpose: Unblock Wave 2 widget tests (TYPING-03, TYPING-06) by establishing the test directory and a compilable stub. Unblock Wave 2 unit tests for submitTyping() by providing a minimal List<StateData> fixture.

Output: test/features/typing/ directory with a compilable stub test file; stateFixture() helper in the existing notifier test.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\code\Claude\StateTheStates\.planning\PROJECT.md
@C:\code\Claude\StateTheStates\.planning\ROADMAP.md
@C:\code\Claude\StateTheStates\.planning\phases\06-speed-typing-mode\06-CONTEXT.md
@C:\code\Claude\StateTheStates\.planning\phases\06-speed-typing-mode\06-RESEARCH.md
@C:\code\Claude\StateTheStates\.planning\phases\06-speed-typing-mode\06-VALIDATION.md

<interfaces>
<!-- Key types the executor needs. Extracted from codebase. -->

From lib/core/models/state_data.dart:
  class StateData {
    final String postal;      // two-letter code e.g. "GA"
    final String name;        // full name e.g. "Georgia"
    final List<String> pathStrings;
    final List<Path> paths;
    final BoundingBox boundingBox;
    final Offset centroid;
    final bool isPlaceable;   // DC = false; all 50 states = true
    final InsetGroup? insetGroup;

    const StateData({required postal, required name, required pathStrings,
      required paths, required boundingBox, required centroid,
      required isPlaceable, required insetGroup});
  }

  class BoundingBox {
    const BoundingBox({required double x, required double y,
      required double w, required double h});
  }

From test/features/game/game_session_notifier_test.dart (existing structure):
  - imports: flutter_riverpod, flutter_test, mocktail, game_state_repository,
    high_score_repository, ticker, game_mode, game_phase, game_session,
    game_session_notifier
  - class MockGameStateRepository extends Mock implements GameStateRepository {}
  - class MockHighScoreRepository extends Mock implements HighScoreRepository {}
  - void main() { setUpAll(...); ... group('...', () { ... }); }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create speed_typing_screen_test.dart stub</name>
  <files>test/features/typing/speed_typing_screen_test.dart</files>
  <read_first>
    - test/features/home/home_screen_test.dart — observe existing widget test structure, imports, ProviderScope wrapper pattern
    - test/features/game/game_session_notifier_test.dart — observe existing test file structure and imports
    - .planning/phases/06-speed-typing-mode/06-VALIDATION.md — Wave 0 requirements for this file
  </read_first>
  <action>
    Create the directory test/features/typing/ and write test/features/typing/speed_typing_screen_test.dart as a compilable stub.

    The file must:
    - Import flutter_test only (NOT import speed_typing_screen.dart — that file does not exist yet and would cause a compile error)
    - Declare void main() with a single placeholder test group labeled 'SpeedTypingScreen'
    - Include one test named 'placeholder — Wave 2 will fill this' that calls expect(true, isTrue) and passes immediately
    - Include a block comment at the top: // Wave 2 will import SpeedTypingScreen and add TYPING-03 and TYPING-06 widget tests.
    - Do NOT use skip:true — the test must actively pass so the suite stays green

    The stub exists solely to satisfy the compiler requirement that a test file in test/features/typing/ exists before Wave 2 adds real assertions.
  </action>
  <verify>
    <automated>cd C:\code\Claude\StateTheStates && flutter test test/features/typing/speed_typing_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File exists at test/features/typing/speed_typing_screen_test.dart
    - flutter test test/features/typing/speed_typing_screen_test.dart exits 0
    - File does NOT contain an import of speed_typing_screen.dart
    - File contains void main() and at least one test() call that passes
  </acceptance_criteria>
  <done>flutter test exits 0 for the new stub file; no import of the not-yet-created screen</done>
</task>

<task type="auto">
  <name>Task 2: Add stateFixture() helper to game_session_notifier_test.dart</name>
  <files>test/features/game/game_session_notifier_test.dart</files>
  <read_first>
    - test/features/game/game_session_notifier_test.dart — read the full file to find where to insert the fixture; observe existing imports and group structure
    - lib/core/models/state_data.dart — StateData constructor signature, BoundingBox constructor, InsetGroup enum, isPlaceable field
    - .planning/phases/06-speed-typing-mode/06-RESEARCH.md — "Resolving StateData Without package:collection" code example showing StateData field names used in submitTyping()
  </read_first>
  <action>
    Add a top-level function stateFixture() to the existing game_session_notifier_test.dart file. Insert it after the existing import block and before void main().

    Add these imports if not already present (add only what is missing):
      import 'dart:ui' show Offset, Path;
      import 'package:state_states/core/models/state_data.dart';

    The fixture function signature must be:
      List<StateData> stateFixture()

    It must return a List containing exactly 5 StateData entries:
      1. postal: 'GA', name: 'Georgia', isPlaceable: true, insetGroup: null
      2. postal: 'CA', name: 'California', isPlaceable: true, insetGroup: null
      3. postal: 'NY', name: 'New York', isPlaceable: true, insetGroup: null  (multi-word — tests D-02)
      4. postal: 'TX', name: 'Texas', isPlaceable: true, insetGroup: null
      5. postal: 'AK', name: 'Alaska', isPlaceable: true, insetGroup: InsetGroup.alaska

    For each StateData: pathStrings: const [], paths: const [], boundingBox: const BoundingBox(x:0, y:0, w:100, h:100), centroid: const Offset(50, 50).

    Do NOT modify any existing test group or setUp block. Append the fixture before void main() or after the mock class declarations.
  </action>
  <verify>
    <automated>cd C:\code\Claude\StateTheStates && flutter test test/features/game/game_session_notifier_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - game_session_notifier_test.dart compiles with no errors
    - flutter test test/features/game/game_session_notifier_test.dart exits 0 (all existing tests still pass)
    - File contains a top-level function named stateFixture that returns List<StateData>
    - stateFixture() includes entries for postal 'GA', 'NY', and 'AK'
    - No existing test is modified or removed
  </acceptance_criteria>
  <done>flutter test exits 0 for notifier test file; stateFixture() function present and compiles; existing tests unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → lib code | Test files import production types; no data crossing at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-00-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs zero new packages — no install tasks in this plan |
</threat_model>

<verification>
After both tasks complete:
- flutter test test/features/typing/speed_typing_screen_test.dart exits 0
- flutter test test/features/game/game_session_notifier_test.dart exits 0
- flutter test exits 0 (full suite green — Wave 0 adds no failures)
</verification>

<success_criteria>
- test/features/typing/speed_typing_screen_test.dart exists, compiles, and passes
- stateFixture() helper available in game_session_notifier_test.dart
- Full test suite exits 0 (no regressions from stub additions)
</success_criteria>

<output>
Create .planning/phases/06-speed-typing-mode/06-00-SUMMARY.md when done
</output>
