---
phase: 06-speed-typing-mode
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/game/game_mode.dart
  - lib/features/game/game_session_notifier.dart
  - lib/core/data/high_score_repository.dart
autonomous: true
requirements:
  - TYPING-04
  - TYPING-05
  - TYPING-07
  - TYPING-08
  - TYPING-09

must_haves:
  truths:
    - "GameMode.speedTyping enum value exists and the app compiles"
    - "GameModeDisplay extension provides displayName getter returning 'Speed Typing' for GameMode.speedTyping"
    - "GameSessionNotifier.startGame() accepts skipCountdown: bool = false; when true the notifier skips directly to GamePhase.playing"
    - "GameSessionNotifier.submitTyping(String input, List<StateData> states) returns true on a new hit (correct state), false on miss or duplicate"
    - "A miss via submitTyping() increments errorCount by 1 and recalculates score as (elapsedSecs ~/10) + (errorCount * 5) + hintPenalty"
    - "submitTyping() triggers completeGame() when matchedPostals.length reaches 50 after a hit"
    - "high_score_repository.dart _key() switch includes GameMode.speedTyping => 'high_score_speed_typing'"
  artifacts:
    - path: "lib/features/game/game_mode.dart"
      provides: "enum GameMode with speedTyping value; GameModeDisplay extension"
      contains: "speedTyping"
    - path: "lib/features/game/game_session_notifier.dart"
      provides: "submitTyping() action + skipCountdown support in startGame()"
      contains: "submitTyping"
    - path: "lib/core/data/high_score_repository.dart"
      provides: "speedTyping key in _key() switch"
      contains: "high_score_speed_typing"
  key_links:
    - from: "GameSessionNotifier.submitTyping()"
      to: "GameMode.speedTyping"
      via: "enum value must exist before submitTyping() can be called with speedTyping mode"
      pattern: "GameMode\\.speedTyping"
    - from: "lib/core/data/high_score_repository.dart"
      to: "lib/features/game/game_mode.dart"
      via: "_key() switch must be exhaustive over all GameMode values"
      pattern: "high_score_speed_typing"
---

<objective>
Add the speedTyping enum value and all pure-Dart game logic required by Mode 5. This plan is the foundation that all subsequent plans depend on — adding GameMode.speedTyping causes Dart exhaustiveness compile errors in high_score_repository.dart, so both files must be updated atomically in this wave.

Purpose: Establish the business logic layer for Speed Typing Mode — enum value, displayName extension, skipCountdown, submitTyping() action, and high score key — all in pure Dart with no widget dependencies.

Output: Three modified source files; the app and existing tests must compile and pass after this plan.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\code\Claude\StateTheStates\.planning\PROJECT.md
@C:\code\Claude\StateTheStates\.planning\ROADMAP.md
@C:\code\Claude\StateTheStates\.planning\phases\06-speed-typing-mode\06-CONTEXT.md
@C:\code\Claude\StateTheStates\.planning\phases\06-speed-typing-mode\06-RESEARCH.md

<interfaces>
<!-- Key types and contracts. Extracted from codebase reads. -->

From lib/features/game/game_mode.dart (current):
  enum GameMode { learn, statesMaster, geographicalMaster, grandMaster }
  // No extension exists yet.

From lib/features/game/game_session_notifier.dart (key signatures):
  void startGame(GameMode mode)   // MUST add: {bool skipCountdown = false}
  void recordDrop(String postal, {required bool isCorrect})  // pattern to follow for miss path
  Future<void> completeGame() async   // called fire-and-forget when 50th state found
  // Internal fields available in submitTyping():
  //   final Stopwatch _stopwatch;
  //   int _restoredOffset;
  //   int _hintPenalty;
  //   AsyncNotifier state field (AsyncData<GameSession>)

From lib/features/game/game_session.dart (relevant fields):
  class GameSession {
    final GamePhase phase;
    final GameMode mode;
    final int score;
    final Duration elapsed;
    final int errorCount;
    final String? activePostal;
    final int hintsRemaining;
    final List<String> matchedPostals;  // source of truth for found states
    GameSession copyWith({...}) ...
  }

From lib/features/game/game_phase.dart:
  enum GamePhase { idle, countdown, playing, paused, completed }

From lib/core/models/state_data.dart:
  class StateData {
    final String postal;     // "GA", "CA", etc. — always uppercase
    final String name;       // "Georgia", "New York", etc. — mixed case
    final bool isPlaceable;  // false only for DC
  }

From lib/core/data/high_score_repository.dart (current _key switch):
  static String _key(GameMode mode) => switch (mode) {
    GameMode.learn               => 'high_score_learn',
    GameMode.statesMaster        => 'high_score_states_master',
    GameMode.geographicalMaster  => 'high_score_geographical_master',
    GameMode.grandMaster         => 'high_score_grand_master',
    // MISSING: GameMode.speedTyping — WILL produce compile error after enum updated
  };
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GameMode.speedTyping + GameModeDisplay extension</name>
  <files>lib/features/game/game_mode.dart, lib/core/data/high_score_repository.dart</files>
  <read_first>
    - lib/features/game/game_mode.dart — current file (3 lines); read before editing
    - lib/core/data/high_score_repository.dart — current _key() switch; must update atomically with enum change to prevent compile error
    - .planning/phases/06-speed-typing-mode/06-CONTEXT.md — D-08 (displayName extension spec), D-09 (teal color 0xFF00695C)
  </read_first>
  <behavior>
    - GameMode.speedTyping added: GameMode.values.contains(GameMode.speedTyping) == true
    - GameMode.speedTyping.displayName == 'Speed Typing'
    - GameMode.learn.displayName == 'Learn'
    - GameMode.statesMaster.displayName == 'States Master'
    - GameMode.geographicalMaster.displayName == 'Geographical Master'
    - GameMode.grandMaster.displayName == 'Grand Master'
    - SharedPreferencesHighScoreRepository._key(GameMode.speedTyping) == 'high_score_speed_typing'
    - flutter test exits 0 (all existing tests compile and pass)
  </behavior>
  <action>
    Edit lib/features/game/game_mode.dart:
    - Append speedTyping to the enum so the declaration becomes:
        enum GameMode { learn, statesMaster, geographicalMaster, grandMaster, speedTyping }
    - Immediately after the enum (same file), add the GameModeDisplay extension per D-08:
        extension GameModeDisplay on GameMode {
          String get displayName => switch (this) {
            GameMode.learn              => 'Learn',
            GameMode.statesMaster       => 'States Master',
            GameMode.geographicalMaster => 'Geographical Master',
            GameMode.grandMaster        => 'Grand Master',
            GameMode.speedTyping        => 'Speed Typing',
          };
        }

    Edit lib/core/data/high_score_repository.dart:
    - In the _key() switch expression, add the arm:
        GameMode.speedTyping => 'high_score_speed_typing',
    - Add this arm after the GameMode.grandMaster arm, before the closing brace.
    - Do NOT change any other line in the file.

    CRITICAL: Both files must be edited before running flutter test, because adding speedTyping to the enum immediately causes a compile error in _key() until the arm is added.
  </action>
  <verify>
    <automated>cd C:\code\Claude\StateTheStates && flutter test test/features/game/game_session_notifier_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/game/game_mode.dart contains the string 'speedTyping' in the enum declaration
    - lib/features/game/game_mode.dart contains extension GameModeDisplay on GameMode with a displayName getter
    - The switch in displayName includes all 5 GameMode values (learn, statesMaster, geographicalMaster, grandMaster, speedTyping)
    - lib/core/data/high_score_repository.dart contains 'high_score_speed_typing' in the _key() switch
    - flutter test test/features/game/game_session_notifier_test.dart exits 0
  </acceptance_criteria>
  <done>Both files compile; existing notifier tests pass; GameMode.speedTyping.displayName == 'Speed Typing'</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add skipCountdown to startGame() and submitTyping() action</name>
  <files>lib/features/game/game_session_notifier.dart, test/features/game/game_session_notifier_test.dart</files>
  <read_first>
    - lib/features/game/game_session_notifier.dart — full file; understand _onTick() countdown flow, recordDrop() miss path (the scoring pattern to replicate), and completeGame() call site in MapScreen comments
    - test/features/game/game_session_notifier_test.dart — full file; understand existing test patterns, FakeTicker usage, ProviderContainer setup, and where to insert new tests
    - lib/core/models/state_data.dart — StateData.isPlaceable and field names used in the for-loop lookup
    - .planning/phases/06-speed-typing-mode/06-RESEARCH.md — Pattern 1 (submitTyping pseudocode), Pitfall 1 (skipCountdown rationale), "Resolving StateData Without package:collection" (explicit for-loop pattern)
    - .planning/phases/06-speed-typing-mode/06-CONTEXT.md — D-01 (exact match full name + postal), D-02 (space required), D-03 (duplicate = miss)
  </read_first>
  <behavior>
    Unit tests to write in game_session_notifier_test.dart BEFORE implementing:

    Group 'submitTyping':
    1. startGame with skipCountdown:true → session.phase == GamePhase.playing immediately (no countdown ticks needed)
    2. submitTyping('GEORGIA', stateFixture()) when phase==playing → returns true, matchedPostals contains 'GA', errorCount unchanged
    3. submitTyping('GA', stateFixture()) when phase==playing → returns true (postal match), matchedPostals contains 'GA'
    4. submitTyping('NOTASTATE', stateFixture()) → returns false, errorCount incremented by 1, score == 5
    5. submitTyping('MASS', stateFixture()) → returns false (no alias match per D-01)
    6. submitTyping('NEWYORK', stateFixture()) → returns false (space required per D-02; 'New York' != 'NEWYORK')
    7. submitTyping('GEORGIA', stateFixture()) twice (duplicate) → second call returns false, errorCount incremented
    8. submitTyping when phase==idle → returns false, state unchanged
    9. 50-state game-end: submitting the 50th state (use a 5-state fixture loop to reach length==5, simulating end condition) → phase transitions to GamePhase.completed
  </behavior>
  <action>
    Edit lib/features/game/game_session_notifier.dart:

    1. Add skipCountdown parameter to startGame():
       Change the signature from: void startGame(GameMode mode)
       To: void startGame(GameMode mode, {bool skipCountdown = false})

       In the startGame() body, after the state = AsyncData(...) assignment that sets phase: GamePhase.countdown, add the following BEFORE _ticker.start(_onTick):
         if (skipCountdown) {
           _stopwatch.start();
           state = AsyncData(state.value!.copyWith(
             phase: GamePhase.playing,
             countdownSecondsRemaining: 0,
           ));
           _ticker.start(_onTick);
           return;
         }
       Then the existing _ticker.start(_onTick) call runs only in the non-skip branch.

    2. Add the submitTyping() method to GameSessionNotifier. Place it immediately after recordDrop() in the file. The method signature is:
         bool submitTyping(String input, List<StateData> states)

       Logic (implement using explicit for-loop, NOT firstWhereOrNull — do NOT add package:collection):
       - If state.value == null or state.value!.phase != GamePhase.playing, return false.
       - final normalized = input.trim(); if normalized.isEmpty, return false.
       - Search states with a for-loop: for each s in states, if s.isPlaceable and (s.name.toUpperCase() == normalized or s.postal == normalized), set match = s and break.
       - If match == null (no match): increment errorCount, recalculate score as (_restoredOffset + _stopwatch.elapsed.inSeconds) ~/ 10 + (newErrorCount * 5) + _hintPenalty, update state, return false.
       - If match != null and current.matchedPostals.contains(match.postal) (duplicate): same miss path as above, return false.
       - If match != null and not a duplicate: update matchedPostals with [...current.matchedPostals, match.postal], update state, call _gameStateRepository?.saveSession(updated, hintPenalty: _hintPenalty). Then if updated.matchedPostals.length == 50, call completeGame() fire-and-forget (do not await). Return true.

       WALLED-GARDEN RULE: submitTyping() must have ZERO imports from the ads module. Do not add any import to game_session_notifier.dart — no new imports are needed.

    Write unit tests in test/features/game/game_session_notifier_test.dart as a new group 'submitTyping' using the stateFixture() helper added in Plan 00. Follow the RED-GREEN-REFACTOR pattern: write all tests in the group first, run flutter test to confirm they fail with "Method not found" or compile error, then implement.

    For the 9 test cases listed in behavior, use the same ProviderContainer + FakeTicker setup pattern as existing tests. For the 50-state game-end test: use a 5-element fixture, submit all 5 and assert phase == completed (simulates the length==50 check with length==5 by submitting the complete fixture).
  </action>
  <verify>
    <automated>cd C:\code\Claude\StateTheStates && flutter test test/features/game/game_session_notifier_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/game/game_session_notifier.dart contains bool submitTyping(String input, List<StateData> states)
    - startGame() signature includes {bool skipCountdown = false}
    - submitTyping() does NOT contain any import from the ads module (grep for 'ad_service' or 'google_mobile_ads' returns 0 matches in game_session_notifier.dart)
    - submitTyping() uses a for-loop (not firstWhereOrNull) for state lookup
    - flutter test test/features/game/game_session_notifier_test.dart exits 0 with all 9 new test cases passing
    - submitTyping('GEORGIA', stateFixture()) returns true and matchedPostals contains 'GA'
    - submitTyping('MASS', stateFixture()) returns false (no partial match)
    - submitTyping('NEWYORK', stateFixture()) returns false (space required)
    - Duplicate submission returns false and increments errorCount
  </acceptance_criteria>
  <done>All submitTyping() unit tests pass; startGame() accepts skipCountdown; walled-garden rule verified; flutter test exits 0</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Widget layer → GameSessionNotifier | submitTyping() accepts raw user input from TextField; normalized via trim() inside the method |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | submitTyping() input | mitigate | input.trim() applied before comparison; empty string guard returns false immediately; no SQL/HTML injection surface (pure string equality) |
| T-06-01-02 | Tampering | GameSessionNotifier ads isolation | mitigate | Zero ad imports in game_session_notifier.dart — walled-garden rule; verified by acceptance criteria grep check |
| T-06-01-SC | Tampering | npm/pip/cargo installs | accept | Phase 6 installs zero new packages |
</threat_model>

<verification>
After both tasks complete:
- flutter test test/features/game/game_session_notifier_test.dart exits 0
- flutter test exits 0 (full suite — no regressions to Phases 1-5)
- lib/features/game/game_mode.dart contains 'speedTyping' in enum and extension
- lib/core/data/high_score_repository.dart contains 'high_score_speed_typing'
- lib/features/game/game_session_notifier.dart contains bool submitTyping
- No ad imports in game_session_notifier.dart
</verification>

<success_criteria>
- GameMode.speedTyping exists; GameMode.speedTyping.displayName == 'Speed Typing'
- submitTyping() correctly matches full name (GEORGIA), postal (GA), rejects aliases (MASS), enforces space (NEWYORK=miss), treats duplicates as miss, triggers completeGame() at 50
- startGame(mode, skipCountdown: true) transitions directly to GamePhase.playing
- high_score_repository.dart key 'high_score_speed_typing' present
- All 9 new unit tests pass; full test suite exits 0
</success_criteria>

<output>
Create .planning/phases/06-speed-typing-mode/06-01-SUMMARY.md when done
</output>
