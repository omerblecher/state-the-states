---
phase: 03-map-render-coordinate-transform-spike
plan: 03
type: tdd
wave: 2
depends_on:
  - 03-01
files_modified:
  - test/features/map/hit_detection_test.dart
autonomous: true
requirements:
  - MAP-01

must_haves:
  truths:
    - "stateHitTest(centroid, states, scale: 1.0) returns the correct postal for all 5 NE micro-states (RI, DE, CT, NJ, MD)"
    - "stateHitTest(centroid, states, scale: 4.0) returns the correct postal for all 5 NE micro-states"
    - "stateHitTest returns null for a point in the ocean far from any state"
    - "The 10-assertion Criterion 2 hard gate is automated and runs in under 5 seconds"
  artifacts:
    - path: "test/features/map/hit_detection_test.dart"
      provides: "10 centroid assertions (5 states × 2 scales) + ocean-null case + expansion edge cases"
  key_links:
    - from: "test/features/map/hit_detection_test.dart"
      to: "lib/features/map/hit_detection.dart"
      via: "imports stateHitTest directly (pure Dart, no widget harness)"
      pattern: "stateHitTest"
    - from: "test/features/map/hit_detection_test.dart"
      to: "assets/map/usa_states_paths.json"
      via: "File('assets/map/usa_states_paths.json').readAsStringSync() — no rootBundle, no asset mocking"
      pattern: "_loadRealStates"
---

<objective>
Create the `hit_detection_test.dart` test file that exercises `stateHitTest()` against real `StateData` objects loaded from the bundled JSON.

This plan is the **Criterion 2 hard gate**: Phase 4 must not begin until all 10 assertions pass. The tests are pure-Dart unit tests with no widget harness — they run in under 5 seconds.

This plan runs in parallel with Plan 02 (different files, no conflicts).

Purpose: Proves the 3-pass hit-detection algorithm returns the correct postal for all five NE micro-states at both fit-to-width scale (1.0) and 4× zoom scale (4.0). At scale 1.0, all five micro-states trigger the `_kMinScreenArea` circular expansion path. At scale 4.0, RI and DE still trigger expansion; CT, NJ, and MD match via path/bbox.

Output: `test/features/map/hit_detection_test.dart` (Criterion 2 test file)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-CONTEXT.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-RESEARCH.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-01-SUMMARY.md

<interfaces>
<!-- From lib/features/map/hit_detection.dart (Plan 03-01 output) -->
String? stateHitTest(
  Offset scenePoint,
  List<StateData> states, {
  double scale = 1.0,
})

<!-- From lib/core/models/state_data.dart -->
class StateData {
  final String postal;
  final Offset centroid;
  final BoundingBox boundingBox;  // .rect → Rect
  final List<Path> paths;
  final bool isPlaceable;
  final InsetGroup? insetGroup;
}

<!-- JSON loading pattern (from test/core/models/state_data_test.dart) -->
// Reads assets/map/usa_states_paths.json via dart:io (no rootBundle, no mocking)
final raw = File('assets/map/usa_states_paths.json').readAsStringSync();
final data = jsonDecode(raw) as Map<String, dynamic>;
return (data['states'] as List).cast<Map<String, dynamic>>().map(StateData.fromJson).toList();
</interfaces>
</context>

<feature>
  <name>stateHitTest NE micro-state centroid assertions (Criterion 2 hard gate)</name>
  <files>test/features/map/hit_detection_test.dart, lib/features/map/hit_detection.dart</files>
  <behavior>
    10 mandatory assertions (Criterion 2):
    - stateHitTest(ri.centroid, states, scale: 1.0) == 'RI'
    - stateHitTest(ri.centroid, states, scale: 4.0) == 'RI'
    - stateHitTest(de.centroid, states, scale: 1.0) == 'DE'
    - stateHitTest(de.centroid, states, scale: 4.0) == 'DE'
    - stateHitTest(ct.centroid, states, scale: 1.0) == 'CT'
    - stateHitTest(ct.centroid, states, scale: 4.0) == 'CT'
    - stateHitTest(nj.centroid, states, scale: 1.0) == 'NJ'
    - stateHitTest(nj.centroid, states, scale: 4.0) == 'NJ'
    - stateHitTest(md.centroid, states, scale: 1.0) == 'MD'
    - stateHitTest(md.centroid, states, scale: 4.0) == 'MD'

    Additional cases:
    - Ocean point (5.0, 5.0) returns null at scale 1.0 (far from any state in scene space)
    - TX centroid at scale 1.0 returns 'TX' (large state sanity check)
    - CA centroid at scale 1.0 returns 'CA' (large state sanity check)
  </behavior>
  <implementation>
    RED phase: Write failing test stubs referencing `stateHitTest` before or alongside the implementation.
    GREEN phase: `hit_detection.dart` (Plan 03-01 Task 2) makes tests pass.
    No REFACTOR needed — implementation is a direct port.

    Test structure:
    - `setUpAll`: load real states from JSON once using `dart:io` File read (no rootBundle)
    - Group 'stateHitTest — NE micro-states at scale 1.0': parametric loop over ['RI','DE','CT','NJ','MD']
    - Group 'stateHitTest — NE micro-states at scale 4.0': same loop with scale: 4.0
    - Group 'stateHitTest — edge cases': ocean null, TX centroid, CA centroid
  </implementation>
</feature>

<tasks>

<task type="tdd">
  <name>Task 1: Create hit_detection_test.dart with all 10 centroid assertions + edge cases</name>
  <files>test/features/map/hit_detection_test.dart</files>
  <read_first>
    - test/core/models/state_data_test.dart — _record helper pattern, JSON loading via dart:io (lines 1–24, 80–90)
    - lib/features/map/hit_detection.dart — stateHitTest() signature (Plan 03-01 Task 2 output)
    - .planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md — hit_detection_test.dart pattern, lines ~642–702
    - .planning/phases/03-map-render-coordinate-transform-spike/03-RESEARCH.md — Validation Dimension 2 (10 assertions, ocean null, expansion edge cases at scale 4.0)
    - assets/map/usa_states_paths.json — verify RI, DE, CT, NJ, MD records exist with centroid coordinates
  </read_first>
  <action>
    Create `test/features/map/hit_detection_test.dart`.

    Imports:
    - `import 'dart:convert';`
    - `import 'dart:io';`
    - `import 'dart:ui' show Offset;`
    - `import 'package:flutter_test/flutter_test.dart';`
    - `import 'package:state_states/core/models/state_data.dart';`
    - `import 'package:state_states/features/map/hit_detection.dart';`

    Define `_loadRealStates()` function that reads `assets/map/usa_states_paths.json` via `File(...).readAsStringSync()`, decodes it, reads `data['states']`, maps to `StateData.fromJson`, returns `List<StateData>`.

    In `main()`:
    - Declare `late List<StateData> states;`
    - Call `TestWidgetsFlutterBinding.ensureInitialized()` (required for `parseSvgPathData()` in `StateData.fromJson`)
    - `setUpAll(() { states = _loadRealStates(); })`

    Group 'stateHitTest — NE micro-states at scale 1.0':
    Use a `for` loop over `['RI', 'DE', 'CT', 'NJ', 'MD']`:
    ```
    test('centroid of $postal at scale 1.0 returns $postal', () {
      final s = states.firstWhere((s) => s.postal == postal);
      expect(stateHitTest(s.centroid, states, scale: 1.0), equals(postal));
    });
    ```

    Group 'stateHitTest — NE micro-states at scale 4.0':
    Same loop but `scale: 4.0`.

    Group 'stateHitTest — edge cases':
    - `test('ocean point (5.0, 5.0) returns null')`: `expect(stateHitTest(const Offset(5.0, 5.0), states), isNull)`
    - `test('TX centroid at scale 1.0 returns TX')`: look up TX centroid, assert result == 'TX'
    - `test('CA centroid at scale 1.0 returns CA')`: look up CA centroid, assert result == 'CA'

    Note: `TestWidgetsFlutterBinding.ensureInitialized()` is needed even though there are no widget tests here, because `StateData.fromJson` calls `parseSvgPathData()` which requires the Flutter engine binding for `dart:ui Path` creation.
  </action>
  <verify>
    <automated>flutter test test/features/map/hit_detection_test.dart</automated>
  </verify>
  <done>
    - `test/features/map/hit_detection_test.dart` exists with 13 tests (5 + 5 + 3)
    - All 13 tests pass: `flutter test test/features/map/hit_detection_test.dart` exits 0
    - The 10 mandatory Criterion 2 assertions are individually named in test output (parametric loop expands them)
    - Ocean null case passes: `stateHitTest(Offset(5,5), states)` returns null
    - Full suite: `flutter test` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test assets → test process | JSON loaded from filesystem in test context; not user-supplied |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | hit_detection_test / real JSON data | accept | Tests run in the developer's local environment only; JSON asset is public domain geographic data with no PII |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs no new packages |
</threat_model>

<verification>
- `flutter test test/features/map/hit_detection_test.dart` exits 0
- Output shows 13 tests: 5 scale-1.0 micro-state tests, 5 scale-4.0 micro-state tests, 3 edge-case tests
- `flutter test` (full suite) exits 0
</verification>

<success_criteria>
**CRITERION 2 HARD GATE:** All 10 centroid assertions pass — stateHitTest returns the correct postal abbreviation for RI, DE, CT, NJ, MD at both scale 1.0 and scale 4.0.

Ocean null case confirmed: a point at (5.0, 5.0) returns null.

Full flutter test suite exits 0.
</success_criteria>

<output>
Create `.planning/phases/03-map-render-coordinate-transform-spike/03-03-SUMMARY.md` when done
</output>
