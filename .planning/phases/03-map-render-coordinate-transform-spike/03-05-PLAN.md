---
phase: 03-map-render-coordinate-transform-spike
plan: 05
type: execute
wave: 4
depends_on:
  - 03-04
files_modified:
  - lib/features/map/spike_map_screen.dart
  - lib/app.dart
  - test/features/map/spike_map_screen_test.dart
autonomous: true
requirements:
  - MAP-03
  - MAP-04

must_haves:
  truths:
    - "SpikeMapScreen renders 6 named DragTarget regions using real state bounding boxes (TX, CA, FL, NY, AK, HI)"
    - "A token dragged onto a named region is correctly identified at 1x, 2x, and 4x zoom"
    - "The /spike route is only registered when kDebugMode is true — it is absent from release builds"
    - "SpikeMapScreen uses _toSceneFromGlobal() (globalToLocal + toScene) for coordinate conversion"
    - "SpikeMapScreen _zoom() is the PRODUCTION version with setEntry(2,2,newScale) — NOT the spike version"
    - "AK and HI DragTarget regions use inset bounding boxes from the JSON (InsetGroup.alaska / InsetGroup.hawaii)"
  artifacts:
    - path: "lib/features/map/spike_map_screen.dart"
      provides: "Dev-only spike screen for Criterion 1 manual validation"
      contains: "class SpikeMapScreen extends ConsumerStatefulWidget"
    - path: "lib/app.dart"
      provides: "Updated router with kDebugMode-guarded /spike route"
      contains: "if (kDebugMode)"
    - path: "test/features/map/spike_map_screen_test.dart"
      provides: "Widget smoke test for SpikeMapScreen"
  key_links:
    - from: "lib/features/map/spike_map_screen.dart"
      to: "lib/features/map/hit_detection.dart"
      via: "stateHitTest() called in DragTarget.onAcceptWithDetails"
      pattern: "stateHitTest"
    - from: "lib/features/map/spike_map_screen.dart"
      to: "lib/core/data/state_data_service.dart"
      via: "ref.watch(stateDataProvider) for real state bboxes"
      pattern: "stateDataProvider"
    - from: "lib/app.dart"
      to: "lib/features/map/spike_map_screen.dart"
      via: "if (kDebugMode) GoRoute(path: '/spike', ...)"
      pattern: "SpikeMapScreen"
---

<objective>
Deliver the coordinate-transform spike screen (`SpikeMapScreen`) and wire it into the router behind a `kDebugMode` guard. This is the **Criterion 1 hard gate** — Phase 4 must not begin until a developer has manually verified zero misidentifications across TX, CA, FL, NY, AK, HI at 1×, 2×, and 4× zoom.

Also create `spike_map_screen_test.dart` (automated smoke + coordinate correctness assertions) and modify `app.dart` to add the `/spike` route.

Deliverables:
1. `SpikeMapScreen` — ConsumerStatefulWidget with 6 named DragTarget regions (real state bboxes), outer catch-all DragTarget calling `stateHitTest()`, zoom buttons (1.5× factor), `_toSceneFromGlobal()`, and `debugPrint` output for manual inspection.
2. `app.dart` — `/spike` route added inside `if (kDebugMode)` guard.
3. `spike_map_screen_test.dart` — smoke test rendering 6 fixture states + coordinate conversion assertion.

Purpose: Proves `TransformationController.toScene()` is accurate at all zoom levels using real map geometry. The spike is deleted or kept behind `--dart-define=SPIKE=true` after Phase 3 verification passes (D-06).

Output: `spike_map_screen.dart`, updated `app.dart`, `test/features/map/spike_map_screen_test.dart`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-CONTEXT.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-RESEARCH.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-04-SUMMARY.md

<interfaces>
<!-- From lib/core/data/state_data_service.dart (Plan 03-01 output) -->
class MapData {
  final List<StateData> states;
  final List<Rect> insetFrameRects;
}
final stateDataProvider = FutureProvider<MapData>(...);

<!-- From lib/features/map/hit_detection.dart (Plan 03-01 output) -->
String? stateHitTest(Offset scenePoint, List<StateData> states, {double scale = 1.0})

<!-- From lib/core/models/state_data.dart -->
class StateData {
  final String postal;
  final BoundingBox boundingBox;  // .rect → Rect
  final Offset centroid;
  final InsetGroup? insetGroup;
}
enum InsetGroup { alaska, hawaii }

<!-- From lib/app.dart (current) -->
final _router = GoRouter(
  initialLocation: '/',
  routes: [
    GoRoute(path: '/', builder: ...),
    GoRoute(path: '/play', builder: ...),
  ],
);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SpikeMapScreen — 6-region coordinate transform spike</name>
  <files>lib/features/map/spike_map_screen.dart</files>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\map\spike_map_screen.dart — direct port template; all lines (181 lines); focus on: _toSceneFromGlobal (lines 37-39), DragTarget regions (lines 75-174), zoom buttons
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\map\map_screen.dart — _zoom() PRODUCTION version (lines 405-428) — use THIS, not the spike version (which lacks setEntry(2,2))
    - .planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md — spike_map_screen.dart patterns, lines ~348–440
    - .planning/phases/03-map-render-coordinate-transform-spike/03-RESEARCH.md — Pattern 1 (_toSceneFromGlobal), Pattern 9 (region selection TX/CA/FL/NY/AK/HI), Pattern 10 (routing), Pitfall 1 (entry 2,2), Pitfall 6 (DragTarget offset)
    - lib/features/map/map_screen.dart — _zoom() implementation from Plan 04 output (use the same pattern)
    - lib/features/map/hit_detection.dart — stateHitTest() signature
  </read_first>
  <behavior>
    - Class: ConsumerStatefulWidget (needs ref to watch stateDataProvider)
    - Fields: TransformationController _controller, GlobalKey _ivKey, double minScale = 0.08, double maxScale = 10.0 (spike uses wider range for manual testing), String _lastHit = '—', Offset _lastScene = Offset.zero
    - initState: schedules addPostFrameCallback for _fitMapToScreen; no listener on controller (spike is simpler than production)
    - dispose: _controller.dispose()
    - _toSceneFromGlobal: NON-NULLABLE variant (bang operator acceptable in spike — a null context IS a test failure): `final box = _ivKey.currentContext!.findRenderObject()! as RenderBox; return _controller.toScene(box.globalToLocal(globalOffset));`
    - _zoom: PRODUCTION version with setEntry(2,2,newScale) — copy exactly from map_screen.dart (Pitfall 1)
    - _fitMapToScreen: same pattern as MapScreen (mapW=1000, mapH=628), called from postFrameCallback
    - 6 named regions derived from stateDataProvider data:
      - Filter: TX, CA, FL, NY (mainland bboxes); AK (insetGroup == InsetGroup.alaska → use boundingBox.rect); HI (insetGroup == InsetGroup.hawaii → use boundingBox.rect)
      - For AK/HI use the state's actual boundingBox.rect (already in inset canvas space from pipeline)
    - SizedBox child of InteractiveViewer: 1000×628 (NOT 2000×1000)
    - Stack inside SizedBox: ocean background Container, 6 Positioned named regions (colored semi-transparent boxes with Text label), outer full-size DragTarget
    - Outer DragTarget.onAcceptWithDetails: call _toSceneFromGlobal(details.offset), call stateHitTest(scenePoint, allStates, scale: _controller.value.getMaxScaleOnAxis()), setState(() { _lastHit = hit ?? 'miss'; _lastScene = scenePoint; }), debugPrint for manual verification
    - Display _lastHit and _lastScene in a status bar above the IV (outside the viewer, below zoom buttons) so the developer can read them during manual testing
    - Draggable source: provide a few draggable chips at the top (one per region label) for manual testing; drag type is String (the postal code)
    - Zoom buttons: same as MapScreen — FloatingActionButton.small in Positioned overlay
    - _fitMapToScreen sets minScale = fitScale, maxScale = fitScale * 10.0 (wider range for spike testing)
  </behavior>
  <action>
    Create `lib/features/map/spike_map_screen.dart` as a new file.

    Imports:
    - `import 'package:flutter/foundation.dart' show kDebugMode;`
    - `import 'package:flutter/material.dart';`
    - `import 'dart:math' as math;`
    - `import 'package:flutter_riverpod/flutter_riverpod.dart';`
    - `import '../../core/data/state_data_service.dart';`
    - `import '../../core/models/state_data.dart';`
    - `import 'hit_detection.dart';`

    Region colors for visual distinction (6 distinct colors):
    `const _regionColors = [Colors.red, Colors.blue, Colors.green, Colors.orange, Colors.purple, Colors.teal];`
    Region postals: `const _regionPostals = ['TX', 'CA', 'FL', 'NY', 'AK', 'HI'];`

    In `build()`, after `stateDataProvider.when(data: (mapData) => ...)`:
    - Extract the 6 region states: `final regions = _regionPostals.map((p) => mapData.states.firstWhere((s) => s.postal == p)).toList()`
    - Pass `mapData.states` to the outer DragTarget's stateHitTest call

    The `_toSceneFromGlobal()` helper uses the bang-operator non-nullable variant (acceptable in dev-only spike).

    The `_zoom()` method must be the PRODUCTION version — copy it from `map_screen.dart` verbatim including `m.setEntry(2, 2, newScale)`. Add a comment: `// CRITICAL: setEntry(2,2) keeps getMaxScaleOnAxis() accurate — see RESEARCH.md Pitfall 1`.

    Above the InteractiveViewer, show a status bar Row with:
    - `Text('Hit: $_lastHit')` and `Text('Scene: (${_lastScene.dx.toStringAsFixed(1)}, ${_lastScene.dy.toStringAsFixed(1)})')` and `Text('Zoom: ${_controller.value.getMaxScaleOnAxis().toStringAsFixed(2)}x')`

    Below the status bar, show a Row of 6 `Draggable<String>` chips (one per postal). Each chip's data is its postal string. The `DragTarget<String>` on the outer layer accepts `String` type.

    Include an assertion guard at the top of build: `assert(kDebugMode, 'SpikeMapScreen must not appear in release builds');`
  </action>
  <verify>
    <automated>flutter test test/features/map/spike_map_screen_test.dart</automated>
  </verify>
  <done>
    - `lib/features/map/spike_map_screen.dart` exists with `class SpikeMapScreen extends ConsumerStatefulWidget`
    - `grep "setEntry(2, 2" lib/features/map/spike_map_screen.dart` returns a match
    - `grep "kDebugMode" lib/features/map/spike_map_screen.dart` returns a match (assert guard)
    - `grep "_toSceneFromGlobal" lib/features/map/spike_map_screen.dart` returns a match
    - `grep "stateHitTest" lib/features/map/spike_map_screen.dart` returns a match
    - `dart analyze lib/features/map/spike_map_screen.dart` exits 0
    - `flutter test test/features/map/spike_map_screen_test.dart` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire /spike route in app.dart + create spike_map_screen_test.dart</name>
  <files>lib/app.dart, test/features/map/spike_map_screen_test.dart</files>
  <read_first>
    - lib/app.dart — current routes (only / and /play); add /spike
    - .planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md — app.dart pattern (kDebugMode route guard, lines ~556–582); spike_map_screen_test.dart pattern (lines ~782–816)
    - .planning/phases/03-map-render-coordinate-transform-spike/03-RESEARCH.md — Pattern 10 (kDebugMode vs dart-define routing decision; recommendation: kDebugMode)
    - test/features/game/game_session_notifier_test.dart — ProviderScope override pattern
    - lib/features/map/spike_map_screen.dart — Task 1 output (ConsumerStatefulWidget)
    - lib/core/data/state_data_service.dart — stateDataProvider (needed for override in test)
    - lib/core/models/state_data.dart — StateData + BoundingBox + InsetGroup (for fixture construction in test)
  </read_first>
  <action>
    **app.dart changes:**
    Add to existing imports:
    - `import 'package:flutter/foundation.dart' show kDebugMode;`
    - `import 'features/map/spike_map_screen.dart';`

    In `_router`'s `routes` list, after the `/play` route, add:
    ```
    if (kDebugMode)
      GoRoute(
        path: '/spike',
        builder: (context, state) => const SpikeMapScreen(),
      ),
    ```
    This follows the `kDebugMode` guard pattern from Flags (RESEARCH.md Pattern 10).

    **spike_map_screen_test.dart:**
    Create `test/features/map/spike_map_screen_test.dart`.

    Imports:
    - `import 'package:flutter/material.dart';`
    - `import 'package:flutter_riverpod/flutter_riverpod.dart';`
    - `import 'package:flutter_test/flutter_test.dart';`
    - `import 'package:state_states/core/data/state_data_service.dart';`
    - `import 'package:state_states/core/models/state_data.dart';`
    - `import 'package:state_states/features/map/spike_map_screen.dart';`

    In `main()`: `TestWidgetsFlutterBinding.ensureInitialized();`

    Define 6 minimal `StateData` fixtures for TX, CA, FL, NY, AK (insetGroup: InsetGroup.alaska), HI (insetGroup: InsetGroup.hawaii) using the `_record()`-style pattern from `state_data_test.dart`. Use real-ish bounding boxes from RESEARCH.md Pattern 9:
    - TX: BoundingBox(x:293.9, y:358.3, w:268.2, h:261.3), centroid Offset(428.0, 488.9)
    - CA: BoundingBox(x:0.0, y:155.7, w:154.2, h:262.4), centroid Offset(77.1, 287.0)
    - FL: BoundingBox(x:683.7, y:479.1, w:174.0, h:148.8), centroid Offset(770.7, 553.5)
    - NY: BoundingBox(x:796.8, y:111.3, w:145.7, h:109.7), centroid Offset(869.7, 166.2)
    - AK: BoundingBox(x:0.0, y:462.4, w:250.0, h:134.2), centroid Offset(125.0, 529.5), insetGroup: InsetGroup.alaska
    - HI: BoundingBox(x:255.0, y:533.9, w:130.0, h:61.2), centroid Offset(320.0, 564.5), insetGroup: InsetGroup.hawaii

    Each fixture path string: `'M0,0 L1,0 L1,1 Z'` (minimal valid path, sufficient for smoke test).
    Use `StateData.fromJson()` with a constructed map to build fixtures.

    Test 1 (`testWidgets('SpikeMapScreen renders without exception with fixture states')`):
    - Override `stateDataProvider` with fixture `MapData` (states: 6 fixtures + 45 others from real JSON OR just the 6 — the spike only uses _regionPostals, so 6 is sufficient for the smoke test)
    - For simplicity, override with: `stateDataProvider.overrideWith((ref) async => MapData(states: _fixtures, insetFrameRects: [Rect.fromLTWH(0, 462.38, 250, 134.24), Rect.fromLTWH(255, 533.88, 130, 61.24)]))`
    - Pump `ProviderScope(overrides: [...], child: MaterialApp(home: SpikeMapScreen()))`
    - `await tester.pumpAndSettle()`
    - Assert `find.byType(InteractiveViewer)` `findsOneWidget`
    - Assert `find.text('TX')` `findsWidgets` (at least one: the draggable chip label or region label)

    Test 2 (`testWidgets('SpikeMapScreen zoom buttons are present')`):
    - Same setup
    - Assert `find.byIcon(Icons.add)` `findsOneWidget`
    - Assert `find.byIcon(Icons.remove)` `findsOneWidget`

    Note: The coordinate transform accuracy at 1×/2×/4× (Criterion 1) is validated MANUALLY by a developer running the spike in debug mode, per D-08 (golden tests = unit tests of stateHitTest, not widget screenshot golden files). The `spike_map_screen_test.dart` covers smoke + structure, not manual coordinate verification.
  </action>
  <verify>
    <automated>flutter test test/features/map/spike_map_screen_test.dart && flutter test</automated>
  </verify>
  <done>
    - `lib/app.dart` contains `if (kDebugMode)` guard around the `/spike` route
    - `grep "SpikeMapScreen" lib/app.dart` returns a match
    - `grep "kDebugMode" lib/app.dart` returns a match
    - `test/features/map/spike_map_screen_test.dart` exists with 2 widget tests
    - `flutter test test/features/map/spike_map_screen_test.dart` exits 0
    - `flutter test` (full suite) exits 0
    - `dart analyze lib/app.dart` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DragTarget.onAcceptWithDetails.offset → stateHitTest | Global screen coordinate converted via globalToLocal + toScene before reaching hit-test logic |
| kDebugMode guard → route registration | SpikeMapScreen only reachable in debug builds; release builds have no /spike route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Information Disclosure | SpikeMapScreen in release build | mitigate | `if (kDebugMode)` in GoRouter routes list ensures route is absent from release builds; `assert(kDebugMode)` in build() provides a secondary guard |
| T-03-10 | Tampering | details.offset direct pass to toScene | mitigate | `_toSceneFromGlobal()` applies `box.globalToLocal()` first — Pitfall 6 guard; passing raw global offset to toScene() gives wrong scene coordinates |
| T-03-11 | Elevation of Privilege | SpikeMapScreen debug output (debugPrint) | accept | debugPrint only runs in debug mode; output is geographic coordinate data with no PII; stripped in release builds |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs no new packages |
</threat_model>

<verification>
After both tasks complete:
- `flutter test test/features/map/spike_map_screen_test.dart` exits 0
- `flutter test` (full suite — all phases) exits 0
- `grep "if (kDebugMode)" lib/app.dart` returns a match
- `grep "setEntry(2, 2" lib/features/map/spike_map_screen.dart` returns a match
- `grep "globalToLocal" lib/features/map/spike_map_screen.dart` returns a match (inside _toSceneFromGlobal)
- `dart analyze lib/app.dart lib/features/map/spike_map_screen.dart` exits 0

**CRITERION 1 HARD GATE (manual):**
Run `flutter run` in debug mode. Navigate to `/spike` (or add a button on HomeScreen pointing to context.go('/spike')).
Drag each of the 6 labeled chips (TX, CA, FL, NY, AK, HI) onto their corresponding colored region at:
- 1× zoom (fit-to-width): verify status bar shows correct postal
- 2× zoom (one zoom-in tap): verify status bar shows correct postal
- 4× zoom (two zoom-in taps from 1.5× starting point, or navigate directly): verify status bar shows correct postal
Zero misidentifications across 18 drops (6 regions × 3 zoom levels) = Criterion 1 passes.
</verification>

<success_criteria>
- `SpikeMapScreen` renders 6 named regions with real state bounding boxes
- `/spike` route is registered only when `kDebugMode` is true — confirmed by grep
- `_zoom()` has `setEntry(2,2)` — confirmed by grep
- `_toSceneFromGlobal()` applies `globalToLocal` before `toScene()` — confirmed by grep
- 2 automated smoke tests pass
- Full `flutter test` suite exits 0

**PHASE 3 COMPLETION GATES (both must pass before Phase 4):**
1. CRITERION 1 (manual): Spike screen shows zero misidentifications for TX, CA, FL, NY, AK, HI at 1×, 2×, 4× zoom
2. CRITERION 2 (automated): `flutter test test/features/map/hit_detection_test.dart` exits 0 — all 10 centroid assertions pass
</success_criteria>

<output>
Create `.planning/phases/03-map-render-coordinate-transform-spike/03-05-SUMMARY.md` when done
</output>
