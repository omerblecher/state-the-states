---
context: phase
phase: 03-map-render-coordinate-transform-spike
plan: 5
total_plans: 5
status: complete_pending_manual_gate
last_updated: 2026-05-31T18:30:00Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: sequential-on-main — Run ALL executor agents sequentially on the main tree, NO worktree isolation. A prior Phase 2 incident on this repo corrupted main checkout when parallel worktrees were used on Windows. Structural mitigation: set USE_WORKTREES=false or omit isolation="worktree" from every Agent() call.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree isolation on Windows | Parallel git-worktree executors corrupted main in Phase 2. Agent branches tangled; HEAD ended up on agent branch; post-wave tracking commit silently reverted Wave 1 code. | blocking | Run all executors with sequential_execution mode, no isolation="worktree". Verified after each plan with git cat-file -e HEAD:<path> and flutter test. |
| Executor type erasure | Phase 3 executor wrote _buildMapStack(List<dynamic>) instead of List<StateData>, silently deferring a type error to runtime. Caught in code review. | advisory | Always run /gsd:code-review immediately after execution. grep for List<dynamic> and .cast() calls in new code. |
| Dead state + redundant callbacks | Executor added _mapPaintReady bool that nothing read, plus a postFrameCallback registration inside _buildMapStack that fired on every rebuild. Caught in code review. | advisory | Code review after each execution wave is required, not optional. |

<current_state>
Phase 3 is FULLY EXECUTED. All 5 plans complete, 94/94 tests pass, code review done and fixes committed, everything pushed to GitHub (main branch, commit b30e4b0).

The ONLY remaining gate before Phase 4 begins is Criterion 1 manual validation on the /spike screen.

The app runs on the Android emulator (Pixel 7). After a ~3-second initial load (SVG path parsing on main thread), the US map appears with color-filled states and zoom buttons. The /spike route is registered at kDebugMode guard in app.dart.
</current_state>

<completed_work>
All Phase 3 plans executed this session:

- Plan 03-01 (Wave 1): MapData wrapper + stateDataProvider updated to FutureProvider<MapData> + stateHitTest() pure-Dart port from Flags hit_detection.dart
- Plan 03-02 (Wave 2): Full UsaMapPainter — 6-color palette, ocean fill, scale-adaptive borders, inset frame rects, setEquals shouldRepaint
- Plan 03-03 (Wave 2): hit_detection_test.dart — CRITERION 2 CLEARED (10 NE micro-state centroid assertions pass at scale 1.0 and 4.0)
- Plan 03-04 (Wave 3): Production MapScreen as ConsumerStatefulWidget with InteractiveViewer, AnimatedBuilder, zoom buttons, Phase 4 handoff constructor
- Plan 03-05 (Wave 4): SpikeMapScreen with 6 real-state DragTarget regions + /spike route wired in app.dart behind kDebugMode guard
- Code review: WR-01–04 and WR-07 fixed (dead state, type erasure, key-order, firstWhere guard)
- Android fix: MainActivity.kt moved from com.otis.brooke.state_states to com.otis.brooke.state.the.state package (ClassNotFoundException fix)
</completed_work>

<remaining_work>
ONE item before Phase 4 can begin:

**Criterion 1 (MANUAL REQUIRED):** Run flutter run in debug mode. Navigate to /spike. Drag each of the 6 labeled chips (TX, CA, FL, NY, AK, HI) onto their colored region at:
- 1× zoom (fit-to-width): status bar must show the correct postal
- 2× zoom (one zoom-in tap): status bar must show the correct postal
- 4× zoom (two more zoom-in taps): status bar must show the correct postal

Zero misidentifications across 18 drops (6 regions × 3 zoom levels) = Criterion 1 PASSES → Phase 4 ready.

Note: there is no UI button linking to /spike on the home screen. Either use DevTools Route Navigator, or temporarily add `TextButton(onPressed: () => context.go('/spike'), child: Text('Spike'))` to HomeScreen for testing.
</remaining_work>

<decisions_made>
- Sequential-on-main execution mode for this repo (no worktrees) — Windows + prior incident
- insetFrameRects accessed by explicit key (alaska/hawaii), not Map.values insertion order — WR-04 fix
- max() dropped from hit_detection.dart imports — only used in removed isDegenerate branch
- _mapPaintReady dead state removed from MapScreen — WR-01/02 fix
- _buildMapStack parameter typed List<StateData> (not List<dynamic>) — WR-03 fix
- SpikeMapScreen firstWhere guarded with orElse — WR-07 fix
</decisions_made>

<blockers>
None blocking code work. One manual gate pending:
- Criterion 1 validation (human action — cannot be automated)
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current phase/plan status
2. `.planning/ROADMAP.md` — Phase 4 scope and requirements (MAP-05 through MAP-08)
3. `.planning/phases/03-map-render-coordinate-transform-spike/03-05-SUMMARY.md` — SpikeMapScreen details and Criterion 1 manual test steps
4. `C:\Users\omerb\.claude\projects\C--code-Claude-StateTheStates\memory\worktree-tangle-state-the-states.md` — sequential execution requirement (MUST READ before any execute-phase)

## Infrastructure State
- Android emulator: Pixel_7 (SDK 36, x86_64)
- App package: com.otis.brooke.state.the.state
- Build: debug APK at build\app\outputs\flutter-apk\app-debug.apk
- Flutter: 3.44.x, Dart 3.10
- Tests: 94/94 passing
- Git: main branch, b30e4b0, pushed to GitHub

## Open Review Findings (non-blocking)
From 03-REVIEW.md — not fixed this session, track for Phase 4:
- WR-05: bbox-expansion edge cases not tested in hit_detection_test.dart
- WR-06: entry(2,2) sync assertion deferred to SpikeMapScreen manual test (no automated assertion)
- IN-01: assert(kDebugMode) in SpikeMapScreen.build() is stripped in release — comment updated but not removed
- IN-02: kDebugMode import + assert closure in map_screen.dart is dead code until Phase 4

<context>
Phase 3 executed cleanly in this session. The coordinate-transform spike is fully wired. All automated gates pass. The only remaining work is the manual Criterion 1 validation which only a human can do (drag chips on the actual running app and verify zero misidentifications).

After Criterion 1 passes, Phase 4 (drag-drop game flow) can begin immediately. Phase 4 extends MapScreen directly — the Phase 4 handoff constructor (matchedPostals, showLabels, mode) is already wired and waiting.

The app has a 3-second startup delay due to 51 states × complex SVG path parsing on the main thread. This is a known performance issue (captured as WR-05 context) — the user experience is: spinner for 3s then map appears. This is acceptable for v1 and can be optimized in a future phase.
</context>

<next_action>
Start with: Run `flutter run` in debug mode. Add a temporary debug button to HomeScreen or use DevTools Route Navigator to navigate to `/spike`. Drag the 6 state chips at 1×, 2×, and 4× zoom and confirm zero misidentifications in the status bar. Once Criterion 1 passes, run `/gsd:execute-phase 4`.
</next_action>
