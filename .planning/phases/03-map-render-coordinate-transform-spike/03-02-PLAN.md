---
phase: 03-map-render-coordinate-transform-spike
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/features/map/usa_map_painter.dart
  - test/features/map/usa_map_painter_test.dart
autonomous: true
requirements:
  - MAP-01
  - MAP-02

must_haves:
  truths:
    - "All 50 state paths (plus DC) render as filled polygons from bundled JSON data"
    - "Alaska renders in the bottom-left inset frame and Hawaii in the bottom-center frame"
    - "Thin border rectangles appear around the AK and HI inset groups"
    - "Matched states render as grey (#AAAAAA); unmatched states use the 6-color cycling palette"
    - "shouldRepaint uses setEquals on matchedPostals — no reference-equality trap"
    - "Border stroke width is scale-adaptive: (1.0 / viewScale).clamp(0.15, 1.2)"
  artifacts:
    - path: "lib/features/map/usa_map_painter.dart"
      provides: "Full UsaMapPainter replacing the Phase 1 blank stub"
      contains: "class UsaMapPainter extends CustomPainter"
      exports: ["UsaMapPainter"]
    - path: "test/features/map/usa_map_painter_test.dart"
      provides: "Smoke test: painter renders without exception"
  key_links:
    - from: "lib/features/map/usa_map_painter.dart"
      to: "lib/core/models/state_data.dart"
      via: "states parameter; iterates state.paths, state.postal, state.centroid"
      pattern: "state\\.paths"
    - from: "lib/features/map/usa_map_painter.dart"
      to: "lib/core/data/state_data_service.dart"
      via: "insetFrameRects parameter sourced from MapData.insetFrameRects"
      pattern: "insetFrameRects"
---

<objective>
Replace the Phase 1 blank `UsaMapPainter` stub with a full CustomPainter that renders all 51 state records (50 placeable + DC) as filled polygons with scale-adaptive borders, and draws the AK/HI inset frame rectangles.

Also create the painter smoke test (`usa_map_painter_test.dart`) which verifies the painter renders without throwing an exception when given real `StateData` objects.

Purpose: Delivers MAP-01 (vector render from bundled JSON) and MAP-02 (AK/HI insets with frame rects). The painter is stateless — it receives all render parameters via its constructor, enabling Phase 4 to pass live game session state without touching this file.

Output: Updated `usa_map_painter.dart`, new `test/features/map/usa_map_painter_test.dart`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-CONTEXT.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-RESEARCH.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md
@.planning/phases/03-map-render-coordinate-transform-spike/03-01-SUMMARY.md

<interfaces>
<!-- From lib/core/data/state_data_service.dart (Plan 03-01 output) -->
class MapData {
  final List<StateData> states;
  final List<Rect> insetFrameRects;
  const MapData({required this.states, required this.insetFrameRects});
}
final stateDataProvider = FutureProvider<MapData>((ref) => ...);

<!-- From lib/core/models/state_data.dart -->
class StateData {
  final String postal;
  final String name;
  final List<Path> paths;
  final BoundingBox boundingBox;
  final Offset centroid;
  final bool isPlaceable;
  final InsetGroup? insetGroup;
}
enum InsetGroup { alaska, hawaii }
class BoundingBox {
  Rect get rect => Rect.fromLTWH(x, y, w, h);
}

<!-- From lib/features/game/game_mode.dart (Phase 2 output) -->
enum GameMode { learn, statesMaster, geographicalMaster, grandMaster }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement UsaMapPainter — full fill + border + inset frame pass</name>
  <files>lib/features/map/usa_map_painter.dart</files>
  <read_first>
    - lib/features/map/usa_map_painter.dart — current Phase 1 blank stub (will be completely replaced)
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\map\world_map_painter.dart — direct port template; fill pass, border pass, shouldRepaint, palette constants
    - .planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md — UsaMapPainter patterns, lines ~28–129
    - .planning/phases/03-map-render-coordinate-transform-spike/03-RESEARCH.md — Pattern 5 (paint passes), Pattern 7 (shouldRepaint), Pitfall 5 (setEquals)
    - lib/core/models/state_data.dart — StateData schema
    - lib/features/game/game_mode.dart — GameMode enum (needed for mode parameter)
  </read_first>
  <behavior>
    - File-level color constants (not class fields): _palette (6 colors per D-01), _matchedColor #AAAAAA (D-02), _oceanColor #A8D5E8 (D-05), _borderColor #555555 (D-05)
    - Constructor: const UsaMapPainter({required states, required matchedPostals, required insetFrameRects, showLabels = false, mode, viewScale = 1.0})
    - paint(): 3 steps: (1) fill ocean background rect, (2) iterate states — fill pass + border pass per-path (no isDegenerate branch), (3) draw inset frame rects
    - Fill color: matchedPostals.contains(state.postal) ? _matchedColor : _palette[i % _palette.length] — palette indexed by state list index i, not by postal
    - Border strokeWidth: (1.0 / viewScale).clamp(0.15, 1.2) per D-13
    - shouldRepaint: !setEquals(old.matchedPostals, matchedPostals) || old.showLabels != showLabels || old.mode != mode || (old.viewScale - viewScale).abs() > 0.001
    - showLabels parameter is declared but the label-draw pass is a TODO comment; draws nothing in Phase 3 per deferred section of CONTEXT.md
    - isComplex: true not set here (set at the CustomPaint call site in MapScreen)
    - No dual-canvas translate (_drawWorldCopy pattern from Flags) — US map is single canvas with insets baked by pipeline
  </behavior>
  <action>
    Completely replace the Phase 1 blank stub in `lib/features/map/usa_map_painter.dart`.

    Imports:
    - `import 'package:flutter/foundation.dart' show setEquals;`
    - `import 'package:flutter/material.dart';`
    - `import '../../core/models/state_data.dart';`
    - `import '../../features/game/game_mode.dart';`

    Declare 4 file-level color constants before the class:
    - `_palette`: list of 6 `Color` values in order: `Color(0xFF8DB87F)` (soft green), `Color(0xFFD4B483)` (tan), `Color(0xFFE8A055)` (orange), `Color(0xFFE89090)` (pink), `Color(0xFFA07EC8)` (purple), `Color(0xFFE8D870)` (yellow) — per D-01
    - `_matchedColor = Color(0xFFAAAAAA)` — per D-02, D-05
    - `_oceanColor = Color(0xFFA8D5E8)` — per D-05
    - `_borderColor = Color(0xFF555555)` — per D-05

    `paint()` method body:
    1. `canvas.drawRect(Rect.fromLTWH(0, 0, size.width, size.height), Paint()..color = _oceanColor)`
    2. Create `fillPaint` (PaintingStyle.fill) and `borderPaint` (PaintingStyle.stroke, color: _borderColor, strokeWidth: (1.0 / viewScale).clamp(0.15, 1.2))
    3. Loop `for (int i = 0; i < states.length; i++)`: set fillPaint.color, iterate `state.paths`, call `canvas.drawPath(path, fillPaint)` then `canvas.drawPath(path, borderPaint)` for each path
    4. Create `framePaint` (same stroke style as borderPaint). Loop `for (final frameRect in insetFrameRects)`: call `canvas.drawRect(frameRect, framePaint)`
    5. Add `// Phase 4: label pass (showLabels / mode) goes here` comment after frame rects

    `shouldRepaint` must use `setEquals` from `package:flutter/foundation.dart` for the Set comparison — NOT `==`.
  </action>
  <verify>
    <automated>flutter test test/features/map/usa_map_painter_test.dart</automated>
  </verify>
  <done>
    - `usa_map_painter.dart` contains `class UsaMapPainter extends CustomPainter`
    - Constructor has `required this.insetFrameRects` parameter of type `List<Rect>`
    - `shouldRepaint` body contains `setEquals`
    - `grep -v "^//" lib/features/map/usa_map_painter.dart | grep "isDegenerate"` returns no output
    - `flutter test test/features/map/usa_map_painter_test.dart` exits 0
    - `dart analyze lib/features/map/usa_map_painter.dart` exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Create usa_map_painter_test.dart — smoke test (renders without exception)</name>
  <files>test/features/map/usa_map_painter_test.dart</files>
  <read_first>
    - test/core/data/state_data_service_test.dart — ProviderContainer pattern, TestWidgetsFlutterBinding.ensureInitialized()
    - .planning/phases/03-map-render-coordinate-transform-spike/03-PATTERNS.md — usa_map_painter_test.dart pattern, lines ~706–744
    - lib/core/data/state_data_service.dart — MapData (Plan 03-01 output); stateDataProvider type
    - lib/features/map/usa_map_painter.dart — constructor signature (Task 1 output)
  </read_first>
  <action>
    Create `test/features/map/usa_map_painter_test.dart`.

    Imports:
    - `import 'package:flutter/material.dart';`
    - `import 'package:flutter_riverpod/flutter_riverpod.dart';`
    - `import 'package:flutter_test/flutter_test.dart';`
    - `import 'package:state_states/core/data/state_data_service.dart';`
    - `import 'package:state_states/features/map/usa_map_painter.dart';`

    In `main()`, call `TestWidgetsFlutterBinding.ensureInitialized()` at the top.

    Test 1 (`testWidgets('UsaMapPainter renders all 51 states without exception')`):
    - Create `ProviderContainer()`, add teardown to dispose it
    - Await `container.read(stateDataProvider.future)` to get `MapData`
    - Pump `MaterialApp(home: SizedBox(width: 1000, height: 628, child: CustomPaint(isComplex: true, painter: UsaMapPainter(states: mapData.states, matchedPostals: const {}, insetFrameRects: mapData.insetFrameRects, viewScale: 1.0), size: const Size(1000, 628))))`
    - Assert `find.byType(CustomPaint)` `findsOneWidget`

    Test 2 (`testWidgets('UsaMapPainter renders matched state in grey without exception')`):
    - Same setup but pass `matchedPostals: const {'TX'}` to verify the branch executes
    - Assert `find.byType(CustomPaint)` `findsOneWidget`

    Test 3 (`test('shouldRepaint returns false for identical parameters')`):
    - Create two `UsaMapPainter` instances with identical parameters
    - Assert `painter.shouldRepaint(same)` is `false`

    Test 4 (`test('shouldRepaint returns true when matchedPostals changes')`):
    - Create two painters; one with `matchedPostals: {}`, one with `matchedPostals: {'TX'}`
    - Assert `painter2.shouldRepaint(painter1)` is `true`
  </action>
  <verify>
    <automated>flutter test test/features/map/usa_map_painter_test.dart</automated>
  </verify>
  <done>
    - `test/features/map/usa_map_painter_test.dart` exists with 4 tests
    - `flutter test test/features/map/usa_map_painter_test.dart` exits 0
    - No exception thrown during painter rendering of 51 real states
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StateData list → Canvas | Pre-parsed `dart:ui Path` objects drawn to Canvas; no user input reaches the painter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Denial of Service | `UsaMapPainter.paint()` | accept | Painter iterates 51 state records with pre-built Path objects; no allocations inside the paint loop (paints are reused); performance is frame-bounded by InteractiveViewer |
| T-03-04 | Tampering | `shouldRepaint` / Set comparison | mitigate | Use `setEquals()` from `flutter/foundation.dart` — reference-equality trap on `Set<String>` would silently prevent grey rendering of matched states (Pitfall 5 in RESEARCH.md) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Phase 3 installs no new packages; zero new dependencies |
</threat_model>

<verification>
After both tasks complete:
- `flutter test test/features/map/usa_map_painter_test.dart` exits 0 (4 tests pass)
- `dart analyze lib/features/map/usa_map_painter.dart` exits 0
- `grep "setEquals" lib/features/map/usa_map_painter.dart` returns a match (not `==` for Set)
- `grep "isDegenerate" lib/features/map/usa_map_painter.dart` returns no output
- `grep "insetFrameRects" lib/features/map/usa_map_painter.dart` returns matches in constructor + paint()
</verification>

<success_criteria>
- Painter compiles with no analyzer errors
- 4 smoke tests pass: renders 51 states, renders with matched postal, shouldRepaint false on same params, shouldRepaint true on changed matchedPostals
- `setEquals` is used in `shouldRepaint` — confirmed by grep
- No `isDegenerate` branch exists — confirmed by grep
</success_criteria>

<output>
Create `.planning/phases/03-map-render-coordinate-transform-spike/03-02-SUMMARY.md` when done
</output>
