---
phase: 08-full-admob-layer
plan: "04"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - lib/features/game/state_tray.dart
  - lib/features/map/map_screen.dart
  - lib/features/home/home_screen.dart
  - lib/features/map/completion_screen.dart
autonomous: true
requirements: [AD-03, AD-04, HINT-03, HINT-04]

must_haves:
  truths:
    - "StateTray hint button is enabled whenever onHintPressed is non-null (including when hintsRemaining == 0)"
    - "MapScreen._onHintPressed forks: hintsRemaining > 0 uses existing hint animation; hintsRemaining == 0 shows rewarded dialog"
    - "Rewarded dialog: Watch Ad triggers showRewardedAd(); on earned calls refillHints() then useHint(); on failure shows Snackbar"
    - "HomeScreen calls loadBannerForWidth once from initState via addPostFrameCallback and renders getBannerWidget() below mode cards"
    - "CompletionScreen.initState fires showInterstitialAd() with a 1-second delay for all modes"
  artifacts:
    - path: "lib/features/game/state_tray.dart"
      provides: "Hint button enabled when onHintPressed != null (D-08)"
    - path: "lib/features/map/map_screen.dart"
      provides: "_onHintPressed fork + _showRewardedHintDialog"
    - path: "lib/features/home/home_screen.dart"
      provides: "Banner slot + loadBannerForWidth in initState"
    - path: "lib/features/map/completion_screen.dart"
      provides: "Interstitial trigger in initState"
  key_links:
    - from: "lib/features/map/map_screen.dart"
      to: "lib/core/ads/ad_service_provider.dart"
      via: "ref.read(adServiceProvider).showRewardedAd()"
      pattern: "adServiceProvider.*showRewardedAd"
    - from: "lib/features/home/home_screen.dart"
      to: "lib/core/ads/ad_service_provider.dart"
      via: "ref.watch(adServiceProvider).getBannerWidget()"
      pattern: "adServiceProvider.*getBannerWidget"
    - from: "lib/features/map/completion_screen.dart"
      to: "lib/core/ads/ad_service_provider.dart"
      via: "ref.read(adServiceProvider).showInterstitialAd()"
      pattern: "adServiceProvider.*showInterstitialAd"
---

<objective>
Wire all four UI ad call sites: StateTray hint guard, MapScreen rewarded hint dialog, HomeScreen banner, CompletionScreen interstitial.

Purpose: Wave 3b (parallel with 08-03 which touches only app.dart). Delivers the visible ad placements and the rewarded hint refill flow from the widget layer.
Output: Updated state_tray.dart, map_screen.dart, home_screen.dart, completion_screen.dart.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/08-full-admob-layer/08-CONTEXT.md
@.planning/phases/08-full-admob-layer/08-RESEARCH.md
@.planning/phases/08-full-admob-layer/08-PATTERNS.md
@.planning/phases/08-full-admob-layer/08-02-SUMMARY.md

<interfaces>
From lib/core/ads/ad_service.dart:
```dart
abstract interface class AdService {
  Widget getBannerWidget();
  Future<void> showInterstitialAd();
  Future<bool> showRewardedAd();
  Future<void> showAppOpenAd();
}
```

From lib/core/ads/real_ad_service.dart (after Wave 2):
```dart
class RealAdService implements AdService {
  Future<void> loadBannerForWidth(int screenWidthDp) async { ... }
  // (not on AdService interface — cast needed in HomeScreen)
}
```

From lib/features/game/game_session_notifier.dart (after Wave 1):
```dart
// New method added in Wave 1:
void refillHints() { ... }
// Existing method:
bool useHint() { ... }
```

From lib/features/game/state_tray.dart (current):
```dart
// Line 136 — hint button enabled condition to change:
final enabled = widget.onHintPressed != null && widget.hintsRemaining > 0;
// → change to:
final enabled = widget.onHintPressed != null;
```

From lib/features/map/map_screen.dart (current):
```dart
// _onHintPressed() at line 191 — fork on hintsRemaining == 0
void _onHintPressed() {
  final consumed = ref.read(gameSessionProvider.notifier).useHint();
  if (!consumed) return;
  // ... existing hint glow logic ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StateTray hint guard change and MapScreen rewarded dialog (D-08, HINT-03, HINT-04, HINT-05)</name>
  <files>lib/features/game/state_tray.dart, lib/features/map/map_screen.dart</files>
  <read_first>
    - lib/features/game/state_tray.dart (full file — _buildHintButton at line 135; change enabled condition only; label text unchanged)
    - lib/features/map/map_screen.dart (full file — _onHintPressed at line 191; _hintGlowTimer usage; adServiceProvider import needs adding)
    - lib/core/ads/ad_service_provider.dart (adServiceProvider — import needed in map_screen.dart)
    - lib/features/game/game_session_notifier.dart (gameSessionProvider.notifier.refillHints() and useHint())
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (state_tray.dart Pattern Assignment — exact one-line change; map_screen.dart _onHintPressed fork — full _showRewardedHintDialog method)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pattern 8 — rewarded hint dialog with showDialog, AlertDialog, Watch Ad/Cancel actions, onUserEarnedReward path; D-09 — Snackbar copy)
  </read_first>
  <behavior>
    state_tray.dart:
    - When hintsRemaining == 0 and onHintPressed != null: button IS enabled (shows "Hint ×0"), user can tap to get rewarded ad prompt
    - When onHintPressed == null: button is disabled (countdown phase)
    - Label text "Hint ×${widget.hintsRemaining}" unchanged — shows ×0 when depleted

    map_screen.dart:
    - When hintsRemaining > 0: _onHintPressed() calls useHint() then proceeds with existing glow animation (no change to existing path)
    - When hintsRemaining == 0: _onHintPressed() calls _showRewardedHintDialog() instead
    - _showRewardedHintDialog() shows AlertDialog titled "Watch an ad for 2 more hints?" with Cancel and Watch Ad actions
    - On Watch Ad: call adService.showRewardedAd(); if returned true: call refillHints() then useHint() (immediately consume one of the refilled hints); if false: show Snackbar "No ad available right now — try again later."
    - All async gaps guarded with `if (!mounted) return`
    - Snackbar message matches D-09 exactly: "No ad available right now — try again later."
  </behavior>
  <action>
    state_tray.dart change: In _buildHintButton (line 135-151), change the single line `final enabled = widget.onHintPressed != null && widget.hintsRemaining > 0;` to `final enabled = widget.onHintPressed != null;`. No other changes to state_tray.dart.

    map_screen.dart changes:
    1. Add import for `package:state_states/core/ads/ad_service_provider.dart`
    2. Modify _onHintPressed(): Before the existing `final consumed = ref.read(gameSessionProvider.notifier).useHint()` line, add the fork: read current session.hintsRemaining — if == 0, call _showRewardedHintDialog() and return immediately.
    3. Add new method `Future<void> _showRewardedHintDialog() async`: show AlertDialog with Cancel (returns false) and Watch Ad (returns true) actions using `showDialog<bool>`; if result != true, return; call `final earned = await ref.read(adServiceProvider).showRewardedAd()`; if not mounted, return; if earned is true, call refillHints() then useHint() on the notifier; if earned is false, show the Snackbar with the D-09 message.

    The existing hint glow logic after useHint() in _onHintPressed() is not modified — it already handles the success path.
  </action>
  <verify>
    <automated>flutter test test/features/map/map_screen_test.dart test/features/game/state_tray_test.dart --reporter compact</automated>
  </verify>
  <done>
    state_tray.dart _buildHintButton enabled condition is `widget.onHintPressed != null` (no hintsRemaining check). map_screen.dart contains `_showRewardedHintDialog()` method. map_screen.dart contains `refillHints()` call inside the earned branch. map_screen.dart contains the Snackbar with text "No ad available right now — try again later.". `flutter analyze lib/features/game/state_tray.dart lib/features/map/map_screen.dart` exits 0. Existing tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HomeScreen banner slot and CompletionScreen interstitial trigger (AD-03, AD-04)</name>
  <files>lib/features/home/home_screen.dart, lib/features/map/completion_screen.dart</files>
  <read_first>
    - lib/features/home/home_screen.dart (full file — _HomeScreenState has initState? Check current class definition; if initState is absent, it must be added; banner slot goes between Expanded(ListView) at line 99 and the privacy footer block at line 163)
    - lib/features/map/completion_screen.dart (full file — _CompletionScreenState.initState() at lines 57-86; add interstitial call at end of initState before closing brace; NOTE comment at line 85 is the insertion point)
    - lib/core/ads/ad_service_provider.dart (adServiceProvider — import path)
    - lib/core/ads/real_ad_service.dart (RealAdService — cast needed for loadBannerForWidth; RESEARCH.md Open Questions recommends cast approach matching Flags)
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (home_screen.dart Pattern Assignment — initState with addPostFrameCallback + banner slot; completion_screen.dart Pattern Assignment — Future.delayed interstitial call)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pattern 6 — getLargeAnchoredAdaptiveBannerAdSize + Pitfall 4: never call loadBannerForWidth from build(); Code Examples: HomeScreen Banner Slot and CompletionScreen Interstitial Trigger)
  </read_first>
  <behavior>
    home_screen.dart:
    - loadBannerForWidth called ONCE from initState via WidgetsBinding.instance.addPostFrameCallback — never from build()
    - Cast: (ref.read(adServiceProvider) as RealAdService).loadBannerForWidth(widthDp) — RealAdService is concrete class from Wave 2; this is the Flags approach per RESEARCH.md Open Questions
    - Banner widget slot: `ref.watch(adServiceProvider).getBannerWidget()` inserted in the Column between the Expanded(ListView) and the privacy footer SizedBox — uses ref.watch so the widget rebuilds when the banner loads
    - If _HomeScreenState does not have an initState, add one

    completion_screen.dart:
    - Add import for ad_service_provider.dart
    - In _CompletionScreenState.initState(), after all existing star/PB/confetti logic (after line 84), add: Future.delayed(const Duration(seconds: 1), () { if (mounted) ref.read(adServiceProvider).showInterstitialAd(); })
    - Remove the NOTE comment "NO ad call here (D-13 — v2 only)" — this is now v2 and the call is live
  </behavior>
  <action>
    home_screen.dart: Add imports for `package:state_states/core/ads/ad_service_provider.dart` and `package:state_states/core/ads/real_ad_service.dart`. In _HomeScreenState, add or extend initState() to call WidgetsBinding.instance.addPostFrameCallback with a callback that reads MediaQuery.of(context).size.width.toInt() and passes it to (ref.read(adServiceProvider) as RealAdService).loadBannerForWidth(). Insert `ref.watch(adServiceProvider).getBannerWidget()` in the Column between the Expanded(ListView(...)) child and the privacy footer Padding widget.

    completion_screen.dart: Add import for `package:state_states/core/ads/ad_service_provider.dart`. At the end of initState(), after the existing _pbController.forward() block and before the closing brace, add the Future.delayed interstitial call. Remove the "NO ad call here" comment.
  </action>
  <verify>
    <automated>flutter test test/features/map/completion_screen_test.dart --reporter compact</automated>
  </verify>
  <done>
    home_screen.dart contains `loadBannerForWidth` call in an initState/addPostFrameCallback. home_screen.dart contains `getBannerWidget()` in the _buildBody Column. completion_screen.dart contains `showInterstitialAd()` in initState with Future.delayed 1 second. `flutter analyze lib/features/home/home_screen.dart lib/features/map/completion_screen.dart` exits 0. completion_screen_test.dart still passes (StubAdService no-ops showInterstitialAd).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget layer → adServiceProvider | Ad calls originate only from widget layer (walled-garden rule preserved) |
| user → hint button (hintsRemaining==0) | Button now enabled; rewarded ad dialog is the new gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04-01 | Tampering | Hint granted without watching full ad | mitigate | _showRewardedHintDialog awaits showRewardedAd() which uses Completer<bool>; refillHints/useHint only called on `earned == true` |
| T-08-04-02 | Tampering | GameSessionNotifier accessed from ad callback path | mitigate | widget layer calls notifier via ref.read; notifier has zero ad imports (walled-garden) |
| T-08-04-03 | Denial of Experience | Banner loaded from build() causing multiple instances | mitigate | loadBannerForWidth called only from initState via addPostFrameCallback; guarded by AdLoaded check in RealAdService |
| T-08-04-04 | Tampering | Interstitial fires mid-game | mitigate | showInterstitialAd() called only from CompletionScreen.initState (post-game, not mid-round) |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Wave 3 |
</threat_model>

<verification>
- flutter test test/features/map/completion_screen_test.dart exits 0
- flutter test test/features/map/map_screen_test.dart exits 0
- flutter analyze lib/features/ exits 0
- home_screen.dart contains "loadBannerForWidth"
- home_screen.dart contains "getBannerWidget"
- completion_screen.dart contains "showInterstitialAd"
- map_screen.dart contains "_showRewardedHintDialog"
- map_screen.dart contains "refillHints"
- state_tray.dart _buildHintButton enabled line does NOT contain "hintsRemaining > 0"
</verification>

<success_criteria>
StateTray hint button enabled at hintsRemaining == 0. MapScreen has rewarded hint dialog with correct reward/failure paths. HomeScreen loads and renders banner. CompletionScreen fires interstitial after 1-second delay. All existing tests pass. walled-garden rule preserved (GameSessionNotifier has zero ad imports).
</success_criteria>

<output>
Create `.planning/phases/08-full-admob-layer/08-04-SUMMARY.md` when done
</output>
