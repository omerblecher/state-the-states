---
phase: 08-full-admob-layer
plan: "03"
type: execute
wave: 3
depends_on: ["08-02"]
files_modified:
  - lib/app.dart
autonomous: true
requirements: [AD-05]

must_haves:
  truths:
    - "app.dart App class is ConsumerStatefulWidget (not StatelessWidget)"
    - "initState starts AppStateEventNotifier listening and subscribes to appStateStream"
    - "dispose cancels the stream subscription"
    - "_onAppResumed reads gameSessionProvider phase and suppresses showAppOpenAd() when playing or paused"
    - "Cold launch path calls showAppOpenAd() in initState (after AppStateEventNotifier.startListening)"
  artifacts:
    - path: "lib/app.dart"
      provides: "App as ConsumerStatefulWidget with App Open lifecycle observer"
      contains: "class App extends ConsumerStatefulWidget"
  key_links:
    - from: "lib/app.dart"
      to: "lib/core/ads/app_state_observer.dart"
      via: "import app_state_observer.dart"
      pattern: "app_state_observer"
    - from: "lib/app.dart"
      to: "lib/features/game/game_session_notifier.dart"
      via: "ref.read(gameSessionProvider)"
      pattern: "gameSessionProvider"
    - from: "lib/app.dart"
      to: "lib/core/ads/ad_service_provider.dart"
      via: "ref.read(adServiceProvider)"
      pattern: "adServiceProvider"
---

<objective>
Convert app.dart's App class from StatelessWidget to ConsumerStatefulWidget and wire AppStateEventNotifier for App Open ad trigger on cold launch and resume.

Purpose: App Open requires initState/dispose lifecycle and ref access — neither is available on StatelessWidget. The AppStateEventNotifier pattern (GMA SDK) is the correct Flutter App Open trigger per RESEARCH.md Pattern 2.
Output: Updated lib/app.dart with ConsumerStatefulWidget App and _AppState with full lifecycle subscription.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/08-full-admob-layer/08-CONTEXT.md
@.planning/phases/08-full-admob-layer/08-RESEARCH.md
@.planning/phases/08-full-admob-layer/08-PATTERNS.md
@.planning/phases/08-full-admob-layer/08-02-SUMMARY.md

<interfaces>
From lib/core/ads/app_state_observer.dart (after Wave 2):
```dart
// Re-exports:
// AppStateEventNotifier — static methods: startListening(), appStateStream
// AppState — enum: foreground, background
```

From lib/core/ads/ad_service_provider.dart (after Wave 2):
```dart
final adServiceProvider = Provider<AdService>((ref) { ... });
```

From lib/features/game/game_session_notifier.dart:
```dart
final gameSessionProvider = AsyncNotifierProvider<GameSessionNotifier, GameSession>(...);
```

From lib/features/game/game_phase.dart:
```dart
enum GamePhase { idle, countdown, playing, paused, completed }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert app.dart App to ConsumerStatefulWidget with AppStateEventNotifier (AD-05)</name>
  <files>lib/app.dart</files>
  <read_first>
    - lib/app.dart (full file — current StatelessWidget App class; _router definition at top is preserved exactly; only the App class changes)
    - C:\code\Claude\FlagsRoundTheWorld\lib\app.dart (authoritative analog — lines 63-107 contain the complete ConsumerStatefulWidget + _AppState pattern to port)
    - lib/core/ads/app_state_observer.dart (just created in Wave 2 — the import to add)
    - lib/core/ads/ad_service_provider.dart (adServiceProvider — used in _onAppResumed)
    - lib/features/game/game_session_notifier.dart (gameSessionProvider — phase check)
    - lib/features/game/game_phase.dart (GamePhase.playing, GamePhase.paused)
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (app.dart Pattern Assignment — exact new imports and full class replacement)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pattern 2 — AppStateEventNotifier pattern; Pitfall 6 — App must become ConsumerStatefulWidget)
  </read_first>
  <action>
    Preserve the existing _router GoRouter definition (all routes, all imports for screens) exactly. Only modify the App class and add new imports.

    New imports to add (after the existing import block):
    - import 'dart:async'
    - import 'features/game/game_phase.dart'
    - import 'features/game/game_session_notifier.dart'
    - import 'core/ads/ad_service_provider.dart'
    - import 'core/ads/app_state_observer.dart'

    Replace `class App extends StatelessWidget` through the closing brace with:
    - `class App extends ConsumerStatefulWidget` with `const App({super.key})` constructor and `createState() => _AppState()`
    - `class _AppState extends ConsumerState<App>` with field `StreamSubscription<AppState>? _appStateSubscription`
    - `initState()`: call super.initState(), then AppStateEventNotifier.startListening(), then assign _appStateSubscription by listening to AppStateEventNotifier.appStateStream — on AppState.foreground, call _onAppResumed()
    - `_onAppResumed()`: read `final phase = ref.read(gameSessionProvider).value?.phase`; if phase == GamePhase.playing or phase == GamePhase.paused, return; otherwise call `ref.read(adServiceProvider).showAppOpenAd()`
    - `dispose()`: cancel _appStateSubscription, call super.dispose()
    - `build(BuildContext context)`: returns the exact same MaterialApp.router as before (title: 'State the States', debugShowCheckedModeBanner: false, routerConfig: _router)

    Do NOT add a cold-launch showAppOpenAd() call in initState — AppStateEventNotifier fires AppState.foreground on the first foreground transition including cold launch; a separate initState call would double-fire.
  </action>
  <verify>
    <automated>flutter analyze lib/app.dart</automated>
  </verify>
  <done>
    lib/app.dart contains `class App extends ConsumerStatefulWidget`. lib/app.dart contains `class _AppState extends ConsumerState`. lib/app.dart contains `AppStateEventNotifier.startListening()` in initState. lib/app.dart contains `_appStateSubscription?.cancel()` in dispose. lib/app.dart contains the phase check (playing or paused) before showAppOpenAd(). `flutter analyze lib/app.dart` exits 0. All existing routes remain intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AppStateEventNotifier → app.dart | Stream events from GMA SDK; processed in _onAppResumed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | Denial of Experience | App Open fires during active gameplay on resume | mitigate | _onAppResumed reads gameSessionProvider.value?.phase; early return on playing/paused (belt-and-suspenders alongside RealAdService.showAppOpenAd() suppression) |
| T-08-03-02 | Tampering | StreamSubscription not cancelled on dispose | mitigate | dispose() cancels _appStateSubscription before super.dispose() |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs; existing google_mobile_ads already resolved |
</threat_model>

<verification>
- flutter analyze lib/app.dart exits 0
- flutter test exits 0 (full suite — app.dart change must not break existing widget tests)
- lib/app.dart contains "ConsumerStatefulWidget" (not "StatelessWidget" for App class)
- lib/app.dart contains "AppStateEventNotifier.startListening()"
- lib/app.dart contains "_appStateSubscription?.cancel()"
</verification>

<success_criteria>
app.dart App class is ConsumerStatefulWidget. AppStateEventNotifier subscription is live in initState and cancelled in dispose. _onAppResumed suppresses App Open during gameplay. Full test suite remains GREEN.
</success_criteria>

<output>
Create `.planning/phases/08-full-admob-layer/08-03-SUMMARY.md` when done
</output>
