---
phase: 08-full-admob-layer
plan: "00"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/core/ads/ads_initializer_test.dart
  - test/core/ads/real_ad_service_test.dart
  - test/features/game/game_session_notifier_test.dart
autonomous: false
requirements: [AD-01, AD-02, HINT-04, HINT-05]

must_haves:
  truths:
    - "Production AdMob App ID and four unit IDs are known before any implementation runs"
    - "ads_initializer_test.dart exists with RED tests for AD-01 and AD-02"
    - "real_ad_service_test.dart exists with RED tests for AD-03, AD-04, AD-05, HINT-05"
    - "game_session_notifier_test.dart has a RED test for refillHints()"
  artifacts:
    - path: "test/core/ads/ads_initializer_test.dart"
      provides: "Stub test file covering AD-01 and AD-02 COPPA init order"
    - path: "test/core/ads/real_ad_service_test.dart"
      provides: "Stub test file covering AD-03 through AD-05 and HINT-05"
    - path: "test/features/game/game_session_notifier_test.dart"
      provides: "Extended with RED refillHints() test for HINT-04"
  key_links:
    - from: "test/core/ads/ads_initializer_test.dart"
      to: "lib/core/ads/ads_initializer.dart"
      via: "import"
      pattern: "import.*ads_initializer"
    - from: "test/core/ads/real_ad_service_test.dart"
      to: "lib/core/ads/real_ad_service.dart"
      via: "import"
      pattern: "import.*real_ad_service"
---

<objective>
Collect production AdMob IDs from developer (human checkpoint) and create RED test stubs for all Phase 8 behaviors before implementation begins.

Purpose: Production IDs must be in hand before ad_constants.dart and AndroidManifest.xml are updated; test stubs create the RED state that TDD demands before implementation.
Output: Five production values (App ID + four unit IDs), two new test files (test/core/ads/), one extended test file (game_session_notifier_test.dart).
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-full-admob-layer/08-CONTEXT.md
@.planning/phases/08-full-admob-layer/08-RESEARCH.md
@.planning/phases/08-full-admob-layer/08-PATTERNS.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Collect production AdMob IDs (D-01, D-02)</name>
  <what-built>
    Phase 8 requires five production values from the AdMob console. Claude cannot access the AdMob console — these must be provided by the developer. The values will be written to ad_constants.dart (Wave 1) and AndroidManifest.xml (Wave 1).
  </what-built>
  <how-to-verify>
    1. Log in to https://admob.google.com
    2. Navigate to Apps → "State the States" (package: com.otis.brooke.state.the.state)
       If the app is not yet registered, create it now (platform: Android).
    3. Copy the App ID — format: ca-app-pub-XXXXXXXXXX~XXXXXXXXXX
    4. Navigate to Ad units and collect four unit IDs (create if needed):
       - Banner ad unit — format: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
       - Interstitial ad unit — format: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
       - Rewarded ad unit — format: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
       - App Open ad unit — format: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
    5. Confirm all five IDs are in hand before proceeding.
  </how-to-verify>
  <resume-signal>
    Provide the five production values in this format:
    App ID: ca-app-pub-XXXXXXXXXX~XXXXXXXXXX
    Banner: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
    Interstitial: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
    Rewarded: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
    App Open: ca-app-pub-XXXXXXXXXX/XXXXXXXXXX
  </resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test/core/ads/ stubs (RED) for ads_initializer and real_ad_service</name>
  <files>test/core/ads/ads_initializer_test.dart, test/core/ads/real_ad_service_test.dart</files>
  <read_first>
    - test/core/audio/audio_service_test.dart (mock MethodChannel pattern for platform SDKs — lines 1-55)
    - test/features/game/game_session_notifier_test.dart (Mock class pattern, ProviderContainer usage — lines 1-20)
    - lib/core/ads/ads_initializer.dart (current initializeAds() — what the test imports)
    - lib/core/ads/ad_service.dart (AdService interface — four methods)
    - lib/core/ads/stub_ad_service.dart (reference no-op implementation)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Validation Architecture section — exact test structure for each requirement ID)
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (test/core/ads/ Pattern Assignments — imports and group structure)
  </read_first>
  <behavior>
    ads_initializer_test.dart:
    - Test group "initializeAds — COPPA init order (AD-01, AD-02)"
    - Test "AD-01: updateRequestConfiguration called with tagForChildDirectedTreatment yes" — SKIP or expect-fail (RED) because real SDK not yet mocked with correct stubs
    - Test "AD-02: ironSource setConsent(false) and setDoNotSell(true) called before initialize()" — RED stub
    - Test "AD-02: Unity setGDPRConsent(false) and setCCPAConsent(false) called before initialize()" — RED stub
    - Test "AD-02: no import of gma_mediation_inmobi anywhere in ads_initializer.dart" — this is a source assertion, can pass immediately

    real_ad_service_test.dart:
    - Test group "StubAdService — interface parity (AD-03, HINT-05 reference)"
    - Test "getBannerWidget returns SizedBox.shrink" — GREEN immediately (uses StubAdService, no SDK)
    - Test "showRewardedAd returns false" — GREEN immediately (uses StubAdService)
    - Test group "RealAdService unit tests"
    - Test "AD-03: getBannerWidget returns SizedBox.shrink when _bannerAd is null" — will be GREEN once real_ad_service.dart exists
    - Test "AD-04: showInterstitialAd is a no-op when _interstitialAd is null" — will be GREEN once real_ad_service.dart exists
    - Test "AD-05: showAppOpenAd suppressed when GamePhase.playing" — will be GREEN once real_ad_service.dart exists
    - Test "HINT-05: showRewardedAd returns false when _rewardedAd is null" — will be GREEN once real_ad_service.dart exists
    All RealAdService tests skip with `skip: 'real_ad_service.dart created in Wave 2'` until Wave 2 ships.
  </behavior>
  <action>
    Create test/core/ads/ directory (mkdir if absent). Create two test stub files.

    ads_initializer_test.dart: Import flutter/services.dart, flutter_test/flutter_test.dart, google_mobile_ads/google_mobile_ads.dart, gma_mediation_unity/gma_mediation_unity.dart, gma_mediation_ironsource/gma_mediation_ironsource.dart, state_states/core/ads/ads_initializer.dart. Call TestWidgetsFlutterBinding.ensureInitialized() in main(). Add a mock GMA MethodChannel handler (channel name 'plugins.flutter.io/google_mobile_ads') that accepts all calls and returns minimal responses — pattern from audio_service_test.dart lines 21-55. Write the three test cases listed in behavior block. The AD-02 "no InMobi import" test is a static assertion: verify that ads_initializer.dart does NOT contain the string 'gma_mediation_inmobi' — implement as `expect(File('lib/core/ads/ads_initializer.dart').readAsStringSync().contains('gma_mediation_inmobi'), isFalse)`.

    real_ad_service_test.dart: Import flutter_test/flutter_test.dart, flutter_riverpod/flutter_riverpod.dart, mocktail/mocktail.dart, state_states/core/ads/ad_service.dart, state_states/core/ads/stub_ad_service.dart. Do NOT import real_ad_service.dart yet (file does not exist in Wave 0). Write StubAdService interface parity tests (no SDK, run immediately). Write RealAdService group with all four tests marked skip per behavior block.

    Do NOT place fenced code blocks or full implementations in this action description.
  </action>
  <verify>
    <automated>flutter test test/core/ads/ads_initializer_test.dart test/core/ads/real_ad_service_test.dart --reporter compact</automated>
  </verify>
  <done>
    Both test files compile. StubAdService interface parity tests pass (GREEN). RealAdService group tests are all skipped. ads_initializer_test.dart runs without MissingPluginException (GMA mock channel registered). The no-InMobi-import source assertion passes (ads_initializer.dart currently contains no such import).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend game_session_notifier_test.dart with RED refillHints() test (HINT-04)</name>
  <files>test/features/game/game_session_notifier_test.dart</files>
  <read_first>
    - test/features/game/game_session_notifier_test.dart (full file — understand existing stateFixture(), mock injection, ProviderContainer pattern)
    - lib/features/game/game_session_notifier.dart (useHint() lines 293-314 — the method refillHints() mirrors)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pattern 7 — refillHints() contract: resets to 2, not additive, playing phase guard)
  </read_first>
  <behavior>
    - Test "HINT-04: refillHints() resets hintsRemaining to 2 when in playing phase"
      Setup: start game with startGame(GameMode.learn, skipCountdown: true); call useHint() twice to reach hintsRemaining=0; then call refillHints(); expect session.hintsRemaining == 2.
    - Test "HINT-04: refillHints() is a no-op when game is not in playing phase"
      Setup: provider in idle phase; call refillHints(); expect session.hintsRemaining unchanged (== 2).
    - Test "HINT-04: refillHints() does not modify the score or hintPenalty"
      Setup: playing phase, call refillHints(); expect score is unchanged (no +5 penalty — that belongs to useHint()).
    All three tests are RED until refillHints() is added in Wave 1.
  </behavior>
  <action>
    Append a new group 'refillHints() — HINT-04' after the existing useHint() group in game_session_notifier_test.dart. Use the same ProviderContainer + MockGameStateRepository + MockHighScoreRepository pattern already established in the file. The stateFixture() helper already defined in the file is not needed for these tests (no submitTyping calls). Create a ProviderContainer with injected mocks, await container.read(gameSessionProvider.future), call startGame to put the session in playing phase (skipCountdown: true), then exercise refillHints(). Read the entire existing file before making changes so that the new group is appended in the correct location and does not duplicate any existing imports.
  </action>
  <verify>
    <automated>flutter test test/features/game/game_session_notifier_test.dart --reporter compact</automated>
  </verify>
  <done>
    Existing tests still pass. Three new HINT-04 tests fail with "NoSuchMethodError: Class 'GameSessionNotifier' has no instance method 'refillHints'" — confirming the RED state. Test file compiles cleanly.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer→ad_constants.dart | Production IDs supplied by human; no validation possible by Claude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00-01 | Information Disclosure | AdMob production IDs in CLAUDE.md/history | accept | IDs appear in APK resources regardless; not secrets per D-01 |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Wave 0 (test stubs only, flutter pub already resolved) |
</threat_model>

<verification>
- flutter test test/core/ads/ exits 0 (StubAdService parity passes, RealAdService group skipped)
- flutter test test/features/game/game_session_notifier_test.dart — new HINT-04 group shows 3 failures (RED state confirmed)
- No compilation errors in any test file
</verification>

<success_criteria>
Five production AdMob values collected from developer. Two new test files exist under test/core/ads/. game_session_notifier_test.dart has three new RED HINT-04 tests. All existing tests remain passing. Wave 1 can proceed with known production IDs.
</success_criteria>

<output>
Create `.planning/phases/08-full-admob-layer/08-00-SUMMARY.md` when done
</output>
