---
phase: 08-full-admob-layer
plan: "02"
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - lib/core/ads/real_ad_service.dart
  - lib/core/ads/app_state_observer.dart
  - lib/core/ads/ad_service_provider.dart
  - test/core/ads/real_ad_service_test.dart
autonomous: true
requirements: [AD-03, AD-04, AD-05, HINT-05]

must_haves:
  truths:
    - "RealAdService implements AdService with all four ad types: banner, interstitial, rewarded, App Open"
    - "RealAdService constructor takes Ref and reads gameSessionProvider for App Open suppression"
    - "showRewardedAd() uses Completer<bool>; returns true only from onUserEarnedReward, false on dismiss/skip"
    - "showAppOpenAd() suppresses when GamePhase.playing or GamePhase.paused"
    - "getBannerWidget() returns SizedBox.shrink() when no banner loaded"
    - "adServiceProvider returns RealAdService(ref) and calls preloadAll()"
    - "app_state_observer.dart re-exports AppStateEventNotifier and AppState"
    - "real_ad_service_test.dart RealAdService tests are GREEN (no longer skipped)"
  artifacts:
    - path: "lib/core/ads/real_ad_service.dart"
      provides: "Full RealAdService implementation"
      exports: ["RealAdService"]
      contains: "class RealAdService implements AdService"
    - path: "lib/core/ads/app_state_observer.dart"
      provides: "Re-export of AppStateEventNotifier and AppState"
    - path: "lib/core/ads/ad_service_provider.dart"
      provides: "Provider returning RealAdService(ref) with preloadAll()"
  key_links:
    - from: "lib/core/ads/real_ad_service.dart"
      to: "lib/features/game/game_session_notifier.dart"
      via: "_ref.read(gameSessionProvider)"
      pattern: "gameSessionProvider"
    - from: "lib/core/ads/ad_service_provider.dart"
      to: "lib/core/ads/real_ad_service.dart"
      via: "RealAdService(ref)"
      pattern: "RealAdService"
    - from: "lib/core/ads/real_ad_service.dart"
      to: "lib/core/ads/ad_constants.dart"
      via: "kBannerAdUnitId, kInterstitialAdUnitId, kRewardedAdUnitId, kAppOpenAdUnitId"
      pattern: "k.*AdUnitId"
---

<objective>
Create RealAdService (port of Flags AdMobAdService), app_state_observer.dart thin re-export, and switch adServiceProvider from StubAdService to RealAdService. Enable skipped RealAdService tests.

Purpose: The core ad implementation. Once this plan is done, all four ad types are functional behind the AdService interface. app.dart (Wave 3) and UI call sites (Wave 3) can wire against it.
Output: real_ad_service.dart, app_state_observer.dart (new), updated ad_service_provider.dart, GREEN real_ad_service_test.dart.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/08-full-admob-layer/08-CONTEXT.md
@.planning/phases/08-full-admob-layer/08-RESEARCH.md
@.planning/phases/08-full-admob-layer/08-PATTERNS.md
@.planning/phases/08-full-admob-layer/08-01-SUMMARY.md

<interfaces>
From lib/core/ads/ad_service.dart:
```dart
abstract interface class AdService {
  Widget getBannerWidget();
  Future<void> showInterstitialAd();
  Future<bool> showRewardedAd();
  Future<void> showAppOpenAd();
}
```

From lib/core/ads/ad_load_state.dart:
```dart
sealed class AdLoadState { const AdLoadState(); }
class AdLoaded extends AdLoadState { const AdLoaded(); }
class AdFailed extends AdLoadState { const AdFailed(); }
```

From lib/core/ads/ad_constants.dart (after Wave 1):
```dart
const String kAdMobAppId         = '<PROD_APP_ID>';
const String kBannerAdUnitId     = '<PROD_BANNER>';
const String kInterstitialAdUnitId = '<PROD_INTERSTITIAL>';
const String kRewardedAdUnitId   = '<PROD_REWARDED>';
const String kAppOpenAdUnitId    = '<PROD_APP_OPEN>';
const bool   kAppLovinEnabled    = false;
```

From lib/features/game/game_session_notifier.dart:
```dart
final gameSessionProvider = AsyncNotifierProvider<GameSessionNotifier, GameSession>(...);
```

From lib/features/game/game_phase.dart:
```dart
enum GamePhase { idle, countdown, playing, paused, completed }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create real_ad_service.dart and app_state_observer.dart</name>
  <files>lib/core/ads/real_ad_service.dart, lib/core/ads/app_state_observer.dart</files>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ads\admob_ad_service.dart (authoritative baseline — near-verbatim port; rename AdMobAdService → RealAdService, change package prefix flags_around_the_world → state_states)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ads\app_state_observer.dart (5-line re-export file — copy verbatim)
    - lib/core/ads/ad_service.dart (AdService interface — RealAdService must satisfy all four methods)
    - lib/core/ads/ad_load_state.dart (AdLoaded / AdFailed — used by _bannerState field)
    - lib/core/ads/ad_constants.dart (kBannerAdUnitId, kInterstitialAdUnitId, kRewardedAdUnitId, kAppOpenAdUnitId)
    - lib/features/game/game_session_notifier.dart (gameSessionProvider — injected via Ref)
    - lib/features/game/game_phase.dart (GamePhase.playing, GamePhase.paused — suppression check)
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (real_ad_service.dart Pattern Assignment — complete code sections for all four ad types)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pattern 3: 4-hour expiry; Pattern 4: Completer<bool> rewarded; Pattern 5: interstitial preload-on-dismiss; Pattern 6: getLargeAnchoredAdaptiveBannerAdSize; Pitfall 3: App Open gameplay suppression; Pitfall 5: Ref injection)
  </read_first>
  <action>
    Create lib/core/ads/real_ad_service.dart. Import dart:async, package:flutter/widgets.dart, package:flutter_riverpod/flutter_riverpod.dart, package:google_mobile_ads/google_mobile_ads.dart, package:state_states/features/game/game_session_notifier.dart, package:state_states/features/game/game_phase.dart, ad_service.dart, ad_load_state.dart, ad_constants.dart.

    Class `RealAdService implements AdService` with constructor `RealAdService(this._ref)` and field `final Ref _ref`.

    Banner section: fields `BannerAd? _bannerAd` and `AdLoadState _bannerState = const AdFailed()`. Method `Future<void> loadBannerForWidth(int screenWidthDp)` — guard with `if (_bannerState is AdLoaded) return` (prevents double-load per Pitfall 4). Use `AdSize.getLargeAnchoredAdaptiveBannerAdSize(screenWidthDp)` (NOT the deprecated getCurrentOrientation variant). Override `Widget getBannerWidget()` — return SizedBox.shrink() when _bannerAd is null; wrap AdWidget in SizedBox sized to ad.size.width/height.

    Interstitial section: field `InterstitialAd? _interstitialAd`. Private `_preloadInterstitial()` calls InterstitialAd.load() with InterstitialAdLoadCallback. Override `Future<void> showInterstitialAd()` — null-check, set fullScreenContentCallback with onAdDismissedFullScreenContent (dispose + null + _preloadInterstitial) and onAdFailedToShowFullScreenContent (dispose + null), null _interstitialAd BEFORE await ad.show() to prevent double-show.

    Rewarded section: field `RewardedAd? _rewardedAd`. Private `_preloadRewarded()`. Override `Future<bool> showRewardedAd()` — null-check returns false. Create `Completer<bool> completer`. Set fullScreenContentCallback — onAdDismissedFullScreenContent: dispose + null + _preloadRewarded + `if (!completer.isCompleted) completer.complete(false)`. onAdFailedToShowFullScreenContent: dispose + null + `if (!completer.isCompleted) completer.complete(false)`. Null _rewardedAd BEFORE await ad.show(onUserEarnedReward: (_, __) { if (!completer.isCompleted) completer.complete(true); }). Return completer.future. HINT-05 is absolute: complete(true) fires only in onUserEarnedReward.

    App Open section: fields `AppOpenAd? _appOpenAd`, `DateTime? _appOpenLoadTime`, `static const Duration _kAppOpenExpiry = Duration(hours: 4)`. Private getter `bool get _isAppOpenAdAvailable` checks null + DateTime.now().difference(loadTime) < _kAppOpenExpiry. Private `_preloadAppOpen()` uses AppOpenAd.load() with AppOpenAdLoadCallback — stores ad + DateTime.now() on load. Override `Future<void> showAppOpenAd()` — read session phase via `_ref.read(gameSessionProvider).value?.phase`; return early if playing or paused (AD-05 suppression per Pitfall 3). If not available, call _preloadAppOpen and return. Set fullScreenContentCallback, null _appOpenAd BEFORE await ad.show().

    Startup preload: `void preloadAll()` calls _preloadInterstitial, _preloadRewarded, _preloadAppOpen.

    Create lib/core/ads/app_state_observer.dart as a 5-line re-export: `export 'package:google_mobile_ads/google_mobile_ads.dart' show AppStateEventNotifier, AppState;` with a comment explaining it keeps the google_mobile_ads import inside the lib/core/ads/ walled garden.
  </action>
  <verify>
    <automated>flutter analyze lib/core/ads/real_ad_service.dart lib/core/ads/app_state_observer.dart</automated>
  </verify>
  <done>
    real_ad_service.dart exists. `class RealAdService implements AdService` present. loadBannerForWidth uses getLargeAnchoredAdaptiveBannerAdSize. showRewardedAd uses Completer<bool> and completes true only in onUserEarnedReward. showAppOpenAd reads _ref.read(gameSessionProvider).value?.phase and returns early on playing/paused. app_state_observer.dart re-exports AppStateEventNotifier and AppState. flutter analyze exits 0 for both files.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch adServiceProvider to RealAdService and enable real_ad_service_test.dart (AD-03, AD-04, AD-05, HINT-05)</name>
  <files>lib/core/ads/ad_service_provider.dart, test/core/ads/real_ad_service_test.dart</files>
  <read_first>
    - lib/core/ads/ad_service_provider.dart (full file — current stub returning StubAdService; replace with RealAdService(ref) + preloadAll())
    - lib/core/ads/real_ad_service.dart (just created — import path for RealAdService)
    - lib/core/ads/stub_ad_service.dart (keep this file, do NOT delete — still used by tests)
    - test/core/ads/real_ad_service_test.dart (Wave 0 stub — remove skip markers, add RealAdService import, implement test bodies)
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (ad_service_provider.dart Pattern Assignment — exact three-line provider body; real_ad_service_test.dart Pattern Assignment — Mock class + ProviderContainer pattern)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pitfall 5 — adServiceProvider must call preloadAll(); Validation Architecture — exact test group structure for AD-03 through AD-05, HINT-05)
  </read_first>
  <behavior>
    adServiceProvider:
    - Returns RealAdService(ref) and calls service.preloadAll() before returning

    real_ad_service_test.dart (test bodies to implement):
    - "AD-03: getBannerWidget returns SizedBox.shrink when no banner loaded" — construct RealAdService with a mock Ref that reads AsyncData(GameSession in idle phase); call getBannerWidget(); expect result is SizedBox with width==0 and height==0 OR is SizedBox.shrink()
    - "AD-04: showInterstitialAd no-ops when _interstitialAd is null" — same mock Ref; showInterstitialAd() completes without throwing
    - "AD-05: showAppOpenAd suppressed when GamePhase.playing" — mock Ref returns AsyncData(GameSession(phase: GamePhase.playing)); call showAppOpenAd(); expect no ad SDK call attempted (ad was null anyway, but method returns early before the _isAppOpenAdAvailable check)
    - "HINT-05: showRewardedAd returns false when _rewardedAd is null" — RealAdService with null _rewardedAd; call showRewardedAd(); expect result == false
  </behavior>
  <action>
    Update lib/core/ads/ad_service_provider.dart: Remove the StubAdService import and the const StubAdService() return. Add import for real_ad_service.dart (keep ad_service.dart import). Replace provider body with `Provider<AdService>((ref) { final service = RealAdService(ref); service.preloadAll(); return service; })`. Update the doccomment to reflect Phase 8 activation.

    Update test/core/ads/real_ad_service_test.dart: Add import for real_ad_service.dart and the GameSession/GamePhase/game_session_notifier types needed for mock construction. Create a MockRef class (extends Mock implements Ref). In setUp, create a mock Ref that returns appropriate AsyncData values for gameSessionProvider. Remove all `skip: '...'` markers from RealAdService test cases. Implement test bodies as described in behavior. The GMA method channel mock from ads_initializer_test.dart should be registered in setUp to prevent MissingPluginException — use the same `TestDefaultBinaryMessengerBinding` pattern.
  </action>
  <verify>
    <automated>flutter test test/core/ads/real_ad_service_test.dart --reporter compact</automated>
  </verify>
  <done>
    adServiceProvider returns RealAdService(ref) and calls preloadAll(). stub_ad_service.dart still exists (not deleted). real_ad_service_test.dart has all four RealAdService tests GREEN (no skips). StubAdService interface parity tests still pass. `flutter analyze lib/core/ads/ad_service_provider.dart` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RealAdService → GMA SDK | Ad SDK method calls; SDK handles all network/platform interactions |
| RealAdService → gameSessionProvider | Ref.read() — read-only, no modification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Tampering | Rewarded reward granted on ad dismiss (HINT-05) | mitigate | Completer<bool>: complete(true) ONLY in onUserEarnedReward; complete(false) in onAdDismissedFullScreenContent guarded with isCompleted check |
| T-08-02-02 | Denial of Experience | App Open fires during active gameplay | mitigate | showAppOpenAd() reads gameSessionProvider phase; returns early on playing/paused (AD-05) |
| T-08-02-03 | Tampering | Double-dispose of ad objects | mitigate | Ad field nulled BEFORE show() call and again in dismiss callback — matches Flags pattern exactly |
| T-08-02-04 | Denial of Experience | Banner loaded multiple times (memory leak) | mitigate | loadBannerForWidth guards with `if (_bannerState is AdLoaded) return` |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | No new package installs in Wave 2; packages resolved in Wave 1 |
</threat_model>

<verification>
- flutter test test/core/ads/ exits 0 (all tests in both ads test files pass)
- flutter analyze lib/core/ads/ exits 0
- real_ad_service.dart contains "class RealAdService implements AdService"
- real_ad_service.dart contains "Completer<bool>"
- real_ad_service.dart contains "getLargeAnchoredAdaptiveBannerAdSize"
- real_ad_service.dart does NOT contain "getCurrentOrientationAnchoredAdaptiveBannerAdSize"
- ad_service_provider.dart contains "RealAdService(ref)" and "preloadAll()"
- stub_ad_service.dart still exists (not deleted)
</verification>

<success_criteria>
RealAdService fully implements AdService with all four ad types. Completer-based rewarded ad correctly returns true only on onUserEarnedReward. App Open suppression reads gameSessionProvider phase. adServiceProvider wired to RealAdService with preloadAll(). app_state_observer.dart created. All real_ad_service_test.dart tests pass.
</success_criteria>

<output>
Create `.planning/phases/08-full-admob-layer/08-02-SUMMARY.md` when done
</output>
