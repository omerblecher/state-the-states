---
phase: 08-full-admob-layer
plan: "05"
type: execute
wave: 4
depends_on: ["08-03", "08-04"]
files_modified: []
autonomous: false
requirements: [AD-06]

must_haves:
  truths:
    - "flutter build apk --release exits 0 with all three mediation AARs present"
    - "aapt dump badging on the release APK shows no AD_ID, AdServices, or ADVERTISING permission"
    - "AD_ID absence confirmed after all mediation adapter AARs are included in merged manifest"
  artifacts:
    - path: "build/app/outputs/flutter-apk/app-release.apk"
      provides: "Release APK with all mediation AARs merged"
  key_links:
    - from: "android/app/src/main/AndroidManifest.xml"
      to: "build/app/outputs/flutter-apk/app-release.apk"
      via: "tools:node=remove surviving AAR manifest merge"
      pattern: "tools:node.*remove"
---

<objective>
Build a release APK with all three mediation adapters present and verify that the AD_ID permission remains absent in the merged manifest — confirming COPPA compliance after Phase 8 changes.

Purpose: AD-06 is a build-time verification that cannot be tested in Dart unit tests. It is the final gate before Phase 8 can be verified complete.
Output: Human confirmation that aapt dump badging shows no AD_ID, AdServices, or ADVERTISING permissions.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/08-full-admob-layer/08-CONTEXT.md
@.planning/phases/08-full-admob-layer/08-RESEARCH.md
@.planning/phases/08-full-admob-layer/08-03-SUMMARY.md
@.planning/phases/08-full-admob-layer/08-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build release APK with all mediation AARs</name>
  <files></files>
  <read_first>
    - android/app/src/main/AndroidManifest.xml (confirm all four tools:node="remove" blocks are still present before building)
    - pubspec.yaml (confirm all three gma_mediation_* entries are present)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Environment Availability section — aapt.exe path: C:\Users\omerb\AppData\Local\Android\Sdk\build-tools\37.0.0\aapt.exe; Pitfall 2 — AD_ID erasure by mediation AARs)
  </read_first>
  <action>
    Run `flutter build apk --release`. This triggers a full Gradle build that merges all AAR manifests from google_mobile_ads, gma_mediation_unity, gma_mediation_ironsource, and gma_mediation_inmobi. The build must complete without error before the AD-06 verification can run.

    If the build fails due to manifest merge conflicts (Pitfall 2), inspect `build/intermediates/merged_manifests/release/AndroidManifest.xml` for conflicting permission nodes and add explicit `tools:merge` or `tools:replace` attributes as needed.
  </action>
  <verify>
    <automated>flutter build apk --release</automated>
  </verify>
  <done>
    flutter build apk --release exits 0. build/app/outputs/flutter-apk/app-release.apk exists.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AD_ID absent after mediation AAR merge (AD-06, D-05)</name>
  <what-built>
    Claude built a release APK that includes all three mediation adapter AARs (Unity, ironSource, InMobi). The tools:node="remove" blocks in AndroidManifest.xml should have survived the AAR merge. This checkpoint confirms AD-06 — the AD_ID permission is still absent from the final APK.
  </what-built>
  <how-to-verify>
    Run this command from the repository root (PowerShell):

    ```
    & "C:\Users\omerb\AppData\Local\Android\Sdk\build-tools\37.0.0\aapt.exe" dump badging "build\app\outputs\flutter-apk\app-release.apk" | Select-String -Pattern "AD_ID|AdServices|ADVERTISING" -CaseSensitive:$false
    ```

    PASS: Command returns no output (no matching lines) — AD_ID permission is absent.

    FAIL: Command returns one or more lines containing "AD_ID", "AdServices", or "ADVERTISING" — permission leaked through manifest merge.

    If FAIL: Open android/app/src/main/AndroidManifest.xml and verify all four tools:node="remove" blocks are present. Then check build/intermediates/merged_manifests/release/AndroidManifest.xml (if it exists) to identify which AAR injected the permission and whether a tools:merge="override" attribute is needed.
  </how-to-verify>
  <resume-signal>
    Type "AD_ID absent — PASS" if the aapt command returns no output.
    Type "AD_ID present — lines: [paste output]" if permissions were found, so Claude can diagnose.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mediation AAR manifests → merged AndroidManifest.xml | Third-party AARs may inject permissions; tools:node="remove" must win the merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-05-01 | Information Disclosure | AD_ID resurfaces after mediation AAR merge | mitigate | aapt dump badging human-verify checkpoint; if present: add tools:merge="override" or explicit overrideLibrary attribute |
| T-08-05-02 | Information Disclosure | AdServices (Privacy Sandbox) permissions resurface | mitigate | Same aapt check covers ACCESS_ADSERVICES_AD_ID, ACCESS_ADSERVICES_ATTRIBUTION, ACCESS_ADSERVICES_TOPICS — all four tools:node="remove" blocks in manifest |
| T-08-SC | Tampering | npm/pip/cargo installs | accept | flutter build apk uses Gradle with already-resolved pubspec.lock; no new package resolution |
</threat_model>

<verification>
- flutter build apk --release exits 0
- aapt.exe dump badging on app-release.apk | findstr /i "AD_ID AdServices ADVERTISING" returns no output
- All four tools:node="remove" blocks present in android/app/src/main/AndroidManifest.xml
</verification>

<success_criteria>
Release APK builds successfully. aapt dump badging confirms zero AD_ID, AdServices, or ADVERTISING permissions in the merged manifest. COPPA compliance verified for Phase 8.
</success_criteria>

<output>
Create `.planning/phases/08-full-admob-layer/08-05-SUMMARY.md` when done
</output>
