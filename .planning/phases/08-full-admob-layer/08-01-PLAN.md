---
phase: 08-full-admob-layer
plan: "01"
type: execute
wave: 1
depends_on: ["08-00"]
files_modified:
  - pubspec.yaml
  - lib/core/ads/ads_initializer.dart
  - lib/core/ads/ad_constants.dart
  - android/app/src/main/AndroidManifest.xml
  - lib/features/game/game_session_notifier.dart
autonomous: true
requirements: [AD-01, AD-02, HINT-04]

must_haves:
  truths:
    - "pubspec.yaml declares all three mediation packages at locked versions"
    - "ads_initializer.dart sets ironSource and Unity COPPA flags before MobileAds.instance.initialize()"
    - "ads_initializer.dart does NOT import gma_mediation_inmobi"
    - "ad_constants.dart holds the production App ID and all four unit IDs"
    - "AndroidManifest.xml APPLICATION_ID meta-data holds the production App ID"
    - "GameSessionNotifier.refillHints() resets hintsRemaining to 2 and has zero ad imports"
    - "HINT-04 RED tests from Wave 0 now pass GREEN"
  artifacts:
    - path: "pubspec.yaml"
      provides: "gma_mediation_unity ^1.8.0, gma_mediation_ironsource ^2.4.1, gma_mediation_inmobi ^2.1.0"
      contains: "gma_mediation_unity"
    - path: "lib/core/ads/ads_initializer.dart"
      provides: "COPPA mediation flags before initialize()"
      exports: ["initializeAds"]
    - path: "lib/core/ads/ad_constants.dart"
      provides: "production ad unit IDs"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "production AdMob App ID in APPLICATION_ID meta-data"
    - path: "lib/features/game/game_session_notifier.dart"
      provides: "refillHints() method"
  key_links:
    - from: "lib/core/ads/ads_initializer.dart"
      to: "MobileAds.instance.initialize()"
      via: "mediation COPPA calls precede this line"
      pattern: "setConsent|setGDPRConsent"
    - from: "lib/features/game/game_session_notifier.dart"
      to: "refillHints"
      via: "method definition"
      pattern: "void refillHints"
---

<objective>
Add three mediation packages to pubspec, wire per-SDK COPPA flags in ads_initializer.dart, update production IDs in ad_constants.dart and AndroidManifest.xml, and add refillHints() to GameSessionNotifier — making the HINT-04 tests go GREEN.

Purpose: All pure-Dart, non-widget configuration changes that unlock Wave 2 (RealAdService implementation). Mediation SDKs must be in pubspec before RealAdService can import them.
Output: Updated pubspec.yaml, ads_initializer.dart, ad_constants.dart, AndroidManifest.xml, game_session_notifier.dart.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/08-full-admob-layer/08-CONTEXT.md
@.planning/phases/08-full-admob-layer/08-RESEARCH.md
@.planning/phases/08-full-admob-layer/08-PATTERNS.md
@.planning/phases/08-full-admob-layer/08-00-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mediation packages to pubspec.yaml and run flutter pub get (D-03)</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml (current dependencies section — add after google_mobile_ads: ^8.0.0)
    - .planning/phases/08-full-admob-layer/08-CONTEXT.md (D-03 — exact versions: unity ^1.8.0, ironsource ^2.4.1, inmobi ^2.1.0)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Package Legitimacy Audit — all three are google.dev [OK] Approved)
  </read_first>
  <action>
    Add three lines to the dependencies section of pubspec.yaml, immediately after the existing `google_mobile_ads: ^8.0.0` line:
    - gma_mediation_unity: ^1.8.0
    - gma_mediation_ironsource: ^2.4.1
    - gma_mediation_inmobi: ^2.1.0
    Do NOT add gma_mediation_applovin — D-03 explicitly prohibits this. kAppLovinEnabled = false remains. After editing, run `flutter pub get` to resolve the lockfile.
  </action>
  <verify>
    <automated>dart run --no-serve pubspec.yaml; flutter pub get; grep -c "gma_mediation_unity\|gma_mediation_ironsource\|gma_mediation_inmobi" pubspec.yaml</automated>
  </verify>
  <done>
    pubspec.yaml contains all three gma_mediation_* entries. `flutter pub get` exits 0. pubspec.lock resolves all three packages. gma_mediation_applovin does NOT appear in pubspec.yaml or pubspec.lock.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mediation COPPA flags to ads_initializer.dart and production IDs to ad_constants.dart + AndroidManifest.xml (D-02, D-04, D-01, D-02 manifest)</name>
  <files>lib/core/ads/ads_initializer.dart, lib/core/ads/ad_constants.dart, android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - lib/core/ads/ads_initializer.dart (full file — current initializeAds() function; add mediation calls between updateRequestConfiguration and MobileAds.instance.initialize())
    - lib/core/ads/ad_constants.dart (full file — replace empty strings with production values from Wave 0 checkpoint)
    - android/app/src/main/AndroidManifest.xml (full file — replace test App ID at line 57 with production App ID)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pattern 1 — exact method signatures and import list; Pitfall 1 — InMobi has NO Dart-side COPPA API, do not import gma_mediation_inmobi)
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (ads_initializer.dart Pattern Assignment — exact imports and call sequence)
    - .planning/phases/08-full-admob-layer/08-00-SUMMARY.md (contains the five production IDs collected in Wave 0)
  </read_first>
  <behavior>
    ads_initializer.dart changes:
    - Add import 'package:gma_mediation_ironsource/gma_mediation_ironsource.dart'
    - Add import 'package:gma_mediation_unity/gma_mediation_unity.dart'
    - Add import 'ad_constants.dart' (for kAppLovinEnabled)
    - Do NOT add import for gma_mediation_inmobi (empty stub class — per Pitfall 1)
    - Remove the v1 comment "Mediation SDK COPPA flag calls are omitted in v1 — no mediation SDKs are present (gma_mediation_* packages are v2 scope only)"
    - After updateRequestConfiguration() and before await MobileAds.instance.initialize():
      Step 2: GmaMediationIronsource().setConsent(false) — GDPR no-consent for children
      Step 3: GmaMediationIronsource().setDoNotSell(true) — CCPA/US state privacy do-not-sell
      Step 4: await GmaMediationUnity().setGDPRConsent(false) — Unity GDPR no-consent
      Step 5: await GmaMediationUnity().setCCPAConsent(false) — Unity CCPA no-consent
      Step 6: if (kAppLovinEnabled) block (AppLovin permanently disabled guard)
    - InMobi comment: "// InMobi: NO Dart call. GmaMediationInMobi is an empty stub class. tagForChildDirectedTreatment auto-forwarded by native adapter."

    ad_constants.dart changes:
    - Replace kAdMobTestAppId constant name with kAdMobAppId (remove "Test" from name)
    - Replace test App ID value with the production App ID from Wave 0 checkpoint
    - Replace all four empty-string unit IDs with production values from Wave 0 checkpoint
    - kAppLovinEnabled = false — unchanged

    AndroidManifest.xml changes:
    - Replace android:value in APPLICATION_ID meta-data from "ca-app-pub-3940256099942544~3347511713" to the production App ID
    - All four tools:node="remove" blocks unchanged — must not be touched
  </behavior>
  <verify>
    <automated>flutter test test/core/ads/ads_initializer_test.dart --reporter compact</automated>
  </verify>
  <done>
    ads_initializer.dart contains both GmaMediationIronsource calls and both GmaMediationUnity calls. ads_initializer.dart does NOT contain the string "gma_mediation_inmobi". ad_constants.dart has no empty-string unit IDs. AndroidManifest.xml APPLICATION_ID holds the production App ID. ads_initializer_test.dart AD-01 and AD-02 tests pass GREEN (or if SDK mock is needed, skips acceptably). `flutter analyze lib/core/ads/ads_initializer.dart` exits 0.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add refillHints() to GameSessionNotifier (HINT-04)</name>
  <files>lib/features/game/game_session_notifier.dart</files>
  <read_first>
    - lib/features/game/game_session_notifier.dart (full file — understand useHint() at lines 293-314 which is the direct pattern; add refillHints() as the next sibling method)
    - .planning/phases/08-full-admob-layer/08-RESEARCH.md (Pattern 7 — exact refillHints() implementation: playing-phase guard, copyWith(hintsRemaining: 2), saveSession, no score/penalty change, zero ad imports)
    - .planning/phases/08-full-admob-layer/08-PATTERNS.md (game_session_notifier.dart Pattern Assignment — full method body)
    - test/features/game/game_session_notifier_test.dart (HINT-04 group from Wave 0 — the tests that must go GREEN)
  </read_first>
  <behavior>
    - refillHints() called when session.hintsRemaining == 0 and phase == GamePhase.playing → state becomes hintsRemaining == 2
    - refillHints() called when phase != GamePhase.playing → state unchanged (no-op)
    - refillHints() does NOT modify score or _hintPenalty (no +5 penalty — that is useHint()'s responsibility)
    - refillHints() calls saveSession to persist the updated hintsRemaining
    - Zero ad imports in game_session_notifier.dart (walled-garden rule inviolable — COMP-03)
  </behavior>
  <action>
    Insert refillHints() as a sibling method immediately after useHint() (after line 314, before completeGame()). The method signature is `void refillHints()`. Guard: if state.value is null or state.value.phase is not GamePhase.playing, return immediately. Body: `state = AsyncData(current.copyWith(hintsRemaining: 2))` followed by `_gameStateRepository?.saveSession(state.value!, hintPenalty: _hintPenalty)`. Add a dartdoc comment: "Resets hintsRemaining to exactly 2 (not additive). Called from the widget layer inside onUserEarnedReward only. WALLED-GARDEN RULE: Zero ad imports."

    After writing, run `grep "^import.*ads\|^import.*google_mobile_ads\|^import.*gma_mediation" lib/features/game/game_session_notifier.dart` — expected: no output (confirms walled-garden rule).
  </action>
  <verify>
    <automated>flutter test test/features/game/game_session_notifier_test.dart --reporter compact</automated>
  </verify>
  <done>
    The three HINT-04 test cases that were RED in Wave 0 now pass GREEN. All previously passing tests in game_session_notifier_test.dart still pass. game_session_notifier.dart contains no ad imports (grep confirms empty output). `flutter analyze lib/features/game/game_session_notifier.dart` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec.yaml → pub.dev | Package resolution; mediation packages are google.dev publisher (Legitimacy Audit: all [OK]) |
| ad_constants.dart → production IDs | Developer-supplied values; format validated by AdMob SDK at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | Information Disclosure | AD_ID resurfaces after mediation AAR merge | mitigate | Verified in Wave 5 via aapt dump badging (AD-06); tools:node="remove" blocks remain in place |
| T-08-01-02 | Tampering | COPPA flags called after initialize() | mitigate | Code order enforced in ads_initializer.dart: all flag calls before await MobileAds.instance.initialize() |
| T-08-01-03 | Tampering | gma_mediation_applovin accidentally added | mitigate | D-03 locked; pubspec edit is surgical (only three lines added) |
| T-08-SC | Tampering | flutter pub get fetches gma_mediation_* | accept | All three packages are google.dev [OK] per Package Legitimacy Audit in RESEARCH.md |
</threat_model>

<verification>
- flutter test test/core/ads/ads_initializer_test.dart exits 0
- flutter test test/features/game/game_session_notifier_test.dart — all tests pass (HINT-04 group now GREEN)
- flutter analyze lib/core/ads/ lib/features/game/game_session_notifier.dart exits 0
- grep output for "gma_mediation_inmobi" in lib/core/ads/ads_initializer.dart: no output
- grep output for "^import.*ads\|^import.*google_mobile_ads\|^import.*gma_mediation" in lib/features/game/game_session_notifier.dart: no output
</verification>

<success_criteria>
pubspec.yaml declares three mediation packages. ads_initializer.dart has ironSource and Unity COPPA calls before MobileAds.initialize(). InMobi has no Dart call. ad_constants.dart and AndroidManifest.xml hold production IDs. GameSessionNotifier.refillHints() exists with zero ad imports. HINT-04 tests are GREEN.
</success_criteria>

<output>
Create `.planning/phases/08-full-admob-layer/08-01-SUMMARY.md` when done
</output>
