---
phase: 04-full-play-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/app.dart
  - lib/features/map/completion_screen.dart
  - test/features/home/home_screen_test.dart
  - test/features/map/completion_screen_test.dart
  - test/features/map/state_tray_test.dart
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - SCORE-06
  - SCORE-07

must_haves:
  truths:
    - "app.dart GoRouter includes a /complete route that returns CompletionScreen"
    - "app.dart /play route builder extracts GameMode from state.extra and passes it to MapScreen"
    - "MapScreen constructor accepts mode as non-nullable GameMode with default GameMode.learn"
    - "CompletionScreen stub renders a Back to Menu button that navigates to /"
    - "All three Wave 0 test files exist with compilable stub tests"
  artifacts:
    - path: "lib/app.dart"
      provides: "Updated routing — /play extracts GameMode extra; /complete route added"
      contains: "path: '/complete'"
    - path: "lib/features/map/completion_screen.dart"
      provides: "CompletionScreen stub widget"
      exports: ["CompletionScreen", "computeStarCount"]
    - path: "test/features/home/home_screen_test.dart"
      provides: "HOME-01/02 stub tests"
      contains: "home_screen_test"
    - path: "test/features/map/completion_screen_test.dart"
      provides: "SCORE-06/07 star formula unit tests"
      contains: "computeStarCount"
    - path: "test/features/map/state_tray_test.dart"
      provides: "MODE-01 through MODE-04 tray content stub tests"
      contains: "state_tray_test"
  key_links:
    - from: "lib/app.dart"
      to: "lib/features/map/completion_screen.dart"
      via: "GoRoute /complete builder"
      pattern: "CompletionScreen"
    - from: "lib/app.dart"
      to: "lib/features/map/map_screen.dart"
      via: "GoRoute /play builder"
      pattern: "MapScreen\\(mode:"
---

<objective>
Wire the routing changes and create foundation artifacts so all subsequent waves can compile and test against real routes.

Purpose: Every Wave 2+ plan needs /play to accept GameMode and /complete to exist. Without this, integration tests for the game screen and completion screen fail to compile. The Wave 0 test stubs guarantee Nyquist compliance — no later task ships without a test file already in place.

Output: Updated app.dart with /play GameMode extraction and /complete route; CompletionScreen stub with computeStarCount exported; three Wave 0 test file stubs.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-full-play-loop/04-CONTEXT.md
@.planning/phases/04-full-play-loop/04-RESEARCH.md
@.planning/phases/04-full-play-loop/04-PATTERNS.md
@.planning/phases/04-full-play-loop/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/app.dart (current):
```dart
// Current /play route — no GameMode:
GoRoute(
  path: '/play',
  builder: (context, state) => const MapScreen(),
),
// No /complete route exists yet.
```

From lib/features/map/map_screen.dart (current constructor):
```dart
class MapScreen extends ConsumerStatefulWidget {
  const MapScreen({
    super.key,
    this.matchedPostals = const {},
    this.showLabels = false,
    this.mode,                 // GameMode? nullable — Phase 4 changes to non-nullable with default
  });
  final Set<String> matchedPostals;
  final bool showLabels;
  final GameMode? mode;
}
```

From lib/features/game/game_session.dart:
```dart
class GameSession {
  final GamePhase phase;
  final GameMode mode;
  final int score;
  final Duration elapsed;
  final int errorCount;
  final String? activePostal;
  final int hintsRemaining;
  final List<String> matchedPostals;
}
```

From lib/features/game/game_mode.dart:
```dart
enum GameMode { learn, statesMaster, geographicalMaster, grandMaster }
```

From lib/core/data/high_score_repository.dart:
```dart
abstract interface class HighScoreRepository {
  Future<int?> getBestScore(GameMode mode);
  Future<void> saveBestScore(GameMode mode, int score);
}
final highScoreRepositoryProvider = FutureProvider<HighScoreRepository>(...);
```

From test/features/map/map_screen_test.dart (existing pattern for ProviderScope override):
```dart
await tester.pumpWidget(ProviderScope(
  overrides: [stateDataProvider.overrideWith((ref) async => mapData!)],
  child: const MaterialApp(home: MapScreen()),
));
await tester.pump();
await tester.pump();
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update app.dart routing and MapScreen constructor</name>
  <files>lib/app.dart, lib/features/map/map_screen.dart</files>
  <read_first>
    - lib/app.dart (current routing — see current /play route structure)
    - lib/features/map/map_screen.dart (constructor — mode is currently GameMode? nullable)
    - lib/features/game/game_mode.dart (GameMode enum values)
    - lib/features/game/game_session.dart (GameSession fields needed by /complete route)
    - test/features/map/map_screen_test.dart (existing tests call const MapScreen() — must keep compiling)
    - test/features/map/spike_map_screen_test.dart (may also call MapScreen — check for breakage)
  </read_first>
  <behavior>
    - /play route builder: extract GameMode via `state.extra as GameMode? ?? GameMode.learn`, pass as `MapScreen(mode: mode)`
    - /complete route builder: cast `state.extra as Map` and extract session + previousBest, pass to CompletionScreen
    - MapScreen constructor: change `GameMode? mode` to `GameMode mode = GameMode.learn` (non-nullable with default, preserving backward compat per RESEARCH.md Risk 7)
    - MapScreen internal usage: update `widget.mode` usages — mode is now non-nullable, remove null-check patterns
    - Import CompletionScreen in app.dart once the file exists (stub is created in Task 2)
  </behavior>
  <action>
    In lib/app.dart:

    Change the /play GoRoute builder to extract GameMode from state.extra: `final mode = state.extra as GameMode? ?? GameMode.learn; return MapScreen(mode: mode);`

    Add a new /complete GoRoute after the /play route: builder casts `state.extra as Map<String, dynamic>`, extracts `extra['session'] as GameSession` and `extra['previousBest'] as int?`, returns `CompletionScreen(session: session, previousBest: previousBest)`.

    Add import for CompletionScreen: `import 'features/map/completion_screen.dart';`
    Add import for GameSession: `import 'features/game/game_session.dart';`

    In lib/features/map/map_screen.dart:

    Change the constructor parameter from `this.mode` (GameMode? nullable) to `this.mode = GameMode.learn` (GameMode non-nullable with default). Change the field declaration from `final GameMode? mode;` to `final GameMode mode;`.

    Update the UsaMapPainter call inside _buildMapStack: the `mode:` parameter is now non-nullable — remove any null coalescing. The painter already accepts `GameMode?` per Phase 3; passing a non-nullable value is safe.

    Do NOT change the spike route registration (kDebugMode guard) or any test files.
  </action>
  <verify>
    <automated>flutter test test/features/map/map_screen_test.dart test/features/map/spike_map_screen_test.dart</automated>
  </verify>
  <done>
    - lib/app.dart GoRoute path '/play' builder contains `state.extra as GameMode? ?? GameMode.learn` and passes mode to MapScreen
    - lib/app.dart GoRoute path '/complete' exists and extracts session + previousBest from extra map
    - MapScreen field declaration reads `final GameMode mode;` (non-nullable)
    - MapScreen constructor default reads `this.mode = GameMode.learn`
    - Existing map_screen_test.dart and spike_map_screen_test.dart pass without modification
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CompletionScreen stub + Wave 0 test files</name>
  <files>
    lib/features/map/completion_screen.dart,
    test/features/home/home_screen_test.dart,
    test/features/map/completion_screen_test.dart,
    test/features/map/state_tray_test.dart
  </files>
  <read_first>
    - lib/features/game/game_session.dart (GameSession fields — constructor takes session + previousBest)
    - lib/features/game/game_mode.dart (GameMode enum for mode color helper)
    - lib/core/data/high_score_repository.dart (HighScoreRepository interface and highScoreRepositoryProvider)
    - lib/features/home/home_screen.dart (current placeholder — imported in home_screen_test)
    - test/features/map/map_screen_test.dart (ProviderScope override pattern to replicate in test stubs)
    - .planning/phases/04-full-play-loop/04-PATTERNS.md (completion_screen_test stub pattern lines 1101–1125; home_screen_test stub pattern lines 1063–1096; state_tray_test stub pattern lines 1129–1161)
  </read_first>
  <behavior>
    CompletionScreen stub behaviors:
    - Exports top-level function `computeStarCount(int score, int? previousBest) -> int`
    - computeStarCount: returns 3 when previousBest is null; 3 when score < previousBest; 2 when score <= (previousBest * 1.20).ceil(); 1 otherwise (D-11)
    - Stub widget renders: score display text "Score: {N}", Back to Menu ElevatedButton that calls `context.go('/')`, Play Again OutlinedButton that calls `context.go('/play', extra: widget.session.mode)`
    - Does NOT import share_plus, does NOT call any AdMob method (D-13)

    completion_screen_test.dart stub behaviors (unit tests only — widget tests added in Plan 05):
    - computeStarCount(100, null) == 3 (first game)
    - computeStarCount(50, 100) == 3 (personal best)
    - computeStarCount(115, 100) == 2 (within 20%)
    - computeStarCount(125, 100) == 1 (more than 20% above)

    home_screen_test.dart stub behaviors:
    - Contains a compilable testWidgets that pumps HomeScreen with highScoreRepositoryProvider overridden
    - Test body is a skip/TODO stub — actual assertions added in Plan 06
    - Mock class `MockHighScoreRepository extends Mock implements HighScoreRepository`

    state_tray_test.dart stub behaviors:
    - Contains a compilable testWidgets stub importing state_tray.dart
    - Since state_tray.dart does not yet exist, import it as a forward reference and mark test with `skip: 'StateTray not yet implemented'`
    - Actual assertions (MODE-01 through MODE-04 label visibility, bounce trigger) added in Plan 03
  </behavior>
  <action>
    Create lib/features/map/completion_screen.dart as a `ConsumerStatefulWidget` with `SingleTickerProviderStateMixin`. Constructor: `CompletionScreen({required GameSession session, int? previousBest})`. Export top-level `computeStarCount(int score, int? previousBest)` implementing D-11 formula exactly. The stub build() returns a Scaffold with AppBar title = session.mode.name, body Column containing: Text('Score: ${widget.session.score}'), ElevatedButton.icon with icon Icons.home and label 'Back to Menu' that calls `context.go('/')`, OutlinedButton.icon with icon Icons.replay and label 'Play Again' that calls `context.go('/play', extra: widget.session.mode)`. Import go_router for context.go.

    Create test/features/map/completion_screen_test.dart with the four unit tests for computeStarCount using the exact boundary values from D-11: score=100/previousBest=null expects 3; score=50/previousBest=100 expects 3; score=115/previousBest=100 expects 2; score=125/previousBest=100 expects 1.

    Create test/features/home/home_screen_test.dart importing mocktail, HighScoreRepository, highScoreRepositoryProvider, HomeScreen. Declare `class MockHighScoreRepository extends Mock implements HighScoreRepository {}`. Write one testWidgets 'HomeScreen shows mode cards' that sets up MockHighScoreRepository with `when(() => mockRepo.getBestScore(any())).thenAnswer((_) async => null)`, pumps HomeScreen in ProviderScope overriding highScoreRepositoryProvider, and calls `expect(true, isTrue)` as a passing placeholder — comment says "assertions added in Plan 06".

    Create test/features/map/state_tray_test.dart importing StateTray from `package:state_states/features/game/state_tray.dart` with a single testWidgets marked `skip: 'StateTray not yet implemented — assertions added in Plan 03'`. The test body has no assertions. This ensures the file compiles once state_tray.dart is created.
  </action>
  <verify>
    <automated>flutter test test/features/map/completion_screen_test.dart</automated>
  </verify>
  <done>
    - lib/features/map/completion_screen.dart exists, exports computeStarCount, has Back to Menu and Play Again buttons
    - completion_screen_test.dart: 4 computeStarCount unit tests all pass (flutter test exits 0)
    - home_screen_test.dart: file exists and compiles (MockHighScoreRepository declared, one passing stub test)
    - state_tray_test.dart: file exists and compiles (one skipped test referencing state_tray.dart import)
    - flutter test runs without import errors across all three new test files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| router extra params | `state.extra as GameMode?` and `state.extra as Map` — invalid extras crash at route entry; app-internal navigation so no injection risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | GoRoute /complete extra cast | accept | Internal navigation only; extra is set by trusted MapScreen._advanceToNextPostal(); crash on invalid cast is acceptable as a programming error guard, not a security boundary |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | Phase 4 installs no new packages — audit not applicable |
</threat_model>

<verification>
flutter test test/features/map/map_screen_test.dart test/features/map/spike_map_screen_test.dart test/features/map/completion_screen_test.dart
</verification>

<success_criteria>
- lib/app.dart: /play route passes GameMode to MapScreen; /complete route exists and routes to CompletionScreen
- MapScreen.mode is non-nullable GameMode with default GameMode.learn; all Phase 3 tests still pass
- computeStarCount(int, int?) exported from completion_screen.dart; 4 unit tests green
- Three Wave 0 test files exist and compile: home_screen_test.dart, completion_screen_test.dart, state_tray_test.dart
</success_criteria>

<output>
Create `.planning/phases/04-full-play-loop/04-01-SUMMARY.md` when done
</output>
