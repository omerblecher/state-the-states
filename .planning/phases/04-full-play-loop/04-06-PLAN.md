---
phase: 04-full-play-loop
plan: 06
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - lib/features/home/home_screen.dart
  - test/features/home/home_screen_test.dart
autonomous: true
requirements:
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "HomeScreen shows exactly 4 mode cards in a ListView"
    - "Each mode card shows the mode name, one-line description, and taps to /play with GameMode extra"
    - "Each card FutureBuilder calls HighScoreRepository.getBestScore(mode) and displays result"
    - "Cards display 'Not played' when getBestScore returns null"
    - "Cards display 3 star icons; filled stars computed by _starsForScore()"
    - "Tapping a card navigates to /play with the correct GameMode as extra"
  artifacts:
    - path: "lib/features/home/home_screen.dart"
      provides: "Full HomeScreen with 4 _ModeCard widgets — replaces Phase 1 placeholder"
      exports: ["HomeScreen"]
    - path: "test/features/home/home_screen_test.dart"
      provides: "Widget tests for 4 mode cards rendered, score display, navigation"
      contains: "home_screen_test"
  key_links:
    - from: "lib/features/home/home_screen.dart _ModeCard"
      to: "lib/core/data/high_score_repository.dart"
      via: "FutureBuilder<int?>(future: widget.bestScoreFuture)"
      pattern: "getBestScore"
    - from: "lib/features/home/home_screen.dart"
      to: "lib/app.dart /play route"
      via: "context.go('/play', extra: mode)"
      pattern: "context.go.*play"
---

<objective>
Replace the Phase 1 HomeScreen placeholder with the full mode card list: 4 gradient cards with live best-score display and tap-to-play navigation — a direct port of Flags' home_screen.dart with mode names, descriptions, icons, and colors updated for State States.

Purpose: This is the entry point players see first. The mode cards set expectations for each difficulty tier. The FutureBuilder pattern for score display must handle loading and null states cleanly so first-launch players see a welcoming "Not played" state, not a broken UI.

Output: Full HomeScreen replacing placeholder, complete home_screen_test.dart tests for all 4 mode cards.
</objective>

<execution_context>
@C:\Users\omerb\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omerb\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-full-play-loop/04-CONTEXT.md
@.planning/phases/04-full-play-loop/04-RESEARCH.md
@.planning/phases/04-full-play-loop/04-PATTERNS.md
@.planning/phases/04-full-play-loop/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From lib/core/data/high_score_repository.dart:
```dart
abstract interface class HighScoreRepository {
  Future<int?> getBestScore(GameMode mode);
  Future<void> saveBestScore(GameMode mode, int score);
}
final highScoreRepositoryProvider = FutureProvider<HighScoreRepository>(...);
```

From lib/features/game/game_mode.dart:
```dart
enum GameMode { learn, statesMaster, geographicalMaster, grandMaster }
```

From 04-PATTERNS.md HomeScreen body structure (lines 890–937):
```dart
// HomeScreen extends ConsumerStatefulWidget
// ref.watch(highScoreRepositoryProvider) → handle loading/error/data
// _buildBody(BuildContext context, HighScoreRepository repo):
//   Column: header row + subtitle + Expanded ListView of 4 _ModeCard + privacy footer
```

From 04-PATTERNS.md _ModeCard (lines 942–1004):
```dart
class _ModeCard extends StatefulWidget {
  final GameMode mode;
  final String name;
  final String description;
  final IconData icon;
  final Color cardColor;
  final Future<int?> bestScoreFuture;
  final VoidCallback onTap;
}
// Press animation: scale 1.0 → 0.97, 80ms forward / 150ms reverse, Curves.easeInOut
// GestureDetector onTapDown/onTapUp/onTapCancel
// Gradient card: LinearGradient from cardColor → Color.lerp(cardColor, Colors.black, 0.2)
// FutureBuilder<int?> for stars + score display
```

From 04-PATTERNS.md _starsForScore (lines 954–961):
```dart
int _starsForScore(int? score) {
  if (score == null) return 0;
  if (score <= 80) return 3;
  if (score <= 150) return 2;
  return 1;
}
```

From 04-PATTERNS.md FutureBuilder score display (lines 978–1004):
```dart
FutureBuilder<int?>(
  future: widget.bestScoreFuture,
  builder: (ctx, snap) {
    final stars = snap.hasData ? _starsForScore(snap.data) : 0;
    // 3 star icons: filled amber / outline white alpha 0.4
    // "Best: N" or "Not played" text
  },
),
```

From 04-UI-SPEC.md Section 1 Home Screen + Section 2 Mode Card:
- Header: map icon (Icons.map, 28dp, #1565C0) + 'State the States' 28sp w700 #0D2E6B
- Subtitle: 'Choose a mode' 14sp grey
- Mode card heights: ~80dp determined by content (padding: all(16))
- BorderRadius.circular(16), gradient, boxShadow with modeColor alpha 0.4
- Card list vertical gap between cards: 12dp
- Privacy footer: TextButton 'Privacy Policy' centered

From 04-UI-SPEC.md copywriting (mode card names/descriptions/icons):
- Learn: name 'Learn', description 'Drag each state to the map. Abbreviations shown as hints.', icon Icons.explore, color #2E7D32
- States Master: name 'States Master', description 'Place all 50 states by name with no map labels.', icon Icons.flag, color #1565C0
- Geographical Master: name 'Geographical Master', description 'Abbreviations on the map, nothing in your hand.', icon Icons.compass_calibration, color #BF360C
- Grand Master: name 'Grand Master', description 'No labels, no names. Pure geography.', icon Icons.emoji_events, color #4A148C
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full HomeScreen implementation (replaces Phase 1 placeholder)</name>
  <files>lib/features/home/home_screen.dart</files>
  <read_first>
    - lib/features/home/home_screen.dart (current Phase 1 placeholder — understand what to replace)
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\home\home_screen.dart (authoritative port source — read full file, especially _ModeCard, _starsForScore, FutureBuilder pattern, press animation)
    - lib/core/data/high_score_repository.dart (highScoreRepositoryProvider, HighScoreRepository interface)
    - lib/features/game/game_mode.dart (GameMode enum)
    - .planning/phases/04-full-play-loop/04-PATTERNS.md (HomeScreen body structure lines 877–937; _ModeCard lines 942–1004)
    - .planning/phases/04-full-play-loop/04-UI-SPEC.md (Section 1 and Section 2 — dimensions, colors, typography, mode data table)
  </read_first>
  <action>
    Replace the full body of lib/features/home/home_screen.dart. Keep the class name HomeScreen extends ConsumerStatefulWidget and the existing `const HomeScreen({super.key})` constructor.

    Imports: flutter/material.dart; flutter_riverpod; go_router; game_mode.dart; high_score_repository.dart. Do NOT import ad services, url_launcher, or session-restore logic (those are Phase 5 HOME-03).

    _HomeScreenState.build: watch `ref.watch(highScoreRepositoryProvider)`, return Scaffold(backgroundColor: const Color(0xFFF0F4F8), body: SafeArea(child: repoAsync.when(loading: () => CircularProgressIndicator centered, error: ..., data: (repo) => _buildBody(context, repo)))).

    _buildBody returns Column(crossAxisAlignment: CrossAxisAlignment.start, children: [header row, subtitle, Expanded ListView, privacy footer]):

    Header row: Padding(fromLTRB(20, 20, 12, 4), child: Row([Icon(Icons.map, color: Color(0xFF1565C0), size: 28), SizedBox(width: 10), Expanded(child: Text('State the States', style: TextStyle(fontSize: 22, fontWeight: FontWeight.w800, color: Color(0xFF0D2E6B))))]))). Note: 22sp for Row (not 28sp — the 28sp is for the completion screen; home uses 22sp per patterns from Flags).

    Subtitle: Padding(left: 20, bottom: 12, child: Text('Choose a mode', style: TextStyle(fontSize: 14, color: Colors.grey))).

    ListView: Expanded(child: ListView(padding: EdgeInsets.symmetric(horizontal: 16), children: [_ModeCard for each mode with 12dp SizedBox gaps between them])). 4 cards, each receiving: mode, name, description, icon, cardColor (per UI-SPEC), bestScoreFuture: repo.getBestScore(mode), onTap: () => context.go('/play', extra: mode).

    Privacy footer: Padding(EdgeInsets.only(bottom: 8), child: Center(child: TextButton(onPressed: () {}, child: Text('Privacy Policy', style: TextStyle(fontSize: 12, color: Colors.grey))))).

    _ModeCard class is private StatefulWidget with SingleTickerProviderStateMixin. Constructor: mode, name, description, icon, cardColor, bestScoreFuture, onTap. State adds `late AnimationController _scaleController; late Animation<double> _scaleAnim`. initState creates _scaleController(duration: 80ms), _scaleAnim Tween<double>(begin:1.0, end:0.97) with Curves.easeInOut. dispose disposes _scaleController.

    _ModeCardState.build: GestureDetector(onTapDown: (_) => _scaleController.forward(), onTapUp: (_) { _scaleController.reverse(); widget.onTap(); }, onTapCancel: () => _scaleController.reverse(), child: AnimatedBuilder(animation: _scaleAnim, builder: (_, child) => Transform.scale(scale: _scaleAnim.value, child: child), child: Semantics(button: true, label: '${widget.name} mode', child: _buildCard()))).

    _buildCard returns Container(margin: EdgeInsets.only(bottom: 12), decoration: BoxDecoration(borderRadius: BorderRadius.circular(16), gradient: LinearGradient(colors: [widget.cardColor, Color.lerp(widget.cardColor, Colors.black, 0.2)!], begin: Alignment.topLeft, end: Alignment.bottomRight), boxShadow: [BoxShadow(color: widget.cardColor.withValues(alpha: 0.4), blurRadius: 12, offset: Offset(0, 4))]), child: Padding(padding: EdgeInsets.all(16), child: Row(children: [icon container, SizedBox(12), Expanded(name + desc column), SizedBox(8), FutureBuilder score + stars column]))).

    Icon container: Container(width: 52, height: 52, decoration: BoxDecoration(borderRadius: BorderRadius.circular(12), color: Colors.white.withValues(alpha: 0.2)), child: Icon(widget.icon, size: 28, color: Colors.white)).

    Name + desc column: Column(crossAxisAlignment: CrossAxisAlignment.start, children: [Text(name, style: TextStyle(fontSize: 17, fontWeight: FontWeight.w700, color: Colors.white)), SizedBox(height: 4), Text(description, style: TextStyle(fontSize: 12, color: Colors.white.withValues(alpha: 0.8)), maxLines: 2, overflow: TextOverflow.ellipsis)]).

    FutureBuilder score/stars column: FutureBuilder<int?>(future: widget.bestScoreFuture, builder: (ctx, snap) { final stars = snap.hasData ? _starsForScore(snap.data) : 0; return Column(mainAxisSize: MainAxisSize.min, crossAxisAlignment: CrossAxisAlignment.end, children: [Row(mainAxisSize: MainAxisSize.min, children: List.generate(3, (i) => Icon(i < stars ? Icons.star_rounded : Icons.star_outline_rounded, color: i < stars ? Colors.amber : Colors.white.withValues(alpha: 0.4), size: 18))), SizedBox(height: 4), snap.hasData && snap.data != null ? Text('Best: ${snap.data}', style: TextStyle(color: Colors.white.withValues(alpha: 0.8), fontSize: 11)) : Text('Not played', style: TextStyle(color: Colors.white.withValues(alpha: 0.5), fontSize: 11))]); }).

    _starsForScore private method: if score == null return 0; if score <= 80 return 3; if score <= 150 return 2; return 1.
  </action>
  <verify>
    <automated>flutter analyze lib/features/home/home_screen.dart</automated>
  </verify>
  <done>
    - lib/features/home/home_screen.dart contains 4 _ModeCard instances (one per GameMode)
    - Each card has correct name and description from UI-SPEC copywriting table
    - onTap calls context.go('/play', extra: mode) with the card's GameMode
    - FutureBuilder displays 'Not played' when getBestScore returns null
    - _starsForScore(null)==0, _starsForScore(80)==3, _starsForScore(150)==2, _starsForScore(151)==1
    - No ad imports, no session-restore logic
    - flutter analyze exits 0 on this file
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fill home_screen_test.dart with 4 mode cards and score display tests</name>
  <files>test/features/home/home_screen_test.dart</files>
  <read_first>
    - test/features/home/home_screen_test.dart (current stub from Plan 01 — has MockHighScoreRepository declared and one passing placeholder test; replace placeholder with real tests)
    - lib/features/home/home_screen.dart (just completed — understand rendered widget structure)
    - lib/core/data/high_score_repository.dart (HighScoreRepository interface, highScoreRepositoryProvider)
    - lib/features/game/game_mode.dart (GameMode enum values for per-mode verification)
    - .planning/phases/04-full-play-loop/04-PATTERNS.md (home_screen_test stub pattern lines 1063–1096 — ProviderScope override, MockHighScoreRepository usage)
    - test/features/map/map_screen_test.dart (double-pump pattern for FutureProvider)
  </read_first>
  <behavior>
    - HomeScreen renders 4 mode cards when highScoreRepositoryProvider resolves
    - Mode card texts 'Learn', 'States Master', 'Geographical Master', 'Grand Master' all found
    - When getBestScore returns null: 'Not played' text appears for that mode
    - When getBestScore returns 50: 'Best: 50' text appears
    - Stars: 3 filled Icons.star_rounded when score <= 80; verify by icon count assertion
    - HomeScreen renders without exception with loading state (CircularProgressIndicator)
  </behavior>
  <action>
    Replace the placeholder test body in test/features/home/home_screen_test.dart with full tests. Keep the `MockHighScoreRepository` class declaration and any existing `class Mock extends Mock` imports.

    Helper function `Widget buildHomeScreen(MockHighScoreRepository mockRepo)`: returns ProviderScope(overrides: [highScoreRepositoryProvider.overrideWith((_) async => mockRepo)], child: const MaterialApp(home: HomeScreen())).

    Test group 'HomeScreen mode cards':

    testWidgets 'shows 4 mode cards with correct names': stub getBestScore to return null for all. Pump buildHomeScreen(mockRepo). await tester.pump(); await tester.pump(). Assert find.text('Learn') findsAtLeastNWidgets(1). Assert find.text('States Master') findsAtLeastNWidgets(1). Assert find.text('Geographical Master') findsAtLeastNWidgets(1). Assert find.text('Grand Master') findsAtLeastNWidgets(1).

    testWidgets 'shows Not played when no score stored': stub getBestScore(any()) returns null. Pump. Double pump. Assert find.text('Not played') findsAtLeastNWidgets(1).

    testWidgets 'shows Best: N when score available': set up mockRepo so `getBestScore(GameMode.learn)` returns Future.value(50), all other modes return null. Pump. Double pump. Assert find.text('Best: 50') findsOneWidget. Assert find.text('Not played') findsAtLeastNWidgets(1) (for the other 3 modes).

    testWidgets 'renders without crashing in loading state': override highScoreRepositoryProvider with a Future that never completes (Completer<HighScoreRepository>().future). Pump HomeScreen. pump once. Assert find.byType(CircularProgressIndicator) findsOneWidget.

    For the first three testWidgets, ensure they use `tester.pumpAndSettle` or double pump to let the FutureProvider emit data before assertions.
  </action>
  <verify>
    <automated>flutter test test/features/home/home_screen_test.dart</automated>
  </verify>
  <done>
    - 4 widget tests in home_screen_test.dart all pass
    - All 4 mode names found in rendered widget tree
    - 'Not played' text appears when scores are null
    - 'Best: 50' appears when Learn score is 50
    - Loading state shows CircularProgressIndicator
    - flutter test test/features/home/home_screen_test.dart exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /play route extra | GameMode passed as extra from HomeScreen tap — app-internal, trusted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | Best score display from SharedPreferences | accept | Score is local; no PII; offline app |
| T-04-10 | Tampering | HighScoreRepository read in _buildBody | accept | Reads only — write happens in GameSessionNotifier.completeGame() which already has lower-wins guard |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
flutter test test/features/home/home_screen_test.dart
</verification>

<success_criteria>
- HomeScreen shows exactly 4 mode cards with names Learn / States Master / Geographical Master / Grand Master
- Each card taps to /play with correct GameMode as extra (context.go('/play', extra: mode))
- FutureBuilder shows 'Not played' (null) and 'Best: N' (non-null) states correctly
- _starsForScore thresholds: <=80 = 3 stars, <=150 = 2 stars, >150 = 1 star
- 4 home_screen_test.dart tests pass
- Full test suite (flutter test) passes with no regressions
</success_criteria>

<output>
Create `.planning/phases/04-full-play-loop/04-06-SUMMARY.md` when done
</output>
