---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate_states.py
  - scripts/requirements.txt
  - scripts/test_pipeline.py
  - assets/map/usa_states_paths.json
autonomous: true
requirements: [DATA-01, DATA-02]
user_setup: []

must_haves:
  truths:
    - "Running python scripts/generate_states.py produces assets/map/usa_states_paths.json"
    - "The JSON contains 51 records: 50 isPlaceable:true states + 1 isPlaceable:false DC (D-03: DC rendered as non-placeable filler)"
    - "Alaska geometry passes shapely.validation.is_valid() after antimeridian split + reprojection"
    - "Alaska and Hawaii path coordinates are pre-transformed into lower-left inset canvas space, not their geographic latitudes (D-02: per-landmass conic projection before inset; D-08: AK ~0.45x lower-left ocean overlay)"
    - "The viewBox is derived from CONUS Albers bounds, normalized to width 1000 (D-01: Albers equal-area conic for CONUS; D-07: derived viewBox, not hardcoded)"
  artifacts:
    - path: "scripts/generate_states.py"
      provides: "Build-time Albers + antimeridian + inset-baking pipeline"
      contains: "EPSG:5070"
    - path: "scripts/test_pipeline.py"
      provides: "Pipeline output validation (state count, AK validity, inset positions)"
      contains: "test_state_count"
    - path: "assets/map/usa_states_paths.json"
      provides: "Bundled pre-transformed state path data"
      contains: "\"states\""
    - path: "scripts/requirements.txt"
      provides: "Python deps incl antimeridian"
      contains: "antimeridian"
  key_links:
    - from: "generate_states.py"
      to: "antimeridian.fix_shape"
      via: "Alaska geometry fixed before to_crs"
      pattern: "fix_shape"
    - from: "generate_states.py"
      to: "usa_states_paths.json"
      via: "json.dump with 'states' key"
      pattern: "\"states\""
---

<objective>
Port Flags' `generate_map.py` into a build-time `scripts/generate_states.py` that converts Natural Earth admin-1 (public domain) into the bundled `assets/map/usa_states_paths.json`, replacing the equirectangular projection with the three-CRS Albers pipeline (CONUS EPSG:5070, Alaska EPSG:3338 after antimeridian split, Hawaii proj4), baking AK/HI inset transforms into final canvas coordinates, and emitting per-state centroids and bounding boxes for 51 records (50 placeable + non-placeable DC). Ship `scripts/test_pipeline.py` (Wave 0 test stub) validating the output.

Purpose: `usa_states_paths.json` is the single data prerequisite every later phase depends on. DATA-01 (pipeline + data) and DATA-02 (Alaska antimeridian split + shapely validity) land here. This plan is pure Python and the JSON asset — zero overlap with the Flutter scaffold (plan 01-01), so it runs in parallel.
Output: A runnable pipeline, its test suite, and the committed `usa_states_paths.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the three-CRS Albers pipeline with antimeridian split and inset baking</name>
  <files>scripts/generate_states.py, scripts/requirements.txt, assets/map/usa_states_paths.json</files>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\scripts\generate_map.py (the structural baseline: file layout, polygon_to_path, MultiPolygon iteration, representative_point centroid, output record, json.dump)
    - C:\code\Claude\FlagsRoundTheWorld\scripts\requirements.txt (Python deps baseline)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 1: Three-CRS Albers Pipeline", §"Pattern 2: Antimeridian Split", §"Pattern 3: Inset Baking", §"Pattern 6: StateData JSON Schema", §"Code Examples → Complete Pipeline Skeleton", §"Natural Earth Shapefile Field Verification"
    - .planning/phases/01-foundation/01-PATTERNS.md §"generate_states.py" (the full delta table from analog)
    - .planning/phases/01-foundation/01-CONTEXT.md D-01, D-02, D-03, D-04, D-07, D-08
  </read_first>
  <action>
    Write `scripts/requirements.txt` as `geopandas>=1.0`, `shapely>=2.0`, `pyproj>=3.6.0`, `antimeridian>=0.4` (adds antimeridian to the Flags baseline). Add `pytest` if used by the test task; otherwise note the unittest std-lib fallback.
    Write `scripts/generate_states.py` porting the Flags structure. Constants: `NE_URL = 'https://naciscdn.org/naturalearth/10m/cultural/ne_10m_admin_1_states_provinces.zip'`; `FIFTY_STATES` set of the 50 USPS codes; `ALL_RECORDS = FIFTY_STATES | {'DC'}`; `HI_PROJ4 = '+proj=aea +lat_0=13 +lon_0=-157 +lat_1=8 +lat_2=18 +x_0=0 +y_0=0 +datum=NAD83 +units=m +no_defs'`; `CANVAS_WIDTH = 1000`; Alaska inset `SCALE_FACTOR = 0.45` (D-08).
    First-run field verification (MANDATORY, field names MEDIUM confidence): read shapefile to EPSG:4326, print `gdf.columns.tolist()`, filter `adm0_a3 == 'USA'` (fall back to `ADM0_A3` if the lowercase column is absent), then filter `postal in ALL_RECORDS`, print USA row count and a postal sample, and assert the filtered placeable set has exactly 50 members + DC before proceeding.
    Three branches: (a) CONUS+DC: `to_crs('EPSG:5070')`, derive `normalize_bounds` (height = round(1000 * proj_h/proj_w), y flipped for SVG coords), which fixes the derived viewBox (D-07). (b) Alaska: apply `antimeridian.fix_shape()` to each AK geometry BEFORE `to_crs('EPSG:3338')` (D-02/DATA-02), then `assert shapely.validation.is_valid(geom)` for each reprojected geometry, normalize to its own bounds, then bake the inset scale(~0.45×)+translate into a lower-left target rect so output coords are final canvas space (D-08). (c) Hawaii: `to_crs(HI_PROJ4)`, normalize, bake inset scale+translate to a frame just right of Alaska.
    Emit per-record `{postal, name, paths[], boundingBox{x,y,w,h}, centroid{x,y}, isPlaceable, insetGroup}` where DC has `isPlaceable:false` (D-03) and AK/HI have `insetGroup:"alaska"|"hawaii"`. Centroid uses `representative_point()` on the largest polygon (Flags pattern), in canvas space. Top-level output: `{version:1, viewBox:{width:1000, height:<derived>}, insetFrames:{alaska:{x,y,w,h}, hawaii:{x,y,w,h}}, states:[...]}` — JSON key is `"states"` NOT `"countries"` (Pitfall 7). Write to `assets/map/usa_states_paths.json` via `json.dump(..., separators=(',',':'))`. Optionally emit a standalone PNG render for the manual AK/HI visual check (Success Criterion #2 / VALIDATION manual check).
  </action>
  <verify>
    <automated>python scripts/generate_states.py succeeds AND python -c "import json; d=json.load(open('assets/map/usa_states_paths.json')); s=d['states']; assert len(s)==51; assert sum(1 for x in s if x['isPlaceable'])==50; assert d['viewBox']['width']==1000; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/requirements.txt` contains `antimeridian>=0.4`
    - Running `python scripts/generate_states.py` exits 0 and writes `assets/map/usa_states_paths.json`
    - JSON top-level key is `states` (not `countries`); `usa_states_paths.json` contains the substring `"states"`
    - `json.load(...)['states']` has exactly 51 records; exactly 50 have `isPlaceable:true`; exactly 1 (DC) has `isPlaceable:false`
    - `d['viewBox']['width'] == 1000` and `d['viewBox']['height']` is a derived value in ~620-625 range (D-07)
    - The AK record has `insetGroup == "alaska"` and the HI record has `insetGroup == "hawaii"`
    - `generate_states.py` calls `antimeridian.fix_shape` before `to_crs('EPSG:3338')` for Alaska (source assertion)
    - `generate_states.py` contains `EPSG:5070` (CONUS), `EPSG:3338` (Alaska), and the `HI_PROJ4` aea string (Hawaii)
  </acceptance_criteria>
  <done>The pipeline produces a valid 51-record JSON with derived viewBox, three-CRS reprojection, and baked AK/HI inset coordinates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write scripts/test_pipeline.py validating state count, Alaska validity, and inset positions</name>
  <files>scripts/test_pipeline.py</files>
  <behavior>
    - test_state_count: loads usa_states_paths.json; asserts 51 total records, exactly 50 isPlaceable:true, exactly 1 (DC) isPlaceable:false
    - test_alaska_validity: re-runs the Alaska branch geometry (or asserts the pipeline's own validity gate ran) — Alaska geometry passes shapely.validation.is_valid() after fix_shape + to_crs('EPSG:3338')
    - test_inset_positions: Alaska centroid x/y falls inside the alaska insetFrame rect (lower-left); Hawaii centroid falls inside the hawaii insetFrame rect — proving inset transforms were baked, not geographic coords
    - test_no_dc_placeable: the record with postal 'DC' has isPlaceable false and is excluded from any placeable iteration
    - test_viewbox_derived: viewBox width is exactly 1000 and height is in (600, 640) — derived from CONUS bounds
  </behavior>
  <read_first>
    - scripts/generate_states.py (the pipeline under test — read its emitted schema and the insetFrames structure)
    - .planning/phases/01-foundation/01-VALIDATION.md §"Wave 0 Requirements" + §"Per-Task Verification Map" (the exact behaviors to assert)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Validation Architecture → Phase Requirements → Test Map" (DATA-01/DATA-02 Python rows)
  </read_first>
  <action>
    Create `scripts/test_pipeline.py` using `pytest` (or `unittest` std-lib if pytest is not installed — match the requirements.txt choice from Task 1). Implement the five tests in the behavior block. `test_state_count` and `test_inset_positions` load `assets/map/usa_states_paths.json` and assert against the schema. `test_alaska_validity` imports the pipeline's antimeridian-fix + reproject helper(s) from `generate_states.py` and asserts `shapely.validation.is_valid()` is True on the fixed+reprojected Alaska geometry (DATA-02). Read the alaska/hawaii rects from the JSON `insetFrames` and assert each landmass centroid lies inside its rect. Make tests importable both as `python -m pytest scripts/test_pipeline.py` and via the named selectors `::test_state_count` / `::test_alaska_validity` referenced in VALIDATION.md.
  </action>
  <verify>
    <automated>python -m pytest scripts/test_pipeline.py -x exits 0 (or python -m unittest scripts.test_pipeline if unittest fallback chosen)</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/test_pipeline.py` defines `test_state_count`, `test_alaska_validity`, `test_inset_positions`, `test_no_dc_placeable`, `test_viewbox_derived`
    - `python -m pytest scripts/test_pipeline.py -x` exits 0 with all tests passing
    - `python -m pytest scripts/test_pipeline.py::test_state_count` selects and passes the count test
    - `python -m pytest scripts/test_pipeline.py::test_alaska_validity` asserts `shapely.validation.is_valid()` True on the fixed+reprojected Alaska geometry
    - `test_inset_positions` asserts the Alaska centroid is inside the `insetFrames['alaska']` rect and Hawaii centroid inside `insetFrames['hawaii']`
  </acceptance_criteria>
  <done>The pipeline test suite is green, proving 50 placeable states, Alaska shapely validity, and baked inset positions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time pipeline → bundled JSON asset | a malformed/oversized JSON crashes the app at load (Dart StateDataService consumes it in plan 01-03) |
| external shapefile download → pipeline | Natural Earth field-name drift (adm0_a3 vs ADM0_A3) could silently produce a wrong record set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (data integrity) | generate_states.py emitting malformed/incomplete JSON that crashes the Dart loader | mitigate | test_pipeline.py asserts 51 records / valid structure; Dart service test in 01-03 round-trips the same file |
| T-02-02 | Information Disclosure (wrong data) | Natural Earth field-name case mismatch silently selecting wrong rows | mitigate | First-run field verification prints columns and asserts exactly 50 placeable + DC before emitting |
| T-02-03 | Denial of Service (render) | Antimeridian smear producing a degenerate full-width Alaska polygon | mitigate (HIGH — core data correctness) | fix_shape() before reproject + `is_valid()` assertion gate; test_alaska_validity enforces it |
| T-02-SC | Tampering | pip installs (geopandas, shapely, pyproj, antimeridian) | accept | All 4 packages [OK] in RESEARCH.md Package Legitimacy Audit (slopcheck 0.6.1); no [ASSUMED]/[SUS]/[SLOP] packages |
</threat_model>

<verification>
- `python scripts/generate_states.py` produces the JSON with 51 records / 50 placeable
- `python -m pytest scripts/test_pipeline.py` is green (count, AK validity, inset positions)
- Manual: inspect the optional PNG render — AK bottom-left, HI bottom-center, no antimeridian smear (Success Criterion #2)
</verification>

<success_criteria>
- DATA-01: pipeline converts NE admin-1 into bundled JSON with path data, centroids, AK/HI inset transforms
- DATA-02: Alaska antimeridian split + shapely validity; no horizontal smear
- ROADMAP Success Criterion #1: 50 state records, Alaska is_valid(), renders without smear
- ROADMAP Success Criterion #2: AK/HI pre-transformed into inset canvas space
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
