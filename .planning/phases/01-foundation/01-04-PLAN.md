---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-03"]
files_modified:
  - lib/core/ads/ad_service.dart
  - lib/core/ads/stub_ad_service.dart
  - lib/core/ads/ad_service_provider.dart
  - lib/core/ads/ad_load_state.dart
  - lib/core/ads/ad_constants.dart
  - lib/core/audio/audio_service.dart
  - lib/core/audio/stub_audio_service.dart
  - lib/core/audio/real_audio_service.dart
  - lib/core/audio/audio_service_provider.dart
  - lib/main.dart
  - lib/app.dart
  - lib/features/home/home_screen.dart
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03, COMP-04, SESS-05]
user_setup: []

must_haves:
  truths:
    - "The ad layer exists but is stubbed: adServiceProvider returns StubAdService (a walled garden)"
    - "No file under lib/features/ imports from lib/core/ads/ (zero reachable ad imports)"
    - "The Flutter app builds and runs: main() initializes COPPA config, app.dart routes home and play"
    - "aapt dump badging on the built debug APK shows the AD_ID permission is absent"
    - "The audio service interface exposes playAnthem/stopAnthem backed by the placeholder asset"
  artifacts:
    - path: "lib/core/ads/stub_ad_service.dart"
      provides: "No-op walled-garden ad implementation"
      contains: "SizedBox.shrink"
    - path: "lib/core/ads/ad_service_provider.dart"
      provides: "Provider wiring StubAdService (not AdMob)"
      contains: "StubAdService"
    - path: "lib/main.dart"
      provides: "App entrypoint: COPPA init + ProviderScope + RealAudioService override"
      contains: "initializeAds"
    - path: "lib/app.dart"
      provides: "GoRouter (/ home, /play map) + MaterialApp.router"
      contains: "GoRouter"
    - path: "lib/core/audio/audio_service.dart"
      provides: "AudioService interface incl playAnthem/stopAnthem"
      contains: "playAnthem"
  key_links:
    - from: "ad_service_provider.dart"
      to: "StubAdService"
      via: "Provider<AdService> returns const StubAdService()"
      pattern: "const StubAdService\\(\\)"
    - from: "main.dart"
      to: "initializeAds()"
      via: "awaited before runApp"
      pattern: "initializeAds\\(\\)"
    - from: "app.dart"
      to: "MapScreen / HomeScreen"
      via: "GoRouter routes / and /play"
      pattern: "MapScreen"
---

<objective>
Wire the COPPA walled-garden ad layer (`AdService` interface + `StubAdService` + `Provider` returning the stub) and the audio service layer (interface with `playAnthem`/`stopAnthem`, stub, and `RealAudioService` over the placeholder WAV), then assemble the runnable app: `main.dart` (COPPA init + `ProviderScope` with `RealAudioService` override), `app.dart` (`GoRouter` `/`→Home, `/play`→Map), and the placeholder `HomeScreen`. Finish with the blocking COPPA build verification proving no AD_ID permission and zero reachable ad imports.

Purpose: Closes COMP-03 (walled garden — `GameSessionNotifier`/features have zero ad imports) and proves ROADMAP Success Criterion #3 (`google_mobile_ads` declared but `StubAdService` wired; `aapt dump badging` shows AD_ID absent). Depends on plan 01-01 (ads_initializer/manifest/pubspec) and plan 01-03 (`MapScreen` for the `/play` route).
Output: A buildable, runnable, COPPA-verified app shell with the stub/real service split in place.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port the ads walled garden — interface, stub, provider (wire Stub not AdMob), enums, constants</name>
  <files>lib/core/ads/ad_service.dart, lib/core/ads/stub_ad_service.dart, lib/core/ads/ad_service_provider.dart, lib/core/ads/ad_load_state.dart, lib/core/ads/ad_constants.dart</files>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ads\ad_service.dart (interface — copy verbatim)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ads\stub_ad_service.dart (no-op impl — copy verbatim)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ads\ad_load_state.dart (sealed class — copy verbatim)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ads\ad_constants.dart (structure — clear all unit IDs)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\ads\ad_service_provider.dart (DO NOT copy verbatim — it wires AdMobAdService; replace with StubAdService)
    - .planning/phases/01-foundation/01-PATTERNS.md §"lib/core/ads/*" (each file's exact delta)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Pattern 4: Flags Ad Stub Wired Garden"
  </read_first>
  <action>
    Copy verbatim into `lib/core/ads/`: `ad_service.dart` (the abstract `interface class AdService` with `getBannerWidget`, `showInterstitialAd`, `showRewardedAd`, `showAppOpenAd`), `stub_ad_service.dart` (`StubAdService implements AdService` returning `const SizedBox.shrink()` and `false`), `ad_load_state.dart` (the `sealed class AdLoadState` + `AdLoaded`/`AdFailed`). Copy `ad_constants.dart` structure but set every unit-ID string to `''` (empty) and `kAppLovinEnabled = false` (per PATTERNS). Create `ad_service_provider.dart` as the Phase-1 REPLACEMENT (NOT the Flags verbatim): `final adServiceProvider = Provider<AdService>((ref) => const StubAdService());` — it must import only `ad_service.dart` + `stub_ad_service.dart`, never `admob_ad_service.dart`, and never call `preloadAll()`. This is the compile-time walled garden (COMP-03): the real AdMob service is never wired in v1.
  </action>
  <verify>
    <automated>grep -c "const StubAdService()" lib/core/ads/ad_service_provider.dart returns 1 AND grep "admob_ad_service" lib/core/ads/ad_service_provider.dart returns empty AND flutter analyze lib/core/ads/ reports no errors</automated>
  </verify>
  <acceptance_criteria>
    - `ad_service.dart` declares `abstract interface class AdService` with the four methods
    - `stub_ad_service.dart` `StubAdService` returns `const SizedBox.shrink()` from `getBannerWidget` and `false` from `showRewardedAd`
    - `ad_service_provider.dart` returns `const StubAdService()` and imports neither `admob_ad_service.dart` nor `google_mobile_ads`
    - `grep preloadAll lib/core/ads/ad_service_provider.dart` returns empty
    - `ad_constants.dart` has all unit-ID strings empty (`''`)
    - `flutter analyze lib/core/ads/` reports zero errors
  </acceptance_criteria>
  <done>The ad layer compiles as a stub-only walled garden with no path to the real AdMob service.</done>
</task>

<task type="auto">
  <name>Task 2: Port the audio service layer — interface + stub + real (with playAnthem/stopAnthem) + provider</name>
  <files>lib/core/audio/audio_service.dart, lib/core/audio/stub_audio_service.dart, lib/core/audio/real_audio_service.dart, lib/core/audio/audio_service_provider.dart</files>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\audio\audio_service.dart (interface base — extend with anthem methods)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\audio\stub_audio_service.dart (no-op impl — add anthem stubs)
    - C:\code\Claude\FlagsRoundTheWorld\lib\core\audio\real_audio_service.dart (just_audio init/play/setMuted/dispose pattern — add anthem player)
    - .planning/phases/01-foundation/01-PATTERNS.md §"lib/core/audio/*" (the anthem-method additions + _anthemPlayer setup)
    - .planning/phases/01-foundation/01-RESEARCH.md §"Standard Stack → just_audio"
  </read_first>
  <action>
    Create `lib/core/audio/audio_service.dart`: the `abstract interface class AudioService` from Flags plus two new methods `Future<void> playAnthem();` and `Future<void> stopAnthem();` (alongside `init`, `playCorrect`, `playError`, `setMuted`, `dispose`). Create `stub_audio_service.dart`: `StubAudioService implements AudioService` with all methods as no-op `async {}`, including the two anthem stubs. Create `real_audio_service.dart` mirroring the Flags `just_audio` pattern: `_correctPlayer`/`_errorPlayer` set from `assets/audio/correct.wav`/`error.wav`, plus a new `_anthemPlayer` set from `assets/audio/anthem_placeholder.wav` with `setLoopMode(LoopMode.one)` in `init()`; `playAnthem()` plays the anthem player, `stopAnthem()` stops it; preserve the try/catch + `_initialized` guard + `dispose()` that disposes all three players (no leaked players). Create `audio_service_provider.dart`: a `Provider<AudioService>` that defaults to `const StubAudioService()` (overridden to `RealAudioService` in `main.dart`, the Flags ProviderScope-override pattern). The real anthem render is Phase 5 (D-05) — Phase 1 only needs the placeholder loop wired so the interface and asset path are proven.
  </action>
  <verify>
    <automated>grep -c "playAnthem" lib/core/audio/audio_service.dart returns 1 AND grep -c "anthem_placeholder.wav" lib/core/audio/real_audio_service.dart returns 1 AND flutter analyze lib/core/audio/ reports no errors</automated>
  </verify>
  <acceptance_criteria>
    - `audio_service.dart` interface declares `playAnthem()` and `stopAnthem()` in addition to init/playCorrect/playError/setMuted/dispose
    - `stub_audio_service.dart` implements all seven methods as no-ops
    - `real_audio_service.dart` sets `_anthemPlayer` from `assets/audio/anthem_placeholder.wav` with `LoopMode.one` and disposes all three players in `dispose()`
    - `audio_service_provider.dart` declares a `Provider<AudioService>` defaulting to `StubAudioService`
    - `flutter analyze lib/core/audio/` reports zero errors
  </acceptance_criteria>
  <done>The audio layer compiles with the stub/real split and anthem methods backed by the placeholder asset.</done>
</task>

<task type="auto">
  <name>Task 3: Assemble main.dart, app.dart, HomeScreen — then verify the COPPA walled garden and AD_ID-free build</name>
  <files>lib/main.dart, lib/app.dart, lib/features/home/home_screen.dart</files>
  <read_first>
    - C:\code\Claude\FlagsRoundTheWorld\lib\main.dart (initializeAds + ProviderScope + audio override pattern)
    - C:\code\Claude\FlagsRoundTheWorld\lib\app.dart (GoRouter + MaterialApp.router; remove AppStateObserver + gameSessionProvider)
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\home\home_screen.dart (ConsumerStatefulWidget scaffold pattern)
    - lib/core/ads/ads_initializer.dart (the COPPA init from plan 01-01)
    - lib/features/map/map_screen.dart (the /play target from plan 01-03)
    - lib/core/audio/audio_service_provider.dart + real_audio_service.dart (Task 2 outputs for the override)
    - .planning/phases/01-foundation/01-PATTERNS.md §"lib/main.dart", §"lib/app.dart", §"lib/features/home/home_screen.dart", §"Shared Patterns → ProviderScope Override for Audio"
    - .planning/phases/01-foundation/01-VALIDATION.md §"Manual-Only Verifications" (COMP-02/03/04 commands)
  </read_first>
  <action>
    Create `lib/main.dart`: `WidgetsFlutterBinding.ensureInitialized()`, `await initializeAds()` (COPPA config from plan 01-01), then `runApp(ProviderScope(overrides: [audioServiceProvider.overrideWith((_) { final svc = RealAudioService(); unawaited(svc.init()); return svc; })], child: const App()))`. Create `lib/app.dart`: a `GoRouter` with `initialLocation: '/'`, routes `/`→`HomeScreen` and `/play`→`MapScreen`; an `App` widget returning `MaterialApp.router(title: 'State the States', debugShowCheckedModeBanner: false, routerConfig: _router)` with localization delegates if l10n is generated (else omit). Remove the Flags `AppStateObserver` and any `gameSessionProvider` reference (Phase 2+). Create `lib/features/home/home_screen.dart`: a placeholder `ConsumerStatefulWidget` `HomeScreen` — a `Scaffold` with the app title and a button that navigates to `/play` (the full mode-card grid is Phase 4). CRITICAL: no file under `lib/features/` may import anything from `lib/core/ads/` (COMP-03 walled garden); HomeScreen imports only routing + audio provider if needed, never the ad layer.
    Run `flutter build apk --debug` (COMP-04 build). Then run the COPPA verifications: `aapt dump badging build/app/outputs/flutter-apk/app-debug.apk | grep AD_ID` MUST return empty (COMP-02), and `grep -r "core/ads" lib/features/` MUST return empty (COMP-03 walled garden). If `aapt` is unavailable, fall back to `adb shell pm dump` / unzip the APK's `AndroidManifest` and confirm AD_ID absent.
  </action>
  <verify>
    <automated>flutter build apk --debug succeeds AND aapt dump badging build/app/outputs/flutter-apk/app-debug.apk | grep AD_ID returns empty AND grep -r "core/ads" lib/features/ returns empty AND grep firebase pubspec.lock returns empty</automated>
  </verify>
  <acceptance_criteria>
    - `main.dart` awaits `initializeAds()` before `runApp` and overrides `audioServiceProvider` with `RealAudioService`
    - `app.dart` declares a `GoRouter` routing `/`→HomeScreen and `/play`→MapScreen and uses `MaterialApp.router`
    - `grep -r "core/ads" lib/features/` returns empty (COMP-03 — zero reachable ad imports from features)
    - `flutter build apk --debug` exits 0 (COMP-04)
    - `aapt dump badging build/app/outputs/flutter-apk/app-debug.apk | grep AD_ID` returns empty (COMP-02)
    - `grep firebase pubspec.lock` returns empty (COMP-01 re-confirmed at build)
    - The built manifest package is `com.otis.brooke.state.the.state`
  </acceptance_criteria>
  <done>The app builds and runs with StubAdService wired, no AD_ID permission, no Firebase, and zero ad imports reachable from features.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app feature code → ad SDK | any reachable import from features to core/ads opens a COPPA-bypass path to the real ad SDK |
| app → Google Play services | google_mobile_ads transitive AD_ID permission must remain stripped in the final APK |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege / COPPA-bypass | A feature file importing core/ads reaches the real ad surface | mitigate (HIGH — COPPA-bypass) | Walled-garden provider returns StubAdService only; `grep -r "core/ads" lib/features/` empty gate in Task 3 |
| T-04-02 | Information Disclosure | AD_ID permission surviving the manifest merge into the APK | mitigate (HIGH — COPPA-bypass) | `tools:node="remove"` (plan 01-01) verified here via `aapt dump badging \| grep AD_ID` empty on the actual build |
| T-04-03 | Tampering / Mis-init | RealAudioService leaking players on dispose | mitigate | dispose() disposes all three players; init guards with try/catch + _initialized flag (Flags pattern) |
| T-04-04 | Information Disclosure | Firebase re-introduced transitively | mitigate (HIGH — COPPA-bypass) | `grep firebase pubspec.lock` empty re-checked at build time in Task 3 |
| T-04-SC | Tampering | google_mobile_ads compiled into the build | accept | google.dev-published, Flags-vetted; only the stub is wired, no real ad requests issued |
</threat_model>

<verification>
- `flutter build apk --debug` succeeds
- `aapt dump badging <apk> | grep AD_ID` empty (COMP-02)
- `grep -r "core/ads" lib/features/` empty (COMP-03)
- `grep firebase pubspec.lock` empty (COMP-01)
- adServiceProvider returns StubAdService; no preloadAll
</verification>

<success_criteria>
- COMP-01: no Firebase (re-confirmed at build)
- COMP-02: AD_ID absent from built APK
- COMP-03: ad layer stubbed; zero reachable ad imports from features
- COMP-04: app builds under App ID com.otis.brooke.state.the.state, G/PG configured
- SESS-05: bundled assets, offline build succeeds
- ROADMAP Success Criterion #3: google_mobile_ads declared but StubAdService wired; AD_ID absent
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
