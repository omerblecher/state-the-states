---
phase: "05-polish-welcome-accessibility"
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - assets/audio/anthem.wav
  - scripts/anthem/render_anthem.ps1
  - LICENSES
autonomous: false
requirements:
  - WEL-02

must_haves:
  truths:
    - "assets/audio/anthem.wav exists and is a non-silent WAV file (playable)"
    - "LICENSES file documents anthem provenance with source, rendering tool, and soundfont"
    - "The rendered WAV is audibly recognizable as Star-Spangled Banner instrumental"
  artifacts:
    - path: "assets/audio/anthem.wav"
      provides: "Rights-clean Star-Spangled Banner WAV for welcome screen"
      min_lines: 1
    - path: "scripts/anthem/render_anthem.ps1"
      provides: "Reproducible rendering script"
      contains: "fluidsynth"
    - path: "LICENSES"
      provides: "Anthem provenance documentation"
      contains: "anthem.wav"
  key_links:
    - from: "scripts/anthem/render_anthem.ps1"
      to: "assets/audio/anthem.wav"
      via: "FluidSynth CLI -F flag"
      pattern: "-F.*anthem"
---

<objective>
Render a rights-clean "Star-Spangled Banner" instrumental WAV using FluidSynth + a public-domain MIDI source + GeneralUser GS soundfont (or MuseScore General SF2 fallback). Replace `assets/audio/anthem_placeholder.wav` with `assets/audio/anthem.wav`.

Purpose: Satisfies WEL-02 ("self-rendered, rights-clean instrumental"). The anthem asset must exist before the welcome screen can be wired in Wave 1.

Output: `assets/audio/anthem.wav`, `scripts/anthem/render_anthem.ps1`, updated `LICENSES` entry.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Install FluidSynth and download source assets</name>
  <files>scripts/anthem/render_anthem.ps1</files>
  <read_first>
    - .planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md (§Pattern 2: Anthem Rendering Pipeline — exact commands, flags, URLs)
  </read_first>
  <action>
    This task requires manual steps because FluidSynth is not installed and the MIDI/SF2 files are not yet downloaded.

    Step 1 — Install FluidSynth (choose one option):
      Option A (Chocolatey): choco install fluidsynth
      Option B (direct binary): Download fluidsynth-2.5.4-win64.zip from https://github.com/FluidSynth/fluidsynth/releases/tag/v2.5.4, extract, add the bin/ directory to PATH.
    Verify: run `fluidsynth --version` in PowerShell. Must print "FluidSynth version 2.x.x".

    Step 2 — Create scripts/anthem/ directory and download source files:
      - Star-Spangled Banner MIDI: https://en.wikipedia.org/wiki/File:2_Star_Spangled_Banner.mid — save as scripts/anthem/star_spangled_banner.mid
      - GeneralUser GS SF2: https://schristiancollins.com/generaluser.php — save as scripts/anthem/GeneralUser_GS.sf2
        (Fallback: MuseScore General SF2 from https://ftp.osuosl.org/pub/musescore/soundfont/MuseScore_General/ — MIT license, requires attribution in LICENSES)

    Step 3 — Create scripts/anthem/render_anthem.ps1 with the following content (do not include fenced blocks; this is the literal file):
      The script must: (a) invoke fluidsynth with flags -ni -F scripts/anthem/anthem_rendered.wav -r 44100 -g 0.8, passing the SF2 path and MIDI path as positional args; (b) copy the rendered WAV to assets/audio/anthem.wav; (c) print a success message.

    Step 4 — Run the script:
      pwsh scripts/anthem/render_anthem.ps1
      Expected: scripts/anthem/anthem_rendered.wav is created (16 MB approx), then copied to assets/audio/anthem.wav.
      Verify audibility: play assets/audio/anthem.wav on desktop before proceeding. Must be recognizable SSB at adequate volume. If too quiet, re-render with -g 1.0. If it clips, use -g 0.7.

    IMPORTANT — Pitfall 2 (RESEARCH.md): The default FluidSynth gain (-g 0.2) is inaudible on mobile. The -g 0.8 flag is mandatory.
  </action>
  <verify>
    <automated>MISSING — anthem rendering is a human-only off-device task; no automated command can download/install FluidSynth or render MIDI. After completing the steps above, run: `flutter test test/core/audio/audio_service_test.dart` to confirm the test suite still passes (no regression from asset rename).</automated>
  </verify>
  <acceptance_criteria>
    - `fluidsynth --version` prints version 2.x.x without error
    - `assets/audio/anthem.wav` exists and has file size greater than 1 MB
    - Playing the WAV on desktop produces an audible Star-Spangled Banner instrumental
    - `scripts/anthem/render_anthem.ps1` exists and contains the fluidsynth -F flag and -g 0.8 gain
  </acceptance_criteria>
  <done>FluidSynth installed, anthem.wav rendered and placed at assets/audio/anthem.wav, audibility verified by ear on desktop.</done>
</task>

<task type="auto">
  <name>Task 2: Update LICENSES file and remove anthem_placeholder.wav reference</name>
  <files>LICENSES</files>
  <read_first>
    - LICENSES (read current content; the anthem provenance entry may already have a placeholder)
    - .planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md (§Pattern 2 — exact LICENSES.md entry text)
    - lib/core/audio/real_audio_service.dart (line 23 — setAsset call with 'anthem_placeholder.wav')
  </read_first>
  <action>
    Update LICENSES to add (or replace the placeholder entry for) anthem provenance. The entry must include:
      - "anthem.wav — Star-Spangled Banner (Francis Scott Key, 1814, public domain composition)."
      - "MIDI source: Wikimedia Commons, 2_Star_Spangled_Banner.mid by Hyacinth (2012), released under Creative Commons Public Domain Mark 1.0."
      - "Rendered using FluidSynth 2.5.4 with GeneralUser GS soundfont by S. Christian Collins (free for commercial use; see https://schristiancollins.com/generaluser.php)."
      (If MuseScore General SF2 was used instead, use: "Rendered using FluidSynth 2.5.4 with MuseScore General soundfont (MIT license — see https://ftp.osuosl.org/pub/musescore/soundfont/MuseScore_General/MuseScore_General_License.md).")

    Do NOT modify real_audio_service.dart in this plan — the setAsset path update ('anthem_placeholder.wav' → 'anthem.wav') is deferred to Plan 02 (audio service refactor) as a 3-file atomic change per the cross-file invariant in 05-PATTERNS.md.

    The anthem_placeholder.wav file can remain in assets/audio/ alongside anthem.wav for now; it will be removed after Wave 1 confirms the real anthem loads without error. Do not delete it in this plan.
  </action>
  <verify>
    <automated>grep -c "anthem.wav" LICENSES</automated>
  </verify>
  <acceptance_criteria>
    - LICENSES contains the string "anthem.wav" with provenance attribution (composition, MIDI source, rendering tool, soundfont)
    - LICENSES contains "FluidSynth" and the soundfont name used
    - LICENSES contains either "Creative Commons Public Domain Mark" (GeneralUser GS path) or "MIT license" (MuseScore path)
    - real_audio_service.dart is NOT modified in this plan (setAsset update deferred to Plan 02)
  </acceptance_criteria>
  <done>LICENSES contains full anthem provenance. The anthem_placeholder.wav filename and setAsset reference are untouched — those change atomically in Plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Off-device tools → bundled asset | FluidSynth renders MIDI → WAV; the WAV is bundled in the APK. The MIDI source and SF2 are public-domain/free-commercial-use. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | FluidSynth binary download from GitHub Releases | mitigate | Download directly from https://github.com/FluidSynth/fluidsynth/releases (official repo); verify file size matches release notes before executing |
| T-05-01 | Information Disclosure | anthem.wav bundled in APK (user could extract it) | accept | SSB composition is public domain; the rendering is not a secret; no IP risk |
| T-05-02 | Denial of Service | _fadeTimer not cancelled in RealAudioService.dispose() | mitigate | Addressed in Plan 02 — _fadeTimer?.cancel() added to dispose() |
</threat_model>

<verification>
After Task 2: `flutter test test/core/audio/audio_service_test.dart` passes (no regression).
After Task 1: `assets/audio/anthem.wav` exists with size > 1 MB.
Human gate: anthem is audible and recognizable as SSB instrumental before Wave 1 begins.
</verification>

<success_criteria>
- assets/audio/anthem.wav exists, size > 1 MB, playable
- scripts/anthem/render_anthem.ps1 exists with -g 0.8 gain flag
- LICENSES contains full anthem provenance (composition, MIDI source, FluidSynth, soundfont)
- flutter test test/core/audio/audio_service_test.dart passes
</success_criteria>

<output>
Create `.planning/phases/05-polish-welcome-accessibility/05-01-SUMMARY.md` when done
</output>
