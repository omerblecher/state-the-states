---
phase: "05-polish-welcome-accessibility"
plan: 07
type: execute
wave: 3
depends_on: ["05-04", "05-05", "05-06"]
files_modified:
  - lib/features/welcome/welcome_screen.dart
  - lib/features/tutorial/tutorial_screen.dart
  - lib/features/home/home_screen.dart
  - lib/features/home/session_restore_card.dart
  - lib/features/game/game_hud.dart
  - test/features/welcome/welcome_screen_test.dart
  - test/features/home/home_screen_test.dart
autonomous: false
requirements:
  - A11Y-01
  - A11Y-02

must_haves:
  truths:
    - "All interactive controls in WelcomeScreen, TutorialScreen, HomeScreen, SessionRestoreCard, GameHud carry Semantics labels"
    - "Every tap target is at least 48×48dp (androidTapTargetGuideline passes in widget tests)"
    - "Correct/incorrect outcomes are signaled by haptic + audio + visual change — never by color alone (A11Y-02, confirmed from Phase 4)"
    - "aapt dump badging of the release APK shows no AD_ID permission"
    - "meetsGuideline(androidTapTargetGuideline) passes for WelcomeScreen and HomeScreen widget tests"
  artifacts:
    - path: "lib/features/welcome/welcome_screen.dart"
      provides: "Semantics labels on GET STARTED button and privacy footer links"
      contains: "Semantics"
    - path: "lib/features/tutorial/tutorial_screen.dart"
      provides: "Semantics on Skip, Next, Done buttons"
      contains: "Semantics"
    - path: "lib/features/home/home_screen.dart"
      provides: "Semantics on mode cards (already present from Phase 4 or added here)"
      contains: "Semantics"
    - path: "lib/features/home/session_restore_card.dart"
      provides: "Semantics on Continue and Dismiss buttons"
      contains: "Semantics"
    - path: "lib/features/game/game_hud.dart"
      provides: "Semantics on hint button and pause button"
      contains: "Semantics"
    - path: "test/features/welcome/welcome_screen_test.dart"
      provides: "meetsGuideline(androidTapTargetGuideline) and meetsGuideline(labeledTapTargetGuideline) assertions"
      contains: "androidTapTargetGuideline"
    - path: "test/features/home/home_screen_test.dart"
      provides: "meetsGuideline(androidTapTargetGuideline) assertion for HomeScreen"
      contains: "androidTapTargetGuideline"
  key_links:
    - from: "test/features/welcome/welcome_screen_test.dart"
      to: "lib/features/welcome/welcome_screen.dart"
      via: "meetsGuideline(androidTapTargetGuideline)"
      pattern: "androidTapTargetGuideline"
---

<objective>
Conduct the Phase 5 accessibility audit: verify every interactive control in new Phase 5 widgets has `Semantics` labels and meets the 48×48dp tap-target guideline, confirm multimodal feedback for correct/incorrect outcomes (already in Phase 4), and run `aapt dump badging` to re-verify no `AD_ID` permission in the release APK.

Purpose: Satisfies A11Y-01 (all interactive controls ≥48×48dp with Semantics labels) and A11Y-02 (multimodal feedback). This is the final wave — runs after all feature widgets are complete.

Output: Semantics labels added to any Phase 5 widgets missing them; `androidTapTargetGuideline` assertions added to welcome and home screen tests; aapt verification checkpoint.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md
@.planning/phases/05-polish-welcome-accessibility/05-04-SUMMARY.md
@.planning/phases/05-polish-welcome-accessibility/05-05-SUMMARY.md
@.planning/phases/05-polish-welcome-accessibility/05-06-SUMMARY.md

<interfaces>
<!-- Flutter accessibility testing API -->
// tester.ensureSemantics() → SemanticsHandle (must call handle.dispose())
// expectLater(tester, meetsGuideline(androidTapTargetGuideline))
// expectLater(tester, meetsGuideline(labeledTapTargetGuideline))
// import: package:flutter/rendering.dart (SemanticsFlag)

<!-- Semantics wrapper pattern (established in Phase 4, game_hud.dart) -->
Semantics(
  button: true,
  label: 'descriptive label',
  child: ElevatedButton(...),
)

<!-- Phase 4 multimodal feedback (already implemented — A11Y-02 is audit-only) -->
// Correct drop: HapticFeedback.lightImpact() + playCorrect() + UsaMapPainter fill color change
// Incorrect drop: HapticFeedback.mediumImpact() + playError() + "not quite" snackbar + bounce animation
// A11Y-02 audit: confirm no code path signals outcome by color alone
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and add Semantics labels to all Phase 5 interactive controls</name>
  <files>lib/features/welcome/welcome_screen.dart, lib/features/tutorial/tutorial_screen.dart, lib/features/home/session_restore_card.dart, lib/features/game/game_hud.dart</files>
  <read_first>
    - lib/features/welcome/welcome_screen.dart (FULL FILE — verify GET STARTED button has Semantics; check privacy footer TextButton)
    - lib/features/tutorial/tutorial_screen.dart (FULL FILE — verify Skip, Next, Done buttons have Semantics with descriptive labels)
    - lib/features/home/session_restore_card.dart (FULL FILE — verify Continue ElevatedButton and Dismiss TextButton have Semantics)
    - lib/features/game/game_hud.dart (FULL FILE — verify hint button and pause/mute buttons have Semantics)
    - lib/features/home/home_screen.dart (FULL FILE — verify mode card tap targets; check if _ModeCard already has Semantics from Phase 4)
    - .planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md (§Validation Architecture — Semantics button wrapper pattern, A11Y-01/A11Y-02 requirements)
  </read_first>
  <action>
    Audit each interactive control in Phase 5 widgets against A11Y-01. For each button/tappable widget that LACKS a `Semantics(button: true, label: '...')` wrapper, ADD one. Concrete required labels:

    WelcomeScreen:
      - GET STARTED ElevatedButton: Semantics(button: true, label: 'Get started, opens the game')
      - Privacy Policy TextButton: Semantics(button: true, label: 'View privacy policy')
      If already present from Task 2 of Plan 03, verify the labels are descriptive (not empty).

    TutorialScreen:
      - Skip TextButton: Semantics(button: true, label: 'Skip tutorial')
      - Next ElevatedButton: Semantics(button: true, label: 'Next tutorial slide')
      - Done/GET STARTED ElevatedButton (last slide): Semantics(button: true, label: 'Finish tutorial')

    SessionRestoreCard:
      - CONTINUE ElevatedButton: Semantics(button: true, label: 'Continue saved ${modeLabel} game')
      - Dismiss TextButton: Semantics(button: true, label: 'Dismiss saved game')
      Root card: Semantics(label: 'Saved game: ${modeLabel}, score ${session.score}', child: ...).
      If already present from Plan 06 Task 1, verify label content is meaningful.

    GameHud hint button:
      Read game_hud.dart — find the hint IconButton or ElevatedButton. If it lacks Semantics:
        Add: `Semantics(button: true, label: 'Use hint (${hintsRemaining} remaining)', child: hintButton)`.
      Pause button: Add `Semantics(button: true, label: 'Pause game')` if missing.
      Mute button: Add `Semantics(button: true, label: 'Toggle mute')` if missing.

    HomeScreen _ModeCard:
      Read existing Semantics on mode cards. If missing, add: `Semantics(button: true, label: '${mode.name} mode, best score ${score ?? 'not played'}', child: card)`.

    A11Y-02 audit (NO code changes expected — verify existing behavior):
      Read map_screen.dart _handleDrop (correct path): confirm HapticFeedback.lightImpact() + playCorrect() are present alongside the visual fill color change.
      Read map_screen.dart _handleDrop (incorrect path): confirm HapticFeedback.mediumImpact() + playError() + snackbar are present alongside the token bounce.
      If any outcome is signaled ONLY by color (no haptic, no audio), add the missing modality. Document what was found in the SUMMARY.

    Tap target sizing audit:
      Check all new buttons in Phase 5 widgets have minimum 48×48dp. ElevatedButton default height is ~48dp — verify by reading the style or adding `minimumSize: const Size(48, 48)` to ButtonStyle where needed. TextButton minimum size defaults to ~36×36dp — ensure all dismiss/skip TextButtons have SizedBox constraints of at least 48×48 or equivalent padding.
  </action>
  <verify>
    <automated>flutter analyze lib/features/welcome/ lib/features/tutorial/ lib/features/home/ lib/features/game/game_hud.dart</automated>
  </verify>
  <acceptance_criteria>
    - Every interactive control in WelcomeScreen, TutorialScreen, SessionRestoreCard, and GameHud has a `Semantics` wrapper with a non-empty `label` string
    - No button label is empty string or whitespace only
    - TextButtons used as Skip/Dismiss have constraints ensuring at least 48dp touch target (via SizedBox, minimumSize, or padding ≥ 12dp vertical)
    - map_screen.dart _handleDrop correct path contains both `HapticFeedback.lightImpact()` and `playCorrect()` (A11Y-02 multimodal verification)
    - map_screen.dart _handleDrop incorrect path contains `HapticFeedback.mediumImpact()` and `playError()` (A11Y-02 multimodal verification)
    - `flutter analyze lib/features/` exits 0
  </acceptance_criteria>
  <done>All Phase 5 interactive controls have Semantics labels; A11Y-02 multimodal feedback confirmed present in Phase 4 code; no color-only outcomes found.</done>
</task>

<task type="auto">
  <name>Task 2: Add meetsGuideline assertions to welcome and home screen tests</name>
  <files>test/features/welcome/welcome_screen_test.dart, test/features/home/home_screen_test.dart</files>
  <read_first>
    - test/features/welcome/welcome_screen_test.dart (FULL FILE — existing tests, especially the androidTapTargetGuideline stub from Plan 03)
    - test/features/home/home_screen_test.dart (FULL FILE — current structure and test helpers)
    - .planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md (§Code Examples — exact Accessibility Guideline Test pattern with tester.ensureSemantics() and handle.dispose())
  </read_first>
  <action>
    In test/features/welcome/welcome_screen_test.dart:
      Ensure the `meetsGuideline(androidTapTargetGuideline)` test is a complete, passing test (not just a stub). It must:
        1. Call `final handle = tester.ensureSemantics();`
        2. pumpWidget with ProviderScope (StubAudioService, minimal StateData mock, StubUserPrefsRepository) + MaterialApp(home: WelcomeScreen()).
        3. await tester.pumpAndSettle().
        4. await expectLater(tester, meetsGuideline(androidTapTargetGuideline)).
        5. await expectLater(tester, meetsGuideline(labeledTapTargetGuideline)).
        6. handle.dispose().
      Import: flutter_test.dart (meetsGuideline is in flutter_test, not material).

    In test/features/home/home_screen_test.dart:
      Add a new test in the existing 'HomeScreen mode cards' group (or a new 'A11Y' group):
        1. `final handle = tester.ensureSemantics();`
        2. pumpWidget buildHomeScreen(mockRepo) where mockRepo returns null for all scores.
        3. await tester.pump(); await tester.pump();
        4. await expectLater(tester, meetsGuideline(androidTapTargetGuideline)).
        5. handle.dispose().
  </action>
  <verify>
    <automated>flutter test test/features/welcome/welcome_screen_test.dart test/features/home/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - test/features/welcome/welcome_screen_test.dart contains a test that calls `meetsGuideline(androidTapTargetGuideline)` with tester.ensureSemantics() guard
    - test/features/welcome/welcome_screen_test.dart contains a test that calls `meetsGuideline(labeledTapTargetGuideline)`
    - test/features/home/home_screen_test.dart contains a test that calls `meetsGuideline(androidTapTargetGuideline)`
    - `flutter test test/features/welcome/welcome_screen_test.dart` passes
    - `flutter test test/features/home/home_screen_test.dart` passes
  </acceptance_criteria>
  <done>Accessibility guideline tests added and passing in both welcome and home screen test suites.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All Phase 5 Flutter work is complete: welcome screen, anthem, tutorial, hint animation, session restore, and accessibility labels. The release APK is built and the COPPA compliance re-verification runs.
  </what-built>
  <how-to-verify>
    Step 1 — Build release APK:
      flutter build apk --release
      (Expected: BUILD SUCCESSFUL with no errors)

    Step 2 — Run aapt dump badging to verify AD_ID is absent:
      $aaptPath = Get-ChildItem "$env:ANDROID_HOME\build-tools" -Recurse -Filter "aapt.exe" | Select-Object -First 1 -ExpandProperty FullName
      & $aaptPath dump badging build\app\outputs\flutter-apk\app-release.apk | Select-String "AD_ID"
      Expected output: NOTHING (no match means AD_ID permission is absent — COMP-01/02 re-verified).
      If AD_ID appears: investigate which package introduced it and remove it before marking phase complete.

    Step 3 — Run full test suite:
      flutter test
      Expected: all tests pass (no failures, no errors)

    Step 4 — Manual smoke test on device or emulator:
      a. Install release APK or run flutter run --release.
      b. On first launch: welcome screen shows USA silhouette filling in, anthem plays with fade-in. Tap GET STARTED → anthem fades out → tutorial appears.
      c. Tutorial: swipe through all 4 slides; verify Skip button and Done button both exit to home screen.
      d. Start a game; use the hint button → map zooms to target state → yellow-green glow appears → glow fades after 3 seconds.
      e. Kill app mid-game; relaunch → resume card appears on home screen with correct mode/score/time/count. Tap Continue → game resumes in paused state.
      f. Second launch: welcome screen appears but tutorial is NOT shown.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Release APK | aapt dump badging verifies no AD_ID permission leaked into release build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Information Disclosure | AD_ID permission reintroduced by transitive dependency update | mitigate | aapt dump badging check in this plan's human-verify checkpoint catches it; must be clean before phase is marked complete |
| T-05-16 | Denial of Service | Inaccessible buttons fail WCAG (affects child users of screen readers) | mitigate | Semantics labels added to all interactive controls; androidTapTargetGuideline assertions in tests |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase — legitimacy audit confirmed N/A in RESEARCH.md |
</threat_model>

<verification>
`flutter test` — full suite passes.
`flutter analyze lib/` — exits 0.
aapt dump badging on release APK returns no AD_ID match.
Manual: welcome → tutorial (first launch) → home → game → hint zoom → session restore all verified on device.
</verification>

<success_criteria>
- Semantics labels present on all Phase 5 interactive controls
- meetsGuideline(androidTapTargetGuideline) passes for WelcomeScreen and HomeScreen
- A11Y-02: correct/incorrect outcomes multimodal (haptic + audio + visual) confirmed in Phase 4 code
- aapt dump badging shows no AD_ID permission in release APK
- Full flutter test suite passes
</success_criteria>

<output>
Create `.planning/phases/05-polish-welcome-accessibility/05-07-SUMMARY.md` when done
</output>
