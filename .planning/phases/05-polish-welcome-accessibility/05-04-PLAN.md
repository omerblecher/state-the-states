---
phase: "05-polish-welcome-accessibility"
plan: 04
type: execute
wave: 3
depends_on: ["05-03"]
files_modified:
  - lib/features/tutorial/tutorial_screen.dart
  - test/features/tutorial/tutorial_screen_test.dart
autonomous: true
requirements:
  - SESS-04

must_haves:
  truths:
    - "TutorialScreen shows 4 slides: Welcome, Drag & Drop, Scoring, Hints"
    - "Both Skip (top-right, always visible) and Done (last slide) call setTutorialSeen(true)"
    - "After tutorial completes or is skipped, app navigates to / (home) via context.go"
    - "Tutorial does not show on second launch — tutorialSeen flag persists"
    - "tutorial_screen_test.dart tests both Skip path and Done path"
  artifacts:
    - path: "lib/features/tutorial/tutorial_screen.dart"
      provides: "4-slide PageView onboarding with _completeTutorial() shared exit"
      contains: "_completeTutorial"
    - path: "test/features/tutorial/tutorial_screen_test.dart"
      provides: "Skip + Done path tests"
      contains: "tutorial_screen_test"
  key_links:
    - from: "lib/features/tutorial/tutorial_screen.dart"
      to: "lib/core/data/user_prefs_repository.dart"
      via: "ref.read(userPrefsRepositoryProvider.future).setTutorialSeen(true)"
      pattern: "setTutorialSeen"
    - from: "lib/features/tutorial/tutorial_screen.dart"
      to: "lib/app.dart"
      via: "context.go('/')"
      pattern: "context\\.go\\('/'\\)"
---

<objective>
Replace the TutorialScreen stub (created in Plan 03) with the full 4-slide `PageView` onboarding implementation. Both the Skip button and the Done button (last slide) must call a shared `_completeTutorial()` helper that sets the tutorial-seen flag and navigates home. Write widget tests covering both exit paths.

Purpose: Satisfies SESS-04 ("4-step skippable tutorial runs once; 'seen' flag persists so it does not repeat"). The tutorial is gated by the welcome screen's routing logic (Plan 03) — this plan only implements the screen itself.

Output: Full `tutorial_screen.dart`, `tutorial_screen_test.dart` with skip + done path coverage.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md
@.planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md
@.planning/phases/05-polish-welcome-accessibility/05-03-SUMMARY.md

<interfaces>
<!-- UserPrefsRepository (user_prefs_repository.dart) -->
abstract interface class UserPrefsRepository {
  Future<bool> getTutorialSeen();
  Future<void> setTutorialSeen(bool seen);
  Future<bool> getMuted();
  Future<void> setMuted(bool muted);
}
// Provider: userPrefsRepositoryProvider — FutureProvider<UserPrefsRepository>
// Usage: final repo = await ref.read(userPrefsRepositoryProvider.future);

<!-- go_router navigation pattern (from home_screen.dart) -->
// context.go('/') — replace current route (no back-stack to welcome/tutorial)

<!-- ConsumerStatefulWidget pattern (from home_screen.dart) -->
class TutorialScreen extends ConsumerStatefulWidget {
  const TutorialScreen({super.key});
  @override ConsumerState<TutorialScreen> createState() => _TutorialScreenState();
}
class _TutorialScreenState extends ConsumerState<TutorialScreen> { ... }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TutorialScreen with PageView, Skip, and Done paths</name>
  <files>lib/features/tutorial/tutorial_screen.dart, test/features/tutorial/tutorial_screen_test.dart</files>
  <read_first>
    - lib/features/tutorial/tutorial_screen.dart (FULL FILE — currently a stub from Plan 03; read its current state)
    - lib/features/home/home_screen.dart (lines 1–30 — ConsumerStatefulWidget + go_router navigation pattern to follow)
    - lib/core/data/user_prefs_repository.dart (FULL FILE — getTutorialSeen / setTutorialSeen signatures)
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§tutorial_screen.dart — imports, widget shell, _completeTutorial() helper, FutureProvider read pattern)
    - .planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md (§Pattern 5: First-Launch Tutorial — PageController, slide data structure, Pitfall 3 warning)
  </read_first>
  <behavior>
    - Test 1 (Skip path): pumpWidget TutorialScreen → tap 'Skip' → verify setTutorialSeen(true) called → verify navigated to '/'
    - Test 2 (Done path): pump TutorialScreen → advance to last slide (swipe or pageController.animateToPage(3)) → tap 'Done' → verify setTutorialSeen(true) called → verify navigated to '/'
    - Test 3 (initial state): first slide title visible on load
  </behavior>
  <action>
    Replace the stub in lib/features/tutorial/tutorial_screen.dart with the full implementation:

    Imports: flutter/material.dart, flutter_riverpod, go_router, core/data/user_prefs_repository.dart.

    Widget class: `TutorialScreen extends ConsumerStatefulWidget`, state class `_TutorialScreenState extends ConsumerState<TutorialScreen>`.

    Fields:
      `final PageController _pageController = PageController();`
      `int _currentPage = 0;`

    Static const slide data (use an inner record or a simple _SlideData class):
      Slide 0 — icon: Icons.map, title: 'Learn All 50 States!', body: 'Place each state token on its correct location on the USA map.'
      Slide 1 — icon: Icons.touch_app, title: 'Drag & Drop', body: 'Drag a state token from the tray and drop it on the map. Try to place it correctly!'
      Slide 2 — icon: Icons.sports_golf, title: 'Golf Scoring', body: 'Lower is better. Time and wrong placements add points. Play fast and accurately!'
      Slide 3 — icon: Icons.lightbulb, title: 'Use Your Hints', body: 'Each round gives you 2 hints. A hint zooms the map to your target state (+5 points each).'

    `_completeTutorial()` method (shared by BOTH skip and done — CRITICAL, RESEARCH.md Pitfall 3):
      `Future<void> _completeTutorial() async { final repo = await ref.read(userPrefsRepositoryProvider.future); await repo.setTutorialSeen(true); if (mounted) context.go('/'); }`

    `dispose()`: `_pageController.dispose(); super.dispose();`

    `build()` layout:
      Scaffold with backgroundColor Color(0xFF0D47A1) (matches welcome gradient start).
      Stack children:
        (1) SafeArea with PageView (controller: _pageController, onPageChanged: (p) => setState(()=>_currentPage=p), children: 4 slide pages). Each slide page is a Column with Expanded icon (Icon size 80, color white), title Text (white, bold, 24), body Text (white 16, textAlign center), padding.
        (2) Positioned top-right: Semantics(button:true, label:'Skip tutorial') wrapping TextButton(onPressed: _completeTutorial, child: Text('Skip', style: TextStyle(color: Colors.white70, fontSize: 16))).
        (3) Positioned bottom: Row with dot indicators + Next/Done button. If _currentPage < 3: ElevatedButton 'NEXT' → `_pageController.nextPage(duration: Duration(milliseconds: 300), curve: Curves.easeInOut)`. If _currentPage == 3: ElevatedButton 'GET STARTED' (or 'DONE') wrapped in Semantics(button:true, label:'Finish tutorial') → onPressed: _completeTutorial.

    Create test/features/tutorial/tutorial_screen_test.dart:
      Use MockUserPrefsRepository (mocktail). Override userPrefsRepositoryProvider in ProviderScope.
      Test 1 (Skip): pump TutorialScreen → find 'Skip' button → tap → pump → verify `setTutorialSeen(true)` was called on mock.
      Test 2 (Done): pump TutorialScreen → jump to page 3 via _pageController or fling gesture 3 times → find 'DONE' or 'GET STARTED' button → tap → pump → verify `setTutorialSeen(true)` called.
      Test 3: first page slide title 'Learn All 50 States!' finds at least one widget on initial pump.

    IMPORTANT: Both 'Skip' onPressed and last-slide button onPressed MUST call exactly `_completeTutorial()` — never inline different logic in each. This is the single most common bug (RESEARCH.md Pitfall 3).
  </action>
  <verify>
    <automated>flutter test test/features/tutorial/tutorial_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/tutorial/tutorial_screen.dart is no longer a stub (contains PageView with 4 children)
    - lib/features/tutorial/tutorial_screen.dart contains exactly one `_completeTutorial` method
    - Both the Skip button and the final-slide button's onPressed reference `_completeTutorial` (not inline logic)
    - lib/features/tutorial/tutorial_screen.dart contains `setTutorialSeen` (via the _completeTutorial helper)
    - test/features/tutorial/tutorial_screen_test.dart contains at least 3 tests (skip path, done path, initial state)
    - `flutter test test/features/tutorial/tutorial_screen_test.dart` passes
    - `flutter analyze lib/features/tutorial/` exits 0
  </acceptance_criteria>
  <done>TutorialScreen fully implemented; both Skip and Done paths set tutorialSeen flag; test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TutorialScreen → UserPrefsRepository | setTutorialSeen(true) writes to SharedPreferences; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Spoofing | tutorial_seen flag cleared (user deletes SharedPreferences) | accept | Showing tutorial again is benign — not a security risk; COPPA-irrelevant |
| T-05-09 | Denial of Service | PageController not disposed if TutorialScreen is popped mid-animation | mitigate | dispose() calls _pageController.dispose() — handled in implementation |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
`flutter test test/features/tutorial/tutorial_screen_test.dart` — all tests pass.
`flutter analyze lib/features/tutorial/` — exits 0.
Manual: on first launch, tutorial appears; pressing Skip navigates to /; on second launch, tutorial is not shown.
</verification>

<success_criteria>
- TutorialScreen shows 4 slides navigable by swipe/Next
- Skip always visible; tapping it sets tutorialSeen=true and goes to /
- Last-slide Done button sets tutorialSeen=true and goes to /
- Both paths use the same _completeTutorial() helper
- tutorial_screen_test.dart passes for skip path, done path, and initial state
</success_criteria>

<output>
Create `.planning/phases/05-polish-welcome-accessibility/05-04-SUMMARY.md` when done
</output>
