---
phase: "05-polish-welcome-accessibility"
plan: 02
type: execute
wave: 1
depends_on: ["05-01"]
files_modified:
  - lib/core/audio/audio_service.dart
  - lib/core/audio/real_audio_service.dart
  - lib/core/audio/stub_audio_service.dart
  - test/core/audio/audio_service_test.dart
autonomous: true
requirements:
  - WEL-02
  - WEL-03

must_haves:
  truths:
    - "AudioService interface exposes fadeInAnthem() and fadeOutAnthem() (not playAnthem/stopAnthem)"
    - "RealAudioService.fadeInAnthem() ramps volume 0→1 over 500ms (25 ticks × 20ms)"
    - "RealAudioService.fadeOutAnthem() ramps volume 1→0 over 800ms (40 ticks × 20ms), then stops"
    - "RealAudioService loads anthem.wav (not anthem_placeholder.wav)"
    - "StubAudioService compiles with the new interface"
    - "audio_service_test.dart asserts both fade methods as no-ops on StubAudioService"
    - "_fadeTimer cancelled in RealAudioService.dispose() (RESEARCH Pitfall 4)"
  artifacts:
    - path: "lib/core/audio/audio_service.dart"
      provides: "Updated AudioService interface"
      contains: "fadeInAnthem"
    - path: "lib/core/audio/real_audio_service.dart"
      provides: "Volume-ramp implementation"
      contains: "Timer.periodic"
    - path: "lib/core/audio/stub_audio_service.dart"
      provides: "No-op implementations"
      contains: "fadeInAnthem"
    - path: "test/core/audio/audio_service_test.dart"
      provides: "Interface-parity test coverage"
      contains: "fadeInAnthem"
  key_links:
    - from: "lib/core/audio/real_audio_service.dart"
      to: "just_audio AudioPlayer.setVolume()"
      via: "Timer.periodic 20ms callback"
      pattern: "Timer\\.periodic"
    - from: "lib/core/audio/audio_service.dart"
      to: "lib/core/audio/stub_audio_service.dart"
      via: "implements AudioService"
      pattern: "fadeInAnthem"
---

<objective>
Atomically rename `playAnthem()`/`stopAnthem()` → `fadeInAnthem()`/`fadeOutAnthem()` across all three audio files and their test, implementing the 500ms fade-in and 800ms fade-out volume ramps using `Timer.periodic` in `RealAudioService`. Also update `setAsset()` to load `anthem.wav` (not `anthem_placeholder.wav`).

Purpose: Satisfies WEL-03 ("seamless fade-out") and WEL-02 (anthem plays with 500ms fade-in per D-A3). This is a 3-file atomic change — all three audio files must stay in sync or the Dart compiler will reject the interface contract.

Output: Updated `audio_service.dart`, `real_audio_service.dart`, `stub_audio_service.dart`, and `audio_service_test.dart`.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md
@.planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md

<interfaces>
<!-- Current AudioService interface (audio_service.dart) — FULL FILE, 15 lines -->
abstract interface class AudioService {
  Future<void> init();
  Future<void> playCorrect();
  Future<void> playError();
  Future<void> playAnthem();    // → rename to fadeInAnthem()
  Future<void> stopAnthem();    // → rename to fadeOutAnthem()
  Future<void> setMuted(bool muted);
  Future<void> dispose();
}

<!-- Current RealAudioService key fields -->
// _anthemPlayer: AudioPlayer with LoopMode.one
// bool _initialized = false
// setAsset call (line 23): 'assets/audio/anthem_placeholder.wav'  → must become 'assets/audio/anthem.wav'
// playAnthem() (lines 56-62): seek(Duration.zero) + unawaited(play())  → replace with fadeInAnthem()
// stopAnthem() (lines 64-70): stop()  → replace with fadeOutAnthem()
// setMuted() (lines 72-80): sets _anthemPlayer.setVolume()  → also track _isMuted flag

<!-- Current StubAudioService (stub_audio_service.dart) — FULL FILE -->
// playAnthem() async {}   → replace with fadeInAnthem() async {}
// stopAnthem() async {}   → replace with fadeOutAnthem() async {}

<!-- Current audio_service_test.dart calls (lines 68-70) -->
// await stub.playAnthem();
// await stub.stopAnthem();
// → update to: await stub.fadeInAnthem();  await stub.fadeOutAnthem();
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update AudioService interface and StubAudioService (3-file atomic rename)</name>
  <files>lib/core/audio/audio_service.dart, lib/core/audio/stub_audio_service.dart, test/core/audio/audio_service_test.dart</files>
  <read_first>
    - lib/core/audio/audio_service.dart (FULL FILE — current interface to replace)
    - lib/core/audio/stub_audio_service.dart (FULL FILE — current no-ops to replace)
    - test/core/audio/audio_service_test.dart (FULL FILE — test calls to update)
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§audio_service.dart MODIFY section — exact updated interface text)
  </read_first>
  <behavior>
    - Test 1: StubAudioService satisfies AudioService via fadeInAnthem() without throwing
    - Test 2: StubAudioService satisfies AudioService via fadeOutAnthem() without throwing
    - Test 3: Neither playAnthem() nor stopAnthem() exists on AudioService (compilation would fail if called)
  </behavior>
  <action>
    In audio_service.dart: Replace the `playAnthem()` declaration with `/// Plays the anthem from the start with a 500ms volume fade-in (D-A3).\nFuture<void> fadeInAnthem();` and replace the `stopAnthem()` declaration with `/// Ramps anthem volume to 0 over ~800ms then stops playback (D-A2).\nFuture<void> fadeOutAnthem();`. No other changes to the file.

    In stub_audio_service.dart: Replace `Future<void> playAnthem() async {}` with `@override\nFuture<void> fadeInAnthem() async {}` and replace `Future<void> stopAnthem() async {}` with `@override\nFuture<void> fadeOutAnthem() async {}`. All other stubs remain unchanged.

    In test/core/audio/audio_service_test.dart: In the StubAudioService group, replace the two lines `await stub.playAnthem();` and `await stub.stopAnthem();` with `await stub.fadeInAnthem();` and `await stub.fadeOutAnthem();`. Update the comment to reference both new method names.

    IMPORTANT: real_audio_service.dart is updated in Task 2, not here. After this task, Dart compilation will fail until Task 2 completes — these two tasks must be committed together or Task 2 must follow immediately in the same execution.
  </action>
  <verify>
    <automated>flutter test test/core/audio/audio_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - audio_service.dart contains `fadeInAnthem` and `fadeOutAnthem` declarations
    - audio_service.dart does NOT contain `playAnthem` or `stopAnthem`
    - stub_audio_service.dart contains `fadeInAnthem` and `fadeOutAnthem` implementations
    - stub_audio_service.dart does NOT contain `playAnthem` or `stopAnthem`
    - test/core/audio/audio_service_test.dart calls `stub.fadeInAnthem()` and `stub.fadeOutAnthem()`
    - `flutter test test/core/audio/audio_service_test.dart` passes (StubAudioService group passes)
  </acceptance_criteria>
  <done>Interface and stub updated atomically; StubAudioService test group passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement fadeInAnthem/fadeOutAnthem in RealAudioService with Timer.periodic volume ramp</name>
  <files>lib/core/audio/real_audio_service.dart</files>
  <read_first>
    - lib/core/audio/real_audio_service.dart (FULL FILE — current implementation to modify)
    - .planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md (§Pattern 3: Anthem Volume Fade — exact Timer.periodic implementation with tick counts and interval)
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§real_audio_service.dart MODIFY section — field additions, import additions, dispose update)
  </read_first>
  <behavior>
    - Test 1: RealAudioService.init() completes without throwing after the anthem_placeholder.wav path is removed (existing test still passes with missing-asset graceful-failure behavior)
    - Test 2: RealAudioService.dispose() completes without throwing even when _fadeTimer is active (cancel before dispose)
    - Test 3: fade methods are defined as non-abstract implementations on RealAudioService
  </behavior>
  <action>
    Add imports `dart:async` (Timer, unawaited — `dart:async show unawaited` already present; add Timer to the show list or rely on dart:async implicit import) and `dart:math as math` at the top of the file.

    Add two new fields after `bool _initialized = false`:
      `Timer? _fadeTimer;`
      `bool _isMuted = false;`

    Update init(): Change the setAsset call from `'assets/audio/anthem_placeholder.wav'` to `'assets/audio/anthem.wav'` (RESEARCH.md Pitfall 7 — anthem asset rename).

    Replace `playAnthem()` (lines 56–62) with `fadeInAnthem()` implementing the Timer.periodic fade-in:
      - Guard: if (!_initialized || _isMuted) return;
      - Cancel any running _fadeTimer;
      - Set initial volume to 0.0 via _anthemPlayer.setVolume(0.0);
      - Seek to Duration.zero and unawaited(_anthemPlayer.play());
      - Start Timer.periodic(Duration(milliseconds: 20), ...) with 25 total ticks (500ms ÷ 20ms);
      - Each tick: volume += 1.0 / 25; clamp to 1.0; await _anthemPlayer.setVolume(volume) in try/catch; cancel timer when volume >= 1.0.

    Replace `stopAnthem()` (lines 64–70) with `fadeOutAnthem()` implementing the Timer.periodic fade-out:
      - Guard: if (!_initialized) return;
      - Cancel any running _fadeTimer;
      - Start volume at `_isMuted ? 0.0 : 1.0`;
      - Start Timer.periodic(Duration(milliseconds: 20), ...) with 40 total ticks (800ms ÷ 20ms);
      - Each tick: volume -= 1.0 / 40; clamp to 0.0; await _anthemPlayer.setVolume(volume) in try/catch; when volume <= 0.0: cancel timer, then await _anthemPlayer.stop() in try/catch.

    Update setMuted(): Add `_isMuted = muted;` as the first statement inside the guard, so fade methods can check this flag.

    Update dispose(): Add `_fadeTimer?.cancel();` as the FIRST statement before the three player disposes (RESEARCH.md Pitfall 4 — timer fires after dispose).

    The @override annotation is required on both fadeInAnthem() and fadeOutAnthem().
  </action>
  <verify>
    <automated>flutter test test/core/audio/audio_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - real_audio_service.dart contains `fadeInAnthem` and `fadeOutAnthem` method declarations with @override
    - real_audio_service.dart does NOT contain `playAnthem` or `stopAnthem`
    - real_audio_service.dart contains `Timer? _fadeTimer;` field
    - real_audio_service.dart contains `bool _isMuted = false;` field
    - real_audio_service.dart setAsset call references `'assets/audio/anthem.wav'` (not `anthem_placeholder.wav`)
    - real_audio_service.dart dispose() contains `_fadeTimer?.cancel()` before any player dispose call
    - `flutter test test/core/audio/audio_service_test.dart` passes (all groups pass — both StubAudioService and RealAudioService)
    - `flutter analyze lib/core/audio/` exits 0 (no compile errors)
  </acceptance_criteria>
  <done>RealAudioService implements the new AudioService interface; full audio test suite green; anthem.wav path wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WelcomeScreen → RealAudioService | UI calls fadeInAnthem/fadeOutAnthem; service manages audio lifecycle internally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Denial of Service | _fadeTimer fires after RealAudioService.dispose() | mitigate | _fadeTimer?.cancel() added as first statement in dispose() — Pitfall 4 explicitly addressed |
| T-05-04 | Denial of Service | fadeInAnthem/fadeOutAnthem called concurrently (rapid CTA taps) | mitigate | Both methods call _fadeTimer?.cancel() at entry, ensuring previous fade is cancelled before starting new one |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — package legitimacy audit in RESEARCH.md confirms N/A |
</threat_model>

<verification>
`flutter test test/core/audio/audio_service_test.dart` — all test groups pass.
`flutter analyze lib/core/audio/` — exits 0.
Grep check: `grep -rn "playAnthem\|stopAnthem" lib/` should return 0 matches.
</verification>

<success_criteria>
- fadeInAnthem/fadeOutAnthem defined in interface and implemented in RealAudioService and StubAudioService
- Timer.periodic 20ms ramp in both fade methods with correct tick counts (25 for fade-in, 40 for fade-out)
- anthem.wav path wired in setAsset()
- _fadeTimer cancelled in dispose()
- audio_service_test.dart updated and passing
</success_criteria>

<output>
Create `.planning/phases/05-polish-welcome-accessibility/05-02-SUMMARY.md` when done
</output>
