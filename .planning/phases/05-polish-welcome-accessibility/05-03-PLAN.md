---
phase: "05-polish-welcome-accessibility"
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - lib/app.dart
  - lib/features/welcome/welcome_screen.dart
  - lib/features/welcome/usa_welcome_painter.dart
  - test/features/welcome/welcome_screen_test.dart
autonomous: true
requirements:
  - WEL-01
  - WEL-02
  - WEL-03

must_haves:
  truths:
    - "App opens to /welcome (not /) on first launch"
    - "WelcomeScreen shows a deep-blue gradient background with white USA silhouette"
    - "States fill in one-by-one with random stagger over ~1.5s (D-W2)"
    - "Alaska and Hawaii inset regions appear in the silhouette (D-W4)"
    - "Anthem fade-in starts on welcome screen load via fadeInAnthem() (D-A3)"
    - "GET STARTED button fires-and-forgets fadeOutAnthem(), then navigates after 850ms (Pitfall 5)"
    - "welcome_screen_test.dart smoke-tests the screen without throwing"
  artifacts:
    - path: "lib/app.dart"
      provides: "Updated router with /welcome initial route and /tutorial route"
      contains: "initialLocation: '/welcome'"
    - path: "lib/features/welcome/welcome_screen.dart"
      provides: "WelcomeScreen widget with stagger animation and CTA"
      contains: "fadeInAnthem"
    - path: "lib/features/welcome/usa_welcome_painter.dart"
      provides: "Stagger-fill silhouette CustomPainter"
      contains: "UsaWelcomePainter"
    - path: "test/features/welcome/welcome_screen_test.dart"
      provides: "Smoke test + A11Y guideline check stub"
      contains: "welcome_screen_test"
  key_links:
    - from: "lib/app.dart"
      to: "lib/features/welcome/welcome_screen.dart"
      via: "GoRoute path: '/welcome'"
      pattern: "'/welcome'"
    - from: "lib/features/welcome/welcome_screen.dart"
      to: "lib/core/audio/audio_service_provider.dart"
      via: "ref.read(audioServiceProvider).fadeInAnthem()"
      pattern: "fadeInAnthem"
    - from: "lib/features/welcome/usa_welcome_painter.dart"
      to: "lib/core/data/state_data_service.dart"
      via: "stateDataProvider supplies MapData.states"
      pattern: "UsaWelcomePainter"
---

<objective>
Wire up the /welcome initial route in app.dart, implement WelcomeScreen with the stagger USA silhouette CustomPainter and anthem fade-in, and add the /tutorial route stub. Write a smoke test for the welcome screen.

Purpose: Delivers WEL-01 (premium opening screen), WEL-02 (anthem plays on load), and WEL-03 (fade-out on navigation). This plan is Wave 1 parallel with Plan 02 (audio service). Plans 02 and 03 share no files — they can execute concurrently. However, Plan 03 calls `fadeInAnthem()` / `fadeOutAnthem()` — the executor must verify Plan 02 is complete before running this plan.

Output: app.dart with /welcome initial route, welcome_screen.dart, usa_welcome_painter.dart, welcome_screen_test.dart stub.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md
@.planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md
@.planning/phases/05-polish-welcome-accessibility/05-02-SUMMARY.md

<interfaces>
<!-- app.dart (FULL FILE) — current routes to extend -->
// initialLocation: '/'  → change to '/welcome'
// Add GoRoute('/welcome') → WelcomeScreen()
// Add GoRoute('/tutorial') → TutorialScreen() (stub widget — Plan 04 fills it in)

<!-- AudioService (post-Plan-02 interface) -->
abstract interface class AudioService {
  Future<void> init();
  Future<void> playCorrect();
  Future<void> playError();
  Future<void> fadeInAnthem();   // 500ms volume ramp 0→1 then plays loop
  Future<void> fadeOutAnthem();  // 800ms volume ramp 1→0 then stops
  Future<void> setMuted(bool muted);
  Future<void> dispose();
}

<!-- stateDataProvider (state_data_service.dart) — FutureProvider<MapData> -->
// ref.watch(stateDataProvider) returns AsyncValue<MapData>
// MapData.states: List<StateData>
// StateData.paths: List<Path>   (dart:ui Path, pre-parsed at load time)
// StateData.postal: String  (e.g. 'TX', 'AK', 'HI')

<!-- userPrefsRepositoryProvider — FutureProvider<UserPrefsRepository> -->
// ref.read(userPrefsRepositoryProvider.future): Future<UserPrefsRepository>
// UserPrefsRepository.getTutorialSeen(): Future<bool>

<!-- audioServiceProvider — Provider<AudioService> (in audio_service_provider.dart) -->
// ref.read(audioServiceProvider): AudioService (synchronous read)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update app.dart routing — /welcome initial route, /tutorial stub route</name>
  <files>lib/app.dart</files>
  <read_first>
    - lib/app.dart (FULL FILE — current router to extend)
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§app.dart MODIFY section — exact router update)
  </read_first>
  <action>
    In lib/app.dart:

    1. Change `initialLocation: '/'` to `initialLocation: '/welcome'`.

    2. Add the following import lines at the top (after existing imports):
       `import 'features/welcome/welcome_screen.dart';`
       `import 'features/tutorial/tutorial_screen.dart';`

    3. Add two new GoRoute entries at the TOP of the routes list (before the existing '/' route):
       - GoRoute(path: '/welcome', builder: (context, state) => const WelcomeScreen())
       - GoRoute(path: '/tutorial', builder: (context, state) => const TutorialScreen())

    4. Leave all existing routes (/play, /complete, /spike, /) unchanged.

    TutorialScreen is a stub at this stage — it will be fully implemented in Plan 04. Create a minimal stub class `class TutorialScreen extends StatelessWidget { const TutorialScreen({super.key}); @override Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Tutorial'))); }` in lib/features/tutorial/tutorial_screen.dart if that file does not yet exist.
  </action>
  <verify>
    <automated>flutter analyze lib/app.dart lib/features/tutorial/</automated>
  </verify>
  <acceptance_criteria>
    - lib/app.dart contains `initialLocation: '/welcome'`
    - lib/app.dart contains a GoRoute with `path: '/welcome'`
    - lib/app.dart contains a GoRoute with `path: '/tutorial'`
    - lib/features/tutorial/tutorial_screen.dart exists (stub or full — at least a compilable StatelessWidget)
    - `flutter analyze lib/app.dart` exits 0
  </acceptance_criteria>
  <done>app.dart updated; /welcome is initial route; /tutorial route registered; TutorialScreen stub compiles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement WelcomeScreen and UsaWelcomePainter with stagger animation and anthem</name>
  <files>lib/features/welcome/welcome_screen.dart, lib/features/welcome/usa_welcome_painter.dart, test/features/welcome/welcome_screen_test.dart</files>
  <read_first>
    - lib/features/map/usa_map_painter.dart (lines 1–80 — CustomPainter pattern, paint() structure, canvas transform idiom)
    - lib/core/audio/audio_service_provider.dart (provider name for audioServiceProvider)
    - lib/core/data/state_data_service.dart (stateDataProvider declaration)
    - lib/core/data/user_prefs_repository.dart (userPrefsRepositoryProvider, getTutorialSeen signature)
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§welcome_screen.dart — gradient colors, initState pattern, CTA handler, Pitfall 5 fire-and-forget)
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§usa_welcome_painter.dart — paint() with canvas.save/scale/restore, shouldRepaint threshold)
    - C:\code\Claude\FlagsRoundTheWorld\lib\features\home\welcome_screen.dart (port source for gradient, CTA sizing, privacy footer structure)
  </read_first>
  <behavior>
    - Test 1: WelcomeScreen renders inside ProviderScope without throwing (smoke test with StubAudioService override)
    - Test 2: WelcomeScreen contains a widget with 'GET STARTED' text
    - Test 3: WelcomeScreen widget tree meets androidTapTargetGuideline (A11Y-01 pre-check)
  </behavior>
  <action>
    Create lib/features/welcome/usa_welcome_painter.dart:
      - Class `UsaWelcomePainter extends CustomPainter` with constructor params: `required List<StateData> states`, `required List<int> staggerOrder`, `required double animValue`.
      - `shouldRepaint`: return `(old.animValue - animValue).abs() > 0.001` (RESEARCH.md Pitfall 6 — do NOT use direct == comparison).
      - `paint()`: (a) canvas.save(); (b) scale the 1000×628 viewBox to fit size using `fitScale = min(size.width/1000, size.height/628)`, centered with translate tx/ty; (c) for each index k in 0..staggerOrder.length-1, threshold = k / staggerOrder.length; skip if animValue < threshold; draw `states[staggerOrder[k]].paths` with white fill (Color(0xFFFFFFFF), PaintingStyle.fill); (d) canvas.restore(). No border paint, no ocean fill (gradient background shows through).

    Create lib/features/welcome/welcome_screen.dart:
      - `class WelcomeScreen extends ConsumerStatefulWidget` with `class _WelcomeScreenState extends ConsumerState<WelcomeScreen> with TickerProviderStateMixin`.
      - Fields: `late final AnimationController _staggerController`, `final _random = math.Random()`, `List<int> _staggerOrder = []`.
      - `initState()`: create `_staggerController = AnimationController(vsync: this, duration: const Duration(milliseconds: 1500))..forward()`. Then `WidgetsBinding.instance.addPostFrameCallback((_) { ref.read(audioServiceProvider).fadeInAnthem(); })`.
      - `dispose()`: `_staggerController.dispose(); super.dispose();`.
      - `_onStartPressed()`: fire-and-forget `ref.read(audioServiceProvider).fadeOutAnthem()` (do NOT await). Then `Future.delayed(const Duration(milliseconds: 850), () async { if (!mounted) return; final repo = await ref.read(userPrefsRepositoryProvider.future); if (!mounted) return; final seen = await repo.getTutorialSeen(); if (!mounted) return; context.go(seen ? '/' : '/tutorial'); })`.
      - `build()`: Scaffold (backgroundColor transparent) wrapping a Container with LinearGradient background colors `[Color(0xFF0D47A1), Color(0xFF1565C0), Color(0xFF1976D2), Color(0xFF42A5F5)]` with `stops: [0.0, 0.3, 0.6, 1.0]`, topLeft→bottomRight. Column children: (1) SafeArea top padding; (2) title text "STATE THE STATES" centered, bold white; (3) subtitle text "Place all 50 states on the map" or similar; (4) Expanded hero area: ref.watch(stateDataProvider).when(loading: SizedBox, error: SizedBox, data: (mapData) { if (_staggerOrder.isEmpty) { _staggerOrder = List.generate(mapData.states.length, (i)=>i)..shuffle(_random); } return AnimatedBuilder(animation: _staggerController, builder: (_, __) => CustomPaint(painter: UsaWelcomePainter(states: mapData.states, staggerOrder: _staggerOrder, animValue: _staggerController.value))); }); (5) Padding with GET STARTED ElevatedButton (width double.infinity, height 56, backgroundColor Colors.white, foregroundColor Color(0xFF1565C0), onPressed: _onStartPressed), wrapped in Semantics(button: true, label: 'Get started'); (6) privacy footer row (copy Flags pattern — TextButton "Privacy Policy" + "·" separator + "© 2026 Otis & Brooke"); (7) SizedBox bottom padding.

    Create test/features/welcome/welcome_screen_test.dart:
      - Import flutter_test, flutter_riverpod, flutter, state_states audio/welcome files.
      - Mock audioServiceProvider with StubAudioService. Mock stateDataProvider to return a minimal MapData with 2 states (so painter doesn't crash). Mock userPrefsRepositoryProvider.
      - Test 1 (smoke): pumpWidget with ProviderScope overrides → pumpAndSettle → no exception.
      - Test 2: find.text('GET STARTED') returns findsOneWidget.
      - Test 3 (A11Y): `tester.ensureSemantics()` + `expectLater(tester, meetsGuideline(androidTapTargetGuideline))` + `handle.dispose()`.
  </action>
  <verify>
    <automated>flutter test test/features/welcome/welcome_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/welcome/welcome_screen.dart exists and contains `fadeInAnthem` call in addPostFrameCallback
    - lib/features/welcome/welcome_screen.dart contains `fadeOutAnthem` call (fire-and-forget) in _onStartPressed
    - lib/features/welcome/welcome_screen.dart contains `Future.delayed(const Duration(milliseconds: 850)` for navigation (Pitfall 5)
    - lib/features/welcome/usa_welcome_painter.dart contains `UsaWelcomePainter` class with shouldRepaint using `0.001` threshold
    - lib/features/welcome/usa_welcome_painter.dart paint() uses `canvas.save()` + scale transform + `canvas.restore()`
    - test/features/welcome/welcome_screen_test.dart exists and contains at least the smoke test and androidTapTargetGuideline test
    - `flutter test test/features/welcome/welcome_screen_test.dart` passes all tests
    - `flutter analyze lib/features/welcome/` exits 0
  </acceptance_criteria>
  <done>WelcomeScreen renders without error; stagger animation wired; anthem calls use new interface; test suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WelcomeScreen → UserPrefsRepository | getTutorialSeen() result determines navigation target; no trust issue (local SharedPreferences) |
| WelcomeScreen → AudioService | fadeInAnthem() called from addPostFrameCallback; no untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Spoofing | tutorial_seen flag cleared by user (SharedPreferences delete) | accept | Tutorial replay is benign; no security or COPPA risk |
| T-05-06 | Denial of Service | stagger AnimationController not disposed if WelcomeScreen navigated away mid-animation | mitigate | dispose() calls _staggerController.dispose() — covered in implementation |
| T-05-07 | Information Disclosure | Privacy Policy footer links to external URL | accept | url_launcher link is optional in v1 — Flags pattern uses empty onPressed; no PII transmitted |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages this phase |
</threat_model>

<verification>
`flutter test test/features/welcome/welcome_screen_test.dart` — all 3 tests pass.
`flutter analyze lib/features/welcome/ lib/app.dart` — exits 0.
Manual: tap GET STARTED on device/emulator — anthem fades out and navigation occurs after ~850ms.
</verification>

<success_criteria>
- /welcome is the app's initial route
- WelcomeScreen shows gradient + white USA silhouette filling in with stagger animation
- GET STARTED button fires fadeOutAnthem() and navigates to /tutorial or / after 850ms
- welcome_screen_test.dart passes including androidTapTargetGuideline check
</success_criteria>

<output>
Create `.planning/phases/05-polish-welcome-accessibility/05-03-SUMMARY.md` when done
</output>
