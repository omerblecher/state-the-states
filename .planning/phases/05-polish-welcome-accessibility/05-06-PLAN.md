---
phase: "05-polish-welcome-accessibility"
plan: 06
type: execute
wave: 2
depends_on: ["05-03"]
files_modified:
  - lib/features/home/home_screen.dart
  - lib/features/home/session_restore_card.dart
  - test/features/home/home_screen_test.dart
autonomous: true
requirements:
  - HOME-03

must_haves:
  truths:
    - "HomeScreen shows a SessionRestoreCard above mode cards when loadSession() returns non-null"
    - "SessionRestoreCard shows mode name, score, elapsed (MM:SS format), and states placed count"
    - "Continue button calls restoreGame() on gameSessionProvider.notifier then navigates to /play"
    - "Dismiss button calls clearSession() and rebuilds (card disappears)"
    - "HomeScreen shows no restore card when loadSession() returns null"
    - "home_screen_test.dart tests both shown and hidden card states"
  artifacts:
    - path: "lib/features/home/session_restore_card.dart"
      provides: "SessionRestoreCard widget with Continue and Dismiss"
      contains: "SessionRestoreCard"
    - path: "lib/features/home/home_screen.dart"
      provides: "FutureBuilder wrapping optional restore card above mode ListView"
      contains: "SessionRestoreCard"
    - path: "test/features/home/home_screen_test.dart"
      provides: "Tests for HOME-03 restore card shown/hidden states"
      contains: "HOME-03"
  key_links:
    - from: "lib/features/home/home_screen.dart"
      to: "lib/core/data/game_state_repository.dart"
      via: "ref.read(gameStateRepositoryProvider.future).then((r)=>r.loadSession())"
      pattern: "loadSession"
    - from: "lib/features/home/session_restore_card.dart"
      to: "lib/features/game/game_session_notifier.dart"
      via: "ref.read(gameSessionProvider.notifier).restoreGame()"
      pattern: "restoreGame"
---

<objective>
Add a "Resume your game" card to `HomeScreen` that appears when `GameStateRepository.loadSession()` returns a non-null saved session. Extract the card into `SessionRestoreCard` widget. Wire Continue (restoreGame + navigate) and Dismiss (clearSession + setState). Extend `home_screen_test.dart` for HOME-03 coverage.

Purpose: Satisfies HOME-03 ("on relaunch with a saved session, the home screen offers a 'continue game' dialog showing mode, score, elapsed time, and states placed").

Output: `session_restore_card.dart`, updated `home_screen.dart`, extended `home_screen_test.dart`.
</objective>

<execution_context>
@C:/Users/omerb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omerb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-polish-welcome-accessibility/05-RESEARCH.md
@.planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md
@.planning/phases/05-polish-welcome-accessibility/05-03-SUMMARY.md

<interfaces>
<!-- GameStateRepository interface (game_state_repository.dart) -->
abstract interface class GameStateRepository {
  Future<void> saveSession(GameSession session, {required int hintPenalty});
  Future<({GameSession session, int hintPenalty})?>  loadSession();
  Future<void> clearSession();
}
// Provider: gameStateRepositoryProvider — FutureProvider<GameStateRepository>

<!-- GameSession fields (game_session.dart) -->
// phase: GamePhase, mode: GameMode, score: int, elapsed: Duration,
// errorCount: int, activePostal: String?, hintsRemaining: int, matchedPostals: List<String>

<!-- GameMode enum values (game_mode.dart) -->
// learn, statesMaster, geographicalMaster, grandMaster
// .name returns camelCase string; for display use a label map or switch

<!-- GameSessionNotifier.restoreGame() (game_session_notifier.dart line 155) -->
// void restoreGame(GameSession restoredSession, {required int hintPenalty})
// Provider: gameSessionProvider — AsyncNotifierProvider<GameSessionNotifier, GameSession>

<!-- HomeScreen current structure (home_screen.dart lines 30–80) -->
// _buildBody(context, repo) returns Column with Header Padding, Subtitle Padding, Expanded ListView
// The restore card goes INSIDE _buildBody, wrapped in a FutureBuilder, ABOVE the Expanded ListView

<!-- home_screen_test.dart current structure -->
// Uses MockHighScoreRepository, buildHomeScreen() helper
// Tests: shows 4 mode cards, shows 'Not played', shows 'Best: N'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SessionRestoreCard widget</name>
  <files>lib/features/home/session_restore_card.dart</files>
  <read_first>
    - lib/features/home/home_screen.dart (lines 64–180 — _ModeCard pattern for card decoration, gradient, boxShadow, button layout to replicate)
    - lib/features/game/game_hud.dart (lines 30–40 — _formatElapsed() MM:SS format to copy exactly)
    - lib/features/game/game_mode.dart (FULL FILE — GameMode enum values and any display label logic)
    - lib/core/data/game_state_repository.dart (lines 1–15 — GameStateRepository interface for clearSession())
    - lib/features/game/game_session_notifier.dart (lines 155–175 — restoreGame() signature)
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§session_restore_card.dart — card decoration pattern, elapsed format, Semantics pattern)
  </read_first>
  <behavior>
    - Test 1: SessionRestoreCard shows mode label text
    - Test 2: SessionRestoreCard shows elapsed time in MM:SS format
    - Test 3: SessionRestoreCard shows states placed count (e.g. "23 / 50")
    - Test 4: tapping Continue button calls onContinue callback
    - Test 5: tapping Dismiss button calls onDismiss callback
  </behavior>
  <action>
    Create lib/features/home/session_restore_card.dart as a StatelessWidget (no Riverpod state needed — callbacks are provided by HomeScreen):

    Constructor params:
      `required GameSession session`
      `required int hintPenalty`
      `required VoidCallback onContinue`
      `required VoidCallback onDismiss`

    Elapsed format helper (private, identical to GameHud):
      `String _formatElapsed(Duration d) { final m = d.inMinutes.remainder(60).toString().padLeft(2,'0'); final s = (d.inSeconds % 60).toString().padLeft(2,'0'); return '$m:$s'; }`

    Mode display label: use a switch or const map to convert GameMode to display string: learn→'Learn', statesMaster→'States Master', geographicalMaster→'Geographical Master', grandMaster→'Grand Master'.

    Layout: A Container with margin EdgeInsets.fromLTRB(16, 8, 16, 4), decoration BoxDecoration(borderRadius: BorderRadius.circular(16), gradient: LinearGradient(colors: [Color(0xFF37474F), Color(0xFF263238)], begin: Alignment.topLeft, end: Alignment.bottomRight), boxShadow with blurRadius 12 offset (0,4)).
    Inside: Padding 16, Column with:
      Row: leading Icon(Icons.play_arrow, color: Colors.white70), SizedBox(8), Expanded Text('Resume ${modeLabel}', style white bold 16).
      SizedBox(4).
      Row with spacer: Text('Score: ${session.score}', white70), SizedBox(16), Text('Time: ${_formatElapsed(session.elapsed)}', white70), SizedBox(16), Text('${session.matchedPostals.length} / 50', white70).
      SizedBox(12).
      Row: Expanded(Semantics(button:true, label:'Continue ${modeLabel} game') wrapping ElevatedButton(onPressed: onContinue, 'CONTINUE', backgroundColor white, foregroundColor Color(0xFF263238))), SizedBox(8), Semantics(button:true, label:'Dismiss saved game') wrapping TextButton(onPressed: onDismiss, 'Dismiss', white70 style).

    The card must have a Semantics wrapper at the root: `Semantics(label: 'Resume ${modeLabel} game, score ${session.score}', child: ...)`.
  </action>
  <verify>
    <automated>flutter test test/features/home/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/home/session_restore_card.dart exists with class `SessionRestoreCard extends StatelessWidget`
    - SessionRestoreCard constructor accepts session, hintPenalty, onContinue, onDismiss
    - _formatElapsed logic uses `remainder(60)` or `% 60` for minutes and `% 60` for seconds with padLeft(2,'0')
    - SessionRestoreCard renders mode label (switch or map from GameMode enum)
    - `flutter analyze lib/features/home/session_restore_card.dart` exits 0
  </acceptance_criteria>
  <done>SessionRestoreCard widget created; shows mode, score, elapsed, states placed; Continue and Dismiss callbacks wired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add FutureBuilder restore card to HomeScreen and extend home_screen_test</name>
  <files>lib/features/home/home_screen.dart, test/features/home/home_screen_test.dart</files>
  <read_first>
    - lib/features/home/home_screen.dart (FULL FILE — _buildBody structure to modify)
    - test/features/home/home_screen_test.dart (FULL FILE — existing test helpers to extend)
    - lib/core/data/game_state_repository.dart (lines 1–70 — loadSession() return type, clearSession())
    - .planning/phases/05-polish-welcome-accessibility/05-PATTERNS.md (§home_screen.dart MODIFY — FutureBuilder pattern, import additions, restoreGame() call)
  </read_first>
  <behavior>
    - Test 1 (HOME-03 shown): pumpWidget with MockGameStateRepository returning a non-null session → pumpAndSettle → find 'CONTINUE' button → findsOneWidget
    - Test 2 (HOME-03 hidden): pumpWidget with MockGameStateRepository returning null → pumpAndSettle → find 'CONTINUE' → findsNothing
    - Test 3: existing mode card tests still pass (no regression)
  </behavior>
  <action>
    In lib/features/home/home_screen.dart:

    1. Add imports at the top:
       `import 'package:state_states/core/data/game_state_repository.dart';`
       `import 'package:state_states/features/game/game_session_notifier.dart';`
       `import 'session_restore_card.dart';`

    2. In `_buildBody()`, add a FutureBuilder at the TOP of the Column children list, BEFORE the existing Header Padding:
       `FutureBuilder<({GameSession session, int hintPenalty})?>(future: ref.read(gameStateRepositoryProvider.future).then((repo) => repo.loadSession()), builder: (context, snapshot) { if (!snapshot.hasData || snapshot.data == null) return const SizedBox.shrink(); final saved = snapshot.data!; return SessionRestoreCard(session: saved.session, hintPenalty: saved.hintPenalty, onContinue: () { ref.read(gameSessionProvider.notifier).restoreGame(saved.session, hintPenalty: saved.hintPenalty); context.go('/play', extra: saved.session.mode); }, onDismiss: () async { final repo = await ref.read(gameStateRepositoryProvider.future); await repo.clearSession(); if (mounted) setState(() {}); }, ); },)`

    In test/features/home/home_screen_test.dart:

    3. Add `MockGameStateRepository extends Mock implements GameStateRepository` class.

    4. Update `buildHomeScreen()` helper to accept an optional `MockGameStateRepository? mockGameStateRepo` parameter. If provided, add `gameStateRepositoryProvider.overrideWith((_) async => mockGameStateRepo!)` to the ProviderScope overrides.

    5. Add a new group 'HomeScreen session restore (HOME-03)' with:
       Test 1: mock returning non-null session (create a minimal GameSession with mode: GameMode.learn, score: 42, elapsed: Duration(seconds: 90), matchedPostals: List.filled(23, 'TX'), hintsRemaining: 1, phase: GamePhase.playing, errorCount: 2) → verify find.text('CONTINUE') or find.byType(SessionRestoreCard) findsOneWidget after pumpAndSettle.
       Test 2: mock returning null → verify find.text('CONTINUE') findsNothing.

    6. Register fallback value for GameSession in setUpAll (mocktail requirement): `registerFallbackValue(const GameSession(...))`.
  </action>
  <verify>
    <automated>flutter test test/features/home/home_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - home_screen.dart imports game_state_repository.dart and session_restore_card.dart
    - home_screen.dart _buildBody() contains `FutureBuilder` that calls `loadSession()`
    - home_screen.dart contains `restoreGame(saved.session, hintPenalty: saved.hintPenalty)` call in onContinue
    - home_screen.dart contains `clearSession()` call in onDismiss
    - test/features/home/home_screen_test.dart contains HOME-03 test group
    - `flutter test test/features/home/home_screen_test.dart` passes all tests including new HOME-03 group
    - `flutter analyze lib/features/home/` exits 0
  </acceptance_criteria>
  <done>Restore card shown when session exists, hidden when none; both paths covered by tests; all existing home screen tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HomeScreen → GameStateRepository | loadSession() reads local SharedPreferences; no external data source |
| SessionRestoreCard → GameSessionNotifier | restoreGame() restores local session; no network call |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | Tampering | Corrupted session JSON in SharedPreferences | mitigate | GameStateRepository.loadSession() already has try/catch that silently discards and clears corrupted keys (Phase 2 D-08) |
| T-05-14 | Spoofing | User manually edits SharedPreferences to inject bogus session | accept | Session data is used locally; no server-side trust; worst case is a corrupt game state that errors gracefully |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new packages |
</threat_model>

<verification>
`flutter test test/features/home/home_screen_test.dart` — all groups pass.
`flutter analyze lib/features/home/` — exits 0.
Manual: start a game, kill the app, relaunch → resume card appears with correct mode/score/elapsed/count. Tap Dismiss → card gone. Relaunch again → card gone.
</verification>

<success_criteria>
- SessionRestoreCard displays mode, score, MM:SS elapsed, states placed count
- Continue calls restoreGame() then navigates to /play
- Dismiss calls clearSession() and rebuilds home screen
- home_screen_test HOME-03 group: shown (findsOneWidget) and hidden (findsNothing) both pass
</success_criteria>

<output>
Create `.planning/phases/05-polish-welcome-accessibility/05-06-SUMMARY.md` when done
</output>
