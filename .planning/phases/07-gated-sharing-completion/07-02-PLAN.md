---
phase: 07-gated-sharing-completion
plan: 02
type: execute
wave: 1
depends_on:
  - "07-01"
files_modified:
  - lib/features/map/completion_screen.dart
autonomous: true
requirements:
  - SHARE-01
  - SHARE-02
  - SHARE-03
  - SHARE-04

must_haves:
  truths:
    - "Share button absent when _isNewPb == false (non-PB completion)"
    - "Share button present when _isNewPb == true (score < previousBest)"
    - "Math gate uses 2-digit × 1-digit multiplication operands and checks _a * _b"
    - "Wrong answer regenerates operands and shows 'Incorrect — try again'; dialog stays open"
    - "Correct answer proceeds to _captureAndShare(); temp PNG deleted in finally block"
    - "Share message is exactly: 'New lowest score in $modeName! Score: $score — State the States 🇺🇸'"
    - "GameSessionNotifier has zero imports from this file (walled-garden rule COMP-03)"
  artifacts:
    - path: "lib/features/map/completion_screen.dart"
      provides: "All SHARE-01 through SHARE-04 production implementation"
      contains: "_scoreCardKey"
    - path: "lib/features/map/completion_screen.dart"
      provides: "_captureAndShare method"
      contains: "Directory.systemTemp"
    - path: "lib/features/map/completion_screen.dart"
      provides: "_showParentalGate method"
      contains: "barrierDismissible: false"
  key_links:
    - from: "_CompletionScreenState._onSharePressed"
      to: "_showParentalGate"
      via: "await _showParentalGate()"
      pattern: "_showParentalGate"
    - from: "_showParentalGate"
      to: "_MathChallengeDialog"
      via: "showDialog(builder: (_) => const _MathChallengeDialog())"
      pattern: "_MathChallengeDialog"
    - from: "_captureAndShare"
      to: "_scoreCardKey"
      via: "_scoreCardKey.currentContext?.findRenderObject() as RenderRepaintBoundary?"
      pattern: "_scoreCardKey"
    - from: "RepaintBoundary at line ~202"
      to: "_scoreCardKey"
      via: "key: _scoreCardKey"
      pattern: "key: _scoreCardKey"
---

<objective>
Implement all three surgical changes to completion_screen.dart that complete the Phase 7 gated sharing feature:

1. PB-gate the Share button behind `if (_isNewPb)` (SHARE-01)
2. Upgrade _MathChallengeDialog from addition to 2-digit × 1-digit multiplication with wrong-answer operand regeneration (SHARE-04)
3. Add screenshot capture + share flow: _scoreCardKey GlobalKey, RepaintBoundary key, _captureAndShare(), _showParentalGate(), refactored _onSharePressed (SHARE-02 + SHARE-03)

Purpose: Complete the v2 sharing stub. One file changes; no new packages; no routes; no notifier changes.

Output: Updated completion_screen.dart where all 4 SHARE requirements are satisfied; full test suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-gated-sharing-completion/07-RESEARCH.md
@.planning/phases/07-gated-sharing-completion/07-UI-SPEC.md

<interfaces>
<!-- Current state of lib/features/map/completion_screen.dart — key facts for executor -->

Existing imports (line 1–9):
  import 'dart:math' as math;
  import 'package:flutter/material.dart';
  import 'package:flutter_riverpod/flutter_riverpod.dart';
  import 'package:go_router/go_router.dart';
  import 'package:share_plus/share_plus.dart';
  import 'package:state_states/features/game/game_mode.dart';
  import 'package:state_states/features/game/game_session.dart';

THREE NEW IMPORTS REQUIRED (add after existing imports):
  import 'dart:io';
  import 'dart:ui' as ui;
  import 'package:flutter/rendering.dart';

Existing state fields in _CompletionScreenState (lines 46–49):
  int _starCount = 3;
  bool _isNewPb = false;
  bool _showPbOverlay = false;
  late AnimationController _pbController;

TWO NEW STATE FIELDS TO ADD:
  final GlobalKey _scoreCardKey = GlobalKey();
  bool _isSharing = false;

RepaintBoundary location (line 202 — no key currently):
  RepaintBoundary(
    child: Card(...)
  )
CHANGE TO:
  RepaintBoundary(
    key: _scoreCardKey,
    child: Card(...)
  )

Share button section (lines 293–309 — unconditional currently):
  const SizedBox(height: 12),
  SizedBox(width: double.infinity, height: 48, child: OutlinedButton.icon(onPressed: _onSharePressed, ...))
REPLACE WITH: PB-gated + _isSharing spinner (see action section)

Current _onSharePressed (lines 89–102):
  Future<void> _onSharePressed() async {
    final passed = await showDialog<bool>(context: context, builder: (_) => const _MathChallengeDialog());
    if (passed != true || !mounted) return;
    final elapsed = _formatTime(widget.session.elapsed);
    final modeName = widget.session.mode.name;      // ← uses .name not .displayName
    final score = widget.session.score;
    await SharePlus.instance.share(ShareParams(text: 'I placed all 50 US states in $elapsed on $modeName mode! Score: $score — State the States 🇺🇸'));
  }
REPLACE BODY ENTIRELY with: delegation to _showParentalGate() and _captureAndShare()

_MathChallengeDialog.initState (lines 355–359) — SEED-BASED ADDITION:
  final seed = DateTime.now().millisecondsSinceEpoch;
  _a = 3 + seed % 7;           // 3–9
  _b = 2 + (seed ~/ 13) % 8;  // 2–9

_MathChallengeDialog._onConfirm (line 369) — ADDITION CHECK:
  if (entered == _a + _b) { Navigator.of(context).pop(true); }
  else { setState(() => _error = 'Incorrect — try again'); }

_MathChallengeDialog.build question text (line 387):
  'What is $_a + $_b?'

GameMode.displayName extension is in lib/features/game/game_mode.dart:
  learn → "Learn"
  statesMaster → "States Master"
  geographicalMaster → "Geographical Master"
  grandMaster → "Grand Master"
  speedTyping → "Name all the states"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add imports, state fields, RepaintBoundary key, and PB-gated Share button</name>
  <files>lib/features/map/completion_screen.dart</files>

  <read_first>
    - lib/features/map/completion_screen.dart — read the full file before any edits; confirm line numbers for: imports block (lines 1–9), state fields (lines 46–49), RepaintBoundary (line 202), Share button section (lines 293–309)
  </read_first>

  <behavior>
    - Share button is absent when CompletionScreen is built with score=125, previousBest=100
    - Share button is present when CompletionScreen is built with score=50, previousBest=100
    - CircularProgressIndicator appears in place of Share button when _isSharing == true
  </behavior>

  <action>
    Make four changes to completion_screen.dart:

    CHANGE 1 — Add three new imports after the existing import block (after 'package:state_states/features/game/game_session.dart'):
      import 'dart:io';
      import 'dart:ui' as ui;
      import 'package:flutter/rendering.dart';

    CHANGE 2 — Add two new fields to _CompletionScreenState immediately after the existing `late AnimationController _pbController;` declaration:
      final GlobalKey _scoreCardKey = GlobalKey();
      bool _isSharing = false;

    CHANGE 3 — Add key to the RepaintBoundary at line ~202. Change:
      RepaintBoundary(
        child: Card(
    to:
      RepaintBoundary(
        key: _scoreCardKey,
        child: Card(

    CHANGE 4 — Replace the unconditional Share button section (the `const SizedBox(height: 12)` and the `SizedBox` containing the OutlinedButton.icon for _onSharePressed, lines 293–309) with:
      if (_isNewPb) ...[
        const SizedBox(height: 12),
        if (_isSharing)
          const SizedBox(
            height: 48,
            child: Center(child: CircularProgressIndicator()),
          )
        else
          SizedBox(
            width: double.infinity,
            height: 48,
            child: OutlinedButton.icon(
              onPressed: _onSharePressed,
              style: OutlinedButton.styleFrom(
                foregroundColor: Colors.grey.shade700,
                side: BorderSide(color: Colors.grey.shade400),
                shape: RoundedRectangleBorder(
                  borderRadius: BorderRadius.circular(14),
                ),
              ),
              icon: const Icon(Icons.share),
              label: const Text('Share result'),
            ),
          ),
      ],

    Do NOT modify _onSharePressed, _MathChallengeDialog, or any other method yet — those are Task 2.
  </action>

  <verify>
    <automated>flutter test test/features/map/completion_screen_test.dart --no-pub</automated>
  </verify>

  <acceptance_criteria>
    - completion_screen.dart contains `import 'dart:io';` (no alias)
    - completion_screen.dart contains `import 'dart:ui' as ui;`
    - completion_screen.dart contains `import 'package:flutter/rendering.dart';`
    - completion_screen.dart contains `final GlobalKey _scoreCardKey = GlobalKey();` in _CompletionScreenState
    - completion_screen.dart contains `bool _isSharing = false;` in _CompletionScreenState
    - completion_screen.dart contains `key: _scoreCardKey,` on the RepaintBoundary line
    - completion_screen.dart contains `if (_isNewPb) ...[` guarding the Share button section
    - completion_screen.dart contains `if (_isSharing)` inside the `if (_isNewPb)` block
    - `flutter test test/features/map/completion_screen_test.dart --no-pub` — SHARE-01 tests (absent on non-PB, present on PB) both pass; pre-existing 11 tests all pass
    - `flutter analyze lib/features/map/completion_screen.dart --no-pub` exits 0 (no errors)
  </acceptance_criteria>

  <done>
    SHARE-01 fully implemented and verified green. Imports and state fields in place for Tasks 2 to build on.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Upgrade math gate to multiplication + add _showParentalGate and _captureAndShare</name>
  <files>lib/features/map/completion_screen.dart</files>

  <read_first>
    - lib/features/map/completion_screen.dart — re-read after Task 1 edits to confirm current state before making further changes; specifically locate: _onSharePressed (lines ~89–102), _MathChallengeDialogState.initState (lines ~354–359), _MathChallengeDialogState._onConfirm (lines ~367–374), build method question Text widget (line ~387)
  </read_first>

  <behavior>
    - _MathChallengeDialog with correct answer (a * b) dismisses and returns true
    - _MathChallengeDialog with wrong answer stays open, shows 'Incorrect — try again', regenerates operands
    - _MathChallengeDialog with Cancel dismisses and returns false
    - _captureAndShare invoked after gate returns true (math gate passes)
    - Share message text is 'New lowest score in $modeName! Score: $score — State the States 🇺🇸' where modeName = widget.session.mode.displayName
  </behavior>

  <action>
    Make three groups of changes to completion_screen.dart:

    GROUP A — Upgrade _MathChallengeDialogState:

    A1. Change _a and _b fields from `late final` to `late int` (they must be mutable for regeneration):
      Change: `late final int _a;` → `late int _a;`
      Change: `late final int _b;` → `late int _b;`

    A2. Replace initState body (the seed-based operand generation):
      Remove:
        final seed = DateTime.now().millisecondsSinceEpoch;
        _a = 3 + seed % 7;
        _b = 2 + (seed ~/ 13) % 8;
      Replace with:
        final rng = math.Random();
        _a = 10 + rng.nextInt(90);
        _b = 2 + rng.nextInt(8);

    A3. Replace _onConfirm body:
      Remove:
        if (entered == _a + _b) {
          Navigator.of(context).pop(true);
        } else {
          setState(() => _error = 'Incorrect — try again');
        }
      Replace with:
        if (entered == _a * _b) {
          Navigator.of(context).pop(true);
        } else {
          _controller.clear();
          final rng = math.Random();
          _a = 10 + rng.nextInt(90);
          _b = 2 + rng.nextInt(8);
          setState(() => _error = 'Incorrect — try again');
        }

    A4. In build(), change question Text from `'What is $_a + $_b?'` to `'What is $_a × $_b?'`
      The × character is Unicode multiplication sign U+00D7, NOT the letter x.

    GROUP B — Refactor _onSharePressed and add _showParentalGate:

    B1. Replace entire body of _onSharePressed with:
        final passed = await _showParentalGate();
        if (passed != true || !mounted) return;
        await _captureAndShare();

    B2. Add new method _showParentalGate immediately after _onSharePressed:
        Future<bool?> _showParentalGate() {
          return showDialog<bool>(
            context: context,
            barrierDismissible: false,
            builder: (_) => const _MathChallengeDialog(),
          );
        }

    GROUP C — Add _captureAndShare method after _showParentalGate:

    C1. Add the following method (use widget.session.mode.displayName not .name):
        Future<void> _captureAndShare() async {
          if (!mounted) return;
          setState(() => _isSharing = true);
          File? file;
          try {
            final boundary = _scoreCardKey.currentContext
                ?.findRenderObject() as RenderRepaintBoundary?;
            if (boundary == null) return;

            final image = await boundary.toImage(pixelRatio: 3.0);
            final byteData =
                await image.toByteData(format: ui.ImageByteFormat.png);
            if (byteData == null) return;

            final bytes = byteData.buffer.asUint8List();
            file = File('${Directory.systemTemp.path}/score_card.png');
            await file.writeAsBytes(bytes);

            final modeName = widget.session.mode.displayName;
            final score = widget.session.score;

            await SharePlus.instance.share(ShareParams(
              text:
                  'New lowest score in $modeName! Score: $score — State the States 🇺🇸',
              files: [XFile(file.path)],
            ));
          } finally {
            file?.deleteSync();
            if (mounted) setState(() => _isSharing = false);
          }
        }

    Also update the doc comment on CompletionScreen class: remove the note "No share_plus or AdMob calls (D-13 — v2 only)" — replace it with "Share flow is widget-layer only; GameSessionNotifier has zero ad/share imports (COMP-03 walled-garden)."
  </action>

  <verify>
    <automated>flutter test test/features/map/completion_screen_test.dart --no-pub</automated>
  </verify>

  <acceptance_criteria>
    - completion_screen.dart contains `_a = 10 + rng.nextInt(90);` in _MathChallengeDialogState.initState
    - completion_screen.dart contains `_b = 2 + rng.nextInt(8);` in _MathChallengeDialogState.initState
    - completion_screen.dart contains `entered == _a * _b` in _MathChallengeDialogState._onConfirm
    - completion_screen.dart contains `_controller.clear();` in the wrong-answer branch of _onConfirm
    - completion_screen.dart contains `What is $_a × $_b?` (U+00D7 multiplication sign, not letter x)
    - completion_screen.dart contains `barrierDismissible: false` in _showParentalGate
    - completion_screen.dart contains `Future<bool?> _showParentalGate()`
    - completion_screen.dart contains `Future<void> _captureAndShare()`
    - completion_screen.dart contains `Directory.systemTemp.path` in _captureAndShare
    - completion_screen.dart contains `file?.deleteSync();` in the finally block
    - completion_screen.dart contains `widget.session.mode.displayName` (not `.name`) in _captureAndShare
    - completion_screen.dart contains `New lowest score in $modeName! Score: $score — State the States` (exact prefix; emoji follows)
    - completion_screen.dart contains `XFile(file.path)` in the ShareParams files list
    - `flutter test test/features/map/completion_screen_test.dart --no-pub` exits 0: all tests pass including SHARE-04 multiplication tests
    - `flutter test --no-pub` exits 0: full suite green
    - `flutter analyze lib/features/map/completion_screen.dart --no-pub` exits 0
    - completion_screen.dart does NOT contain any import of a GameSessionNotifier, AdService, or any package from lib/features/game/game_session_notifier.dart (COMP-03 walled-garden)
  </acceptance_criteria>

  <done>
    All four SHARE requirements implemented: math gate upgraded to multiplication (SHARE-04), PB gate live (SHARE-01), screenshot capture wired (SHARE-02), share message correct (SHARE-03). Full test suite green. Manual verification of SHARE-02 capture path on real device is the only remaining check (noted in VALIDATION.md as manual-only).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → math gate answer | TextField accepts free text; int.tryParse() in _onConfirm nullifies non-numeric input (returns null → never equals _a * _b) |
| widget layer → native share sheet | SharePlus.instance.share() bridges to OS; no app-level trust boundary; OS handles recipient selection |
| temp file → filesystem | score_card.png written to Directory.systemTemp; deleted in finally block immediately after share sheet returns |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing / V4 Access Control | _MathChallengeDialog math gate | mitigate | 2-digit × 1-digit multiplication provides solution space of ~720 unique answers (10–99 × 2–9); operands regenerate on each wrong attempt; int.tryParse() prevents injection; COPPA requires "reasonable" friction not cryptographic auth |
| T-07-02 | Information Disclosure | shared PNG content | accept | Score card contains only: score (int), time (Duration formatted), mode name (string), previous best (int or absent); no PII, no device identifiers, no user account data |
| T-07-03 | Information Disclosure | temp file persistence after crash | mitigate | File? file declared before try; file?.deleteSync() in finally block; Android/iOS OS temp cleanup covers the residual crash case; file path is /tmp/score_card.png (not user-accessible storage) |
| T-07-SC | Tampering | No new package installs in this phase | accept | No pubspec.yaml changes; share_plus 13.1.0 and dart:io/dart:ui are already resolved; no slopcheck required |

</threat_model>

<verification>
After Plan 02 completes:
- `flutter test test/features/map/completion_screen_test.dart --no-pub` exits 0
- `flutter test --no-pub` exits 0
- `flutter analyze lib/features/map/completion_screen.dart --no-pub` exits 0
- Manual device check (per VALIDATION.md): complete a game at lower score than best, tap Share, pass multiplication gate, confirm share sheet shows score_card.png with message "New lowest score in [Mode Name]! Score: [N] — State the States 🇺🇸"
</verification>

<success_criteria>
Phase 7 complete when:
1. SHARE-01: `flutter test` confirms Share button absent when _isNewPb == false, present when _isNewPb == true
2. SHARE-02: Manual device verification confirms score_card.png attaches to native share sheet
3. SHARE-03: Manual device verification confirms share message matches exact format
4. SHARE-04: `flutter test` confirms _MathChallengeDialog uses multiplication (_a * _b), wrong answer regenerates operands, cancel returns false
5. Full test suite (`flutter test --no-pub`) exits 0
6. `flutter analyze` exits 0 on the modified file
</success_criteria>

<output>
Create `.planning/phases/07-gated-sharing-completion/07-02-SUMMARY.md` when done
</output>
