---
phase: 07-gated-sharing-completion
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - test/features/map/completion_screen_test.dart
autonomous: true
requirements:
  - SHARE-01
  - SHARE-04

must_haves:
  truths:
    - "Widget test proves Share button is absent when _isNewPb == false"
    - "Widget test proves Share button is present when _isNewPb == true (score < previousBest)"
    - "MathChallengeDialog test proves correct answer (a * b) dismisses dialog with true"
    - "MathChallengeDialog test proves wrong answer keeps dialog open and shows error"
    - "MathChallengeDialog test proves Cancel dismisses dialog with false"
    - "Widget test proves Share button is absent when previousBest is null (first game)"
    - "All 11 pre-existing tests continue to pass"
  artifacts:
    - path: "test/features/map/completion_screen_test.dart"
      provides: "Wave 0 test cases for SHARE-01 and SHARE-04"
      contains: "Share result"
  key_links:
    - from: "test/features/map/completion_screen_test.dart"
      to: "lib/features/map/completion_screen.dart"
      via: "import package:state_states/features/map/completion_screen.dart"
      pattern: "completion_screen"
---

<objective>
Add Wave 0 test cases for Phase 7 behaviors to the existing completion_screen_test.dart. These tests are written before the implementation changes so they start RED (failing), then turn GREEN when Plan 02 completes.

Purpose: Establish a Nyquist-compliant automated gate that verifies PB-visibility gating (SHARE-01) and multiplication math gate behavior (SHARE-04) without requiring any changes to the production file.

Output: New test group "Phase 7 gated sharing" appended to completion_screen_test.dart; all 11 pre-existing tests continue to pass; 6 new tests fail until Plan 02 implements the production changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-gated-sharing-completion/07-VALIDATION.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add Phase 7 widget test cases (SHARE-01 + SHARE-04)</name>
  <files>test/features/map/completion_screen_test.dart</files>

  <read_first>
    - test/features/map/completion_screen_test.dart — read the full file before editing; the existing 11 tests and helpers (makeSession, buildScreen) must be preserved; append the new group at the end of main()
    - lib/features/map/completion_screen.dart — read to confirm: (a) Share button label text is 'Share result', (b) _MathChallengeDialog class name (Plan 02 renames it to MathChallengeDialog — public, no underscore prefix); after Plan 02's rename the class is importable as MathChallengeDialog from completion_screen.dart, (c) the dialog title is 'Grown-up check'

    NOTE ON CLASS VISIBILITY: Plan 02 Task 1 renames _MathChallengeDialog to MathChallengeDialog (public class, @visibleForTesting). This plan's tests MUST reference MathChallengeDialog (no underscore). In Dart, underscore-prefixed private names are library-scoped and NOT accessible from a different library (even via import). Because test/features/map/completion_screen_test.dart is a separate library from lib/features/map/completion_screen.dart, using _MathChallengeDialog in the test file would cause a compile error ('The name _MathChallengeDialog isn't defined'). Use the public name MathChallengeDialog throughout this test file.
  </read_first>

  <behavior>
    New group name: 'Phase 7 gated sharing'

    Test 1 — SHARE-01 absent on non-PB:
      - Build CompletionScreen with score=125, previousBest=100 (score > previousBest → _isNewPb == false)
      - After pump(), find.text('Share result') findsNothing
      - Wave 0 status: FAILS (current code shows Share button unconditionally, so findsNothing assertion fails)

    Test 2 — SHARE-01 present on PB:
      - Build CompletionScreen with score=50, previousBest=100 (score < previousBest → _isNewPb == true)
      - After pump(), find.text('Share result') findsOneWidget
      - Wave 0 status: PASSES (current code shows Share button unconditionally, so findsOneWidget succeeds; this test turns RED if Share button is ever removed entirely, providing regression protection)

    Test 3 — SHARE-04 correct answer pops dialog with true:
      - Build a widget containing a TextButton that calls showDialog(builder: (_) => const MathChallengeDialog()) and stores result
      - The dialog currently checks _a + _b; after Plan 02 it will check _a * _b
      - This test must verify the CORRECT POST-PLAN-02 behavior: after finding 'What is' text in dialog, parse both operands from the displayed question, compute product, enter it, tap 'Share', verify dialog returned true
      - NOTE: Because _a and _b are generated randomly in initState, the test must read the displayed question text to extract the operands. Use tester.widget<Text>(find.textContaining('What is')) to get the displayed string, then parse operands from it. This is the only way to supply the correct answer for a randomly-generated problem.
      - Mark with a comment: // RED until Plan 02 upgrades MathChallengeDialog to multiplication

    Test 4 — SHARE-04 wrong answer keeps dialog open:
      - Build same dialog-launching widget
      - Enter '9999' (guaranteed wrong for any 2-digit × 1-digit problem where max is 99×9=891)
      - Tap 'Share'
      - find.text('Incorrect — try again') findsOneWidget; dialog is still present (find.byType(AlertDialog) findsOneWidget)

    Test 5 — SHARE-04 cancel returns false:
      - Build same dialog-launching widget
      - Tap 'Cancel'
      - Dialog dismissed; result stored as false (assert result == false or find.byType(AlertDialog) findsNothing)

    Test 6 — SHARE-01 absent on first game (previousBest == null):
      - Build CompletionScreen with score=50, previousBest=null (no previous best recorded)
      - After pump(), find.text('Share result') findsNothing
      - Wave 0 status: FAILS (current code shows Share button unconditionally)
  </behavior>

  <action>
    Append a new testWidgets group named 'Phase 7 gated sharing' to the end of main() in completion_screen_test.dart.

    For tests 1, 2, and 6 (SHARE-01 visibility), use the existing buildScreen() helper from the file. No new helpers needed.

    For tests 3, 4, and 5 (dialog behavior), create a local helper inside the group that wraps MathChallengeDialog in a MaterialApp with a Scaffold containing a TextButton. The TextButton's onPressed calls showDialog<bool>(context: context, barrierDismissible: false, builder: (_) => const MathChallengeDialog()) and stores the result. Use a ValueNotifier<bool?> or setState in a StatefulBuilder to capture the result.

    Concrete approach for dialog result capture: use a local variable `bool? dialogResult;` declared before pumpWidget, then inside the showDialog callback do `dialogResult = result;` after the await. Since flutter_test does not allow async callbacks in onPressed directly, use tester.runAsync() to await the dialog-show future, or use a simpler approach: pump the dialog directly by pushing it onto a Navigator, not via a trigger button. Simplest approach: use showDialog in a testWidgets with await tester.runAsync(() => showDialog(...)) pattern.

    For test 3 (correct answer): after pumping the dialog, use find.textContaining('What is') to get the rendered Text widget. Extract the question string via (tester.widget<Text>(find.textContaining('What is ×'))).data to get e.g. 'What is 42 × 7?'. Parse operands with RegExp(r'What is (\d+) × (\d+)') — note the × character is Unicode U+00D7 (not letter x). Compute product. Enter into TextField via tester.enterText. Tap ElevatedButton with text 'Share'. After pumpAndSettle, the dialog should be dismissed.

    For test 4 (wrong answer): enter '9999' in the TextField. Tap 'Share'. After pump(), find.text('Incorrect — try again') findsOneWidget.

    For test 5 (cancel): tap 'Cancel'. After pumpAndSettle(), find.byType(AlertDialog) findsNothing.

    For test 6 (first game): call buildScreen(makeSession(score: 50), previousBest: null). After pump(), find.text('Share result') findsNothing.

    Add a comment at the top of tests 3–5:
    // SHARE-04: RED until Plan 02 upgrades MathChallengeDialog from addition to multiplication.
    // Test 3 passes when _a * _b is checked; currently _a + _b will give wrong result for parsed operands.

    IMPORTANT: Use MathChallengeDialog (public name, no underscore) throughout — NOT _MathChallengeDialog. Plan 02 Task 1 renames the class to public. The underscore-private name is not accessible from this test library and will cause a compile error if used.

    Do NOT modify any existing test. Do NOT change imports — MathChallengeDialog is in completion_screen.dart which is already imported.
  </action>

  <verify>
    <automated>flutter test test/features/map/completion_screen_test.dart --no-pub</automated>
  </verify>

  <acceptance_criteria>
    - test/features/map/completion_screen_test.dart contains a group named 'Phase 7 gated sharing'
    - The group contains at least 6 testWidgets calls
    - Test 1 (SHARE-01 absent on non-PB): FAILS at Wave 0 — current code shows Share button unconditionally, so the findsNothing assertion fails. This is the expected RED state.
    - Test 2 (SHARE-01 present on PB): PASSES at Wave 0 — current code shows Share button unconditionally, so findsOneWidget succeeds. This test turns RED if the button is later removed entirely.
    - Test 3 (correct answer): FAILS at Wave 0 — current dialog uses addition (_a + _b); test expects multiplication logic. RED until Plan 02.
    - Test 4 (wrong answer): behavior depends on current dialog state; may pass or fail at Wave 0.
    - Test 5 (cancel): behavior depends on current dialog state; may pass or fail at Wave 0.
    - Test 6 (previousBest == null): FAILS at Wave 0 — current code shows Share button unconditionally, so findsNothing assertion fails. RED until Plan 02.
    - All 11 pre-existing tests still pass (the existing groups are unmodified)
    - `flutter test test/features/map/completion_screen_test.dart --no-pub` exits with output showing test count >= 17 (11 existing + 6 new)
    - No Dart compile errors (MathChallengeDialog is the public class name used throughout — no underscore prefix)
  </acceptance_criteria>

  <done>
    Wave 0 test cases exist in completion_screen_test.dart; the file compiles without error; pre-existing 11 tests pass; 6 new tests are visible as failures or passes that will be locked green by Plan 02.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → production widget | Test imports MathChallengeDialog from completion_screen.dart; no untrusted input crosses this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing / V4 Access Control | MathChallengeDialog math gate | accept | Test file verifies gate logic; multiplication provides larger solution space than addition; COPPA requires reasonable not unbreakable gate |
| T-07-SC | Tampering | No new packages installed in this plan | accept | No npm/pip/cargo installs; flutter SDK only |

</threat_model>

<verification>
After Plan 01 completes:
- `flutter test test/features/map/completion_screen_test.dart --no-pub` exits 0 (pre-existing tests pass) or exits with new test failures only
- All 11 pre-existing test names appear in output with status passed
- New group 'Phase 7 gated sharing' appears in output with 6 test cases
</verification>

<success_criteria>
Wave 0 complete when: completion_screen_test.dart has >= 17 test cases, compiles without error, all 11 pre-existing tests pass, and new tests are present (some will fail until Plan 02 implements the production changes).
</success_criteria>

<output>
Create `.planning/phases/07-gated-sharing-completion/07-01-SUMMARY.md` when done
</output>
